; Clip the linked code arena (S3 ceiling is 192 KB).
; LVGLBENCH.AXE is ~137 KB of .text — at 192 KB it runs from IRAM,
; below that the loader uses flash XIP. Reboot after copying to C:\SYSTEM.CFG.
[memory]
app_arena_kb = 192
