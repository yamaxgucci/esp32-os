# DESKTOP.AXE — графическая оболочка в стиле Windows 3.11

Рабочий стол, окна, папки, файлы, мышь. Загружаемое приложение, **не часть
прошивки**: ядро об оболочке не знает.

План, принятые решения и список того, чего здесь сознательно нет —
[`docs/plans/desktop.md`](../../docs/plans/desktop.md). Читать его первым.

**Состояние: все четыре этапа сделаны, включая замер на плате.**
Стол, окна, меню, диалоги, значки, диски, окно папки со строкой состояния,
запуск программ, файловые операции (`F8`/`F7`/`F2`/`Delete`, создать каталог,
свойства файла и диска) и `DESKTOP.INI` — стол возвращается таким, каким его
оставили.

Страница для пользователя — [`docs/user/05-desktop.md`](../../docs/user/05-desktop.md).

## На плате: CYD, 9 сентября 2026

Мерено на ESP32-2432S028R (`argon target esp32`), поверхность 160×120,
панель ILI9341 по SPI2 на 80 МГц, тач XPT2046. Числа снимает сама оболочка:
`run c:\desktop.axe -bench`.

| Что | Сколько |
|---|---|
| Открыть окно на `C:\` (чтение каталога и раскладка) | **2 мс** |
| Первая отрисовка этого окна | **17 мс** |
| Перетаскивание окна, шаг | **17 мс худший, 17 мс средний** |
| из них флаш на панель | **15 мс** |
| Полная перерисовка экрана (`F5`) | **25 мс** |

Читается это так: **рисование стоит 2 мс, остальное — панель.** Кадр в 17 мс —
это около шестидесяти в секунду, то есть на CYD стол не тормозит, и упирается
он в SPI, а не в свою арифметику. Экономить тут можно только площадью
повреждённого прямоугольника — что оболочка и делает.

Замер **самозаводящийся, и это не замена пальцу**. По сенсорной панели скрипт
не потыкает, а виртуальные драйверы ввода требуют радио, которое на этой плате
съедает буферы UART. Поэтому `-bench` прогоняет оболочку по трём вещам сам, и
в числах **нет задержки тача** — она принадлежит панели, а не столу, и число,
включающее обе, скрывало бы, какая половина виновата.

**320×240 на этой плате нельзя, и это измерено, а не выведено.** Попросил —
получил `display: no memory for 320x240 framebuffer`: 320×240 в RGB565 это
153 600 байт, а самый большой свободный блок здесь 65–72 КБ, PSRAM нет
(`0 KB extended`). Путь к полному экрану один — полосовой растеризатор из §9.2
плана: он рисует повреждённый прямоугольник слоями прямо в панель и целого
буфера не требует вовсе. Заодно по времени: флаш 37 КБ стоит 15 мс, значит
150 КБ стоили бы около 60 — шестнадцать кадров в секунду на полный экран.

**Почему кажется, что 320×240 можно.** Прошивка `monitor` (та, что делает из
CYD экран для S3) именно столько и берёт: `frame buffer 153600 bytes: yes`,
после чего у неё остаётся 128 КБ. И это правда — но она **одна на всём чипе**:
голое приложение ESP-IDF без ядра. Под ArgonOS 124 КБ уже занято системой, и
самый большой свободный блок — 65 КБ. То есть дело не в панели и не в чипе, а
в том, кто ещё в памяти.

**Палец проверен человеком, 9 сентября 2026.** Оболочка на стекле CYD
работает от касания: иконки выбираются, перетаскиваются и запускаются пальцем,
без мыши и без клавиатуры. Скриптом это не проверить (панель резистивная, а
виртуальные драйверы ввода требуют радио, которое здесь съедает буферы UART),
поэтому в числах `-bench` задержки тача по-прежнему нет — она принадлежит
панели.

По дороге туда нашлись две вещи, к столу отношения не имеющие, обе в ядре:
вид загрузки (`please wait`) отбирал дисплей у приложения, которое уже
нарисовало кадр, — и оболочка выглядела вылетевшей, хотя жила; а консоль 40×25
на панели 40×30 не гасила нижние пять строк, и там оставался её последний
кадр. Обе починены, см. `docs/09-esp32-cyd.md` (грабли 7).

Своё в столе было одно: подсветка выбранной подписи на пиксель вылезала из
ячейки иконки, и при перетаскивании **вверх** оставляла синий след. На 640×400
этот пиксель тоже есть, но на CYD он вдвое шире, поэтому и заметен. Теперь
украшение обрезается по ячейке.

**Ловушка, стоившая половины замера:** у платы своя `C:\SYSTEM.CFG`, и в ней
рукой написано `[display] driver = panel` — с объяснением прямо в файле: с
поверхностью на 37 КБ при поднятом радио оставалось 23 КБ и файловый сервер не
стартовал. Пока эта строка там, приложение не получает поверхности вообще
(`no system surface; applications bring their own`), и стол не запускается.
`SYSTEM.CFG` читается после `BOARD.CFG` и перекрывает её, так что включить
поверхность можно, ничего не переписывая: дописать в конец `[display]`,
`driver = soft`, `width = 160`, `height = 120`.

## `DESKTOP.INI`: что запоминается, а что нет

`C:\DESKTOP.INI` — обычный INI, который можно прочитать, поправить и удалить;
удалить — это и есть способ вернуть исходное. Формат и ключи — в
[странице пользователя](../../docs/user/05-desktop.md#desktopini).

Запоминается: куда перетащили значки дисков, какие каталоги были открыты в
каких окнах и какого размера, цвет стола, время двойного щелчка.

**Не запоминается сознательно:** какое окно было сверху, где стоял курсор в
списке, куда список прокручен. Все три — про момент, а не про расположение, и
восстановленный курсор в каталоге, который с тех пор изменился, встанет на
другой файл, а не на тот, на котором стоял. Это хуже, чем встать сверху.

Пишется при выходе и после перемещения значка. Ошибка записи не показывается
окном: это происходит на выходе, где сказать уже некому, и после перетаскивания,
где сообщение стоит поперёк того, чем человек занят. Она идёт в журнал.

Значок перетаскивается **сам**, а не контуром — в отличие от окна. Окно тянется
контуром потому, что оно большое и его содержимое стоит перерисовки на каждый
шаг; ячейка значка — 56×44 плоского цвета с картинкой 32×32, так что двигать
саму вещь стоит двух маленьких повреждённых прямоугольников за шаг. В Windows
3.11 значок тащили призраком по той же причине.

Порядок при старте важен и потому записан: файл читается **до** `find_drives` и
до открытия окон — положения значков решают, куда `find_drives` их поставит, а
цвет решает, чем будет закрашен первый кадр.

## Файловые операции: одно ядро на два менеджера

Сами операции — [`apps/common/fsops`](../common/fsops/), общие с `fm`. Здесь
только сторона оболочки: окно прогресса, `Esc`, который его останавливает, и
превращение ошибки в текст, который можно прочитать.

Так, потому что два менеджера с двумя ответами — это два поведения на «файл
занят», «место кончилось» и «половина ушла», а недописанный файл — тот,
который никто не заметит до потом. Обход дерева живёт в чистом `fsops.c` над
таблицей из тринадцати вызовов файловой системы, поэтому он собирается и
проверяется на хосте против дерева, которое существует только в памяти
(`host-tests/test_fsops.c`) — тот же раскол, что у художника (`dsk_paint.c` /
`dsk_paint_surface.c`) и по той же причине.

| Клавиша | Что делает |
|---|---|
| `F8` | копировать (куда — вводом строки; имя без каталога значит «рядом») |
| `F7` | переместить или переименовать |
| `F2` | переименовать на месте (путь с разделителем отвергается: это `F7`) |
| `Delete` | удалить, с подтверждением; каталог — со всем содержимым |

Клавиши — File Manager из Windows 3.11 (`F7`/`F8`/`Delete`), `F5` там и здесь
уже перерисовка. У `F2` в 3.11 клавиши не было, а во всех менеджерах после
неё это `F2`.

**Одно блокирующее место во всей оболочке.** Всё остальное здесь на обратных
вызовах: диалог открывается и сразу возвращает управление, так что оболочка
продолжает перерисовываться и отвечать, пока вопрос на экране. Операции — нет:
копирование идёт до конца внутри одного вызова, а окно прогресса рисуется из
тика, который `fsops` вызывает по ходу. Это не тот же компромисс: вопрос ждёт
человека и может ждать вечно, а операция ограничена собой, ответить на неё
некому, и делать во время неё есть ровно одно — говорить, сколько прошло, и
смотреть, не нажали ли `Esc`.

**Каталог считается перед копированием, файл — нет.** У файла размер уже есть
из его собственного `stat`, так что полоса верна бесплатно. У дерева нет, и
единственный путь к осмысленной полосе — обойти его один раз заранее; на
HostFS это отдельное ожидание, поэтому оно называется «Counting» и
прерывается `Esc`.

## Запуск программы: слот — это стек

Модель Program Manager: оболочка отпускает дисплей, программа владеет всем
экраном, по выходе стол возвращается и перерисовывается.

Половина этого не работала, и починка оказалась в ядре, а не здесь. Симптомы
выглядели как три разных: графическая программа рисовала, но **не получала ни
одной клавиши** (и, значит, экран нельзя было попросить обратно); вывод
консольной программы **не доходил до экрана**; ожидание «нажмите клавишу»
после неё срабатывало мгновенно.

Причина одна. **Консоль и клавиатура принадлежат тому, кто наверху слота
сессии** (`ag_proc_focused` → `ag_session_focused_pid`), а ребёнок процесса
уходил в *первый свободный* слот, который никто не фокусировал. Оттуда он мог
рисовать (дисплей принадлежит тому, кто его взял) и не мог ни читать клавиши
(`api_getch` отвечал `-AG_EPERM`, `ag_poll_event` засыпал в ветке «нет
фокуса»), ни печатать на экран (вывод уходил в журнал). Ребёнок *шелла* этого
не знал, потому что шелл сам привязывает его к своему слоту и сам его
фокусирует.

Попытка отдать передний план явно (`proc->foreground(pid)`) ничего не меняла
и не могла: `s_foreground` — это отдельное понятие в `proc.c`, и на пути
консоли и ввода его никто не читает.

Теперь **слот сессии — это стек процессов**: шелл внизу, приложение над ним,
ребёнок приложения над ним (`ag_session_push_to`, глубина 4). Наверху тот, у
кого клавиатура; при выходе `ag_session_unbind` снимает верхний, и слот
возвращается родителю с событием фокуса — то есть стол сам узнаёт, что пора
перерисоваться. Оболочка при запуске больше не делает ничего особенного:
`ag_spawn` + `ag_wait` (ожидание с таймаутом — чтобы `Ctrl+C`, адресованный
ребёнку, убил ребёнка, а не стол).

## Сборка и запуск

```
argon apps --only DESKTOP.AXE
argon run -Gfx -HostFs build\sd_card
```

В госте:

```
run h:\desktop.axe            Esc или Q — выход
run h:\desktop.axe 30         то же, но выйдет само через 30 секунд
```

Число секунд — для скриптов: приложение никогда не остаётся висеть. `-Gfx`
обязателен: без него у эмулятора нет RGB-панели и рисовать некуда.

| Клавиша | Что делает |
|---|---|
| `Ctrl+Tab` | следующее окно |
| `Ctrl+F4` | закрыть активное окно |
| `Alt+F4` | выйти из оболочки (спросит) |
| `F10` | встать на строку меню; стрелки, `Enter`, `Esc` |
| `Esc`, `Q` | выход, если клавишу больше никому не нужно |
| `F5` | перерисовать весь экран |
| `F6` | послать кадр заново, ничего не рисуя |

**`Alt+Tab` и `Alt+1..4` брать нельзя** — их забирает супервизор для слотов
сессий, и забирает раньше, чем приложение их увидит. Поэтому окна
переключаются `Ctrl+Tab`, а закрываются `Ctrl+F4`: в Windows 3.11 это ровно
те же сочетания для окон *внутри* приложения, так что ничего не выдумано в
обход. Оболочка, которая повесила бы свои окна на `Alt+Tab`, выглядит
работающей до первого нажатия, а потом отдаёт экран пустому слоту с
приглашением шелла — что и случилось при первом прогоне сценария.

Мышью: тянуть за заголовок, тянуть за край или угол — изменить размер,
двойной щелчок по заголовку — развернуть и обратно, кнопки в заголовке —
системная (закрыть), свернуть, развернуть, двойной щелчок по плитке
свёрнутого — вернуть, двойной щелчок по столу — новое окно.

`F6` — не украшение. Он отличает два отказа, неразличимых на снимке: пиксели,
которых оболочка не нарисовала, от пикселей, которые она нарисовала, а до
панели они не дошли.

## Проверка без рук

```
.\apps\desktop\check.ps1                        # поверхность по умолчанию
.\apps\desktop\check.ps1 -Width 320 -Height 240 # размер, как на платах
```

Поднимает QEMU с RGB-окном, запускает оболочку, водит указателем по экрану,
щёлкает, снимает PNG, нажимает `F5`, снимает второй PNG и сравнивает их.
Числа берутся не с картинки, а из транскрипта: сколько событий указателя и
клавиш дошло, был ли фокус, сколько было перерисовок.

Что именно проверяется и почему — в шапке `check.ps1`. Главное там —
**сравнение двух снимков**: по одному снимку нельзя понять, что часть
нарисованного не доехала до стекла.

Раздел **C: печётся в образ прошивки** (`tools/mksysfs.py`, потом
`qemu-boot.ps1 -SysFs`), поэтому весь прогон — одна загрузка: размер
поверхности лежит в `SYSTEM.CFG` до старта, драйверы уже в `C:\DRV` и
перечислены в `[modules]`, сама оболочка на C:. Гость ничего не пишет во
флеш и не перезагружается. Это не аккуратность: очевидный вариант — записать
`C:\SYSTEM.CFG` из шелла и перезагрузиться — проверен и для теста не годится
(на восьми прогонах трижды файл читался обратно битым, `-91`, и загрузка
после такой записи занимала 128 секунд вместо двух).

Сценарий этапа 2: двойной щелчок по значку диска открывает окно на его
корне, двойной щелчок по каталогу входит в него, двойной щелчок по `..`
выходит; потом из этого же окна запускаются две программы — `Home`, стрелки,
`Enter`; потом снимок, `F5`, второй снимок, выход.

Программ именно две, потому что ломаются они по-разному: у консольной вывод
обязан дойти до экрана, а графической обязана дойти клавиша — единственный
способ попросить экран назад. Ни один из двух симптомов не показывал второго,
и утверждаются они порознь.

**Оба снимка — после запусков, а не вокруг них.** Снимок до и снимок после
должны быть одним и тем же состоянием: сдвинувшееся выделение само по себе
дало девять тысяч честно различающихся пикселей в проверке, весь смысл
которой — пиксели, различающиеся нечестно.

Прежний сценарий этапа 1: открыть из меню четыре окна, `Ctrl+Tab` туда и обратно,
поднять первое щелчком по заголовку, свернуть его кнопкой, вернуть двойным
щелчком по плитке, протащить обводкой, снять снимок, `F5`, снять второй,
закрыть два окна `Ctrl+F4`, выйти. Три прогона (640×400 дважды и 320×240):
48–50 событий указателя, 8 клавиш, 2 перерисовки, **2 окна осталось**,
37 пикселей различия между снимками (цифры в строке состояния).

Число оставшихся окон — главная проверка сценария: четыре открылось, два
закрылось. Другое число значит меню, которое не раскрылось, щелчок, который
не туда попал, или закрытие, которое не закрыло, — и по картинке не понять,
что именно.

## Устройство

| Файл | Что в нём | Чистый C |
|---|---|---|
| `desktop.c` | цикл событий, мебель стола, его меню | нет |
| `dsk_rect.c` | прямоугольники и список повреждений | да |
| `dsk_layout.c` | раскладка от размера экрана | да |
| `dsk_paint.c` | рисовальщик: надстройки над шестью операциями | да |
| `dsk_paint_surface.c` | те шесть операций поверх системной поверхности | нет |
| `dsk_wm.c` | окна: геометрия, z-порядок, попадание, перетаскивание | да |
| `dsk_menu.c` | строка меню и раскрывающиеся меню | да |
| `dsk_dlg.c` | модальные диалоги | да |
| `dsk_cursor.c` | программный указатель | нет |
| `dsk_icons.c` | значки 16×16, картинками | нет |
| `dsk_folder.c` | окно папки: чтение, список, прокрутка | нет |
| `dsk_run.c` | отдать машину программе и забрать обратно | нет |
| `dsk_ops.c` | копировать/переместить/удалить: окно прогресса и `Esc` | нет |
| `dsk_ini.c` | `C:\DESKTOP.INI`: чтение, запись, положения значков | нет |
| [`../common/fsops/`](../common/fsops/) | сами операции, общие с `fm` | **да** |

«Чистый C» — без `argon.h`, то есть **собирается и проверяется на ПК**.
Юнит-тесты — `host-tests/test_desktop.c`, идут в `argon tests`; они
подставляют вместо экрана рисовальщик-регистратор и проверяют то, что на
панели не видно: геометрию рамки, во что попадает точка, какие окна
перерисовывает данное повреждение и **в каком порядке**, и что ни одна
заливка не вышла за выданный клип.

**Всё рисование идёт через таблицу из шести функций** (`dsk_painter_t`), и это
решение с причиной: единственная плата с тачем (CYD) не может держать
поверхность 320×240 вообще, и показать на ней этот стол сможет только
растеризатор полосами через `gfx->present`. Второй бэкенд посилен, пока у него
шесть функций, и невозможен, когда оболочка зовёт `ag_gfx_*` из сорока мест.
Побочная выгода уже получена: та же таблица позволила проверить оконный
менеджер тестами.

## Правило программного указателя

Пока указатель на экране, под ним ничего рисовать нельзя: сохранённые
пиксели станут устаревшими, и возврат затрёт нарисованное. Поэтому любая
перерисовка — **спрятать, нарисовать, показать**, в этом порядке, всегда.

## Правило обводки

Окно тянется контуром в один пиксель, а не телом. Это не стилизация: по
проводу к CYD движущаяся картинка стоит 3.5 кадра в секунду, и тащить окно
телом — значит это увидеть.

Под контуром сохраняются пиксели, как под указателем, и отсюда два правила,
каждое из которых уже было нарушено и измерено:

1. **Один `flush` на всю обводку, а не четыре полоски.** Панели отдаются
   целые строки, поэтому левая и правая полоски стоят каждая столько строк,
   сколько занимает окно. Восемь отправок кадра на одно движение мыши — и
   указатель отстаёт на секунды, очередь событий копится, отпускание кнопки
   приходит позже, чем перемещения, и окно уезжает не туда.
2. **Перед перерисовкой обводку надо снять, после — вернуть.** Иначе её
   копия подложки устаревает, и следующее движение возвращает на экран
   картинку, которой там уже нет: чёрный прямоугольник, приколоченный к
   столу, который не убирает даже принудительная перерисовка (F5) — потому
   что именно перерисовка его и портит.

Обе ошибки видны на снимке как одно и то же, и обе сначала были приняты за
третье. Числа: 3267 различающихся пикселей после F5 против 37 теперь.

## Правило фокуса

**Нельзя рисовать, пока слот не получил фокус.** Процесс попадает в слот
сессии не сразу — здесь измерено 90 мс после первой инструкции, — и до этого
`gfx_may_present` в ядре отбрасывает каждый `flush`. При этом `acquire`
отвечает «да», рисующие вызовы молчат, и на стекле остаётся консоль.

Оболочка поэтому ждёт фокуса (не дольше двух секунд, иначе машина без слоёв
сессий была бы машиной, где она висит) и печатает в транскрипт `focus yes`.
На `AG_EV_FOCUS_GAINED` — полная перерисовка, на `AG_EV_FOCUS_LOST` —
перестать рисовать.

Ядро тоже исправлено: владелец дисплея теперь может отправлять кадр, не
дожидаясь фокуса. Сессия отбирает дисплей, когда фокус уходит, так что
владелец по построению и есть тот, кому положено рисовать.

Как это выглядело, пока не было исправлено: первый кадр приходил половиной.
И зависело от размера — на 320×240 оболочка проигрывала гонку **всегда**, на
640×400 выигрывала, потому что вчетверо больше пикселей заливаются дольше,
чем сессия успевает выдать фокус.

## Что нашлось по дороге (и почему стенд работает только теперь)

Шесть починок вне этой папки, все — причины, а не симптомы:

1. **`components/argon_port/idf/src/panel_hw.c` не подключал `sdkconfig.h`,**
   а проверял `#if CONFIG_ARGON_PANEL_QEMU`. Неопределённый макрос в `#if` —
   это ноль, так что вся RGB-панель QEMU вырезалась препроцессором молча.
   Окно SDL при этом открывалось — своего размера 800×600 — и оставалось
   чёрным. Единственный видимый признак: окно не того размера, которого
   просил гость.
2. **QEMU не создаёт RGB-панель без `-M esp32s3,graphics=on`.** Это опция
   машины, а не устройство; `-device esp_rgb` не существует.
3. **KBDVIRT выбрасывал байты со свежепринятого сокета** (`drain_rx` в
   `try_accept`). Клиент, который подключается и сразу печатает, успевает
   прислать клавишу до того, как гость примет соединение на своём такте в
   10 мс. Клавиша доходила в одном прогоне из трёх.
4. **`ensure_listen` в обоих виртуальных драйверах затирал хороший
   дескриптор.** Его зовут два потока (такт ввода ядра и шелл), оба видят
   `listen < 0` до того, как кто-то сохранит результат, проигравший получает
   `-AG_EBUSY` и пишет `-1` поверх сокета победителя. Порт после этого закрыт
   до конца загрузки, а лог за миллисекунду до этого сообщает, что порт
   открыт. Это и была вся нестабильность стенда.
5. **`gfx_may_present` проверял фокус вместо владения** — см. «Правило
   фокуса» выше. Владелец дисплея теперь может отправлять кадр.
6. **`mksysfs.py` не умел собрать образ без пакета платы**, а `--display`
   задавал только имя драйвера, не размер. Теперь есть `--board none` и
   `--display-size WxH`; плюс `[display]` дописывается после `--add`, а не
   до, иначе `--add свой.cfg=SYSTEM.CFG` молча выбрасывал override.

Плюс три умения стенда: `qemu-boot.ps1 -Gfx` (окно панели), элементы
`-Send` с префиксом `!` (команда на хосте посреди сценария — иначе нечем
пошевелить мышью, пока приложение работает и приглашения нет) и
`-SysFs <образ>` (готовый раздел C:, вложенный в образ прошивки).

`tools/inputplay.py` перенесён сюда из ветки `worktree-fallout-cxx`: в `main`
его не было.

## Ловушка, из-за которой стенд выглядел мёртвым

Виртуальные драйверы не начинают слушать, пока что-нибудь не откроет
устройство или не придёт такт ядра. Поэтому `inputplay` подключается только
после того, как оболочка уже запущена; раньше — отказ в соединении и плата,
которая кажется мёртвой.
