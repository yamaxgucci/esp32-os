# ArgonOS — план работ

Цель v1 [Т-37]: *плата загружается, работают встроенные команды, с карты
запускается приложение с графикой, работает внешняя USB-клавиатура.*

Порядок фаз выбран так, чтобы каждая заканчивалась чем-то запускаемым, а
рискованные механизмы проверялись отдельными «спайками» до того, как на них
будет построена подсистема.

---

## Фаза 0 — фундамент (без железа)

- [x] Архитектурный документ, зафиксированные решения
- [x] `sdk/include/argon/abi.h` — контракт ABI
- [x] Скелет репозитория, CMake/ESP-IDF компоненты, `partitions.csv`
- [x] Host-тесты: сборка чистой логики обычным GCC
- [x] Тулчейн: MinGW-w64 GCC 16.1 (host) + ESP-IDF 5.5.5 (esp32s3), оба на `D:`
- [x] Прошивка собирается: `argonos.bin`, 730 КБ (16 августа 2026), ноль
      предупреждений
- [x] CI (прошивка + host-тесты + все `.AXE`) — `.github/workflows/ci.yml`

## Спайк S-1 — исполнение кода из PSRAM  ⚠ ограничивает размер кода

Проверить на S3: `esp_mmu_map(MMU_TARGET_PSRAM, MMU_MEM_CAP_EXEC)`, запись кода
через data-алиас, сброс кэшей, вызов. Замерить скорость относительно SRAM и
flash-XIP. Результат определяет §3.2 архитектуры.

*Требует железа*, и QEMU здесь не заменитель: он не моделирует кэш, поэтому
«работает в эмуляторе» ничего не говорит про писать-через-одну-шину-исполнять-
через-другую. Фазу 2 больше не блокирует — приложения запускаются, код лежит в
арене D/IRAM, данные в PSRAM. Упирается в этот спайк только размер **кода**.

## Спайк S-2 — релокации Xtensa

Собрать тестовое приложение `ld -r`, разобрать таблицы релокаций, реализовать
и покрыть host-тестами применение `R_XTENSA_32`, `R_XTENSA_SLOT0_OP`,
`R_XTENSA_PLT`, литеральные пулы. Без железа проверяется на дампах.

---

## Фаза 1 — «плата загружается и отвечает»

1. ✅ Загрузка ядра, определение чипа, профиль (`full`/`lite`/`nano`)
2. ✅ Слой описания платы: дефолты чипа + `C:\BOARD.CFG`
   (см. `docs/03-board-config.md`); ⬜ вкомпилированные board pack'и под
   конкретные платы и их автовыбор
3. ✅ Журнал логов: кольцевой буфер 4 КБ в internal SRAM, escape-последователь­
   ности вырезаются на входе, счётчик потерянных строк точный (производный, а не
   инкрементальный). Всё логируемое — ядром, ESP-IDF, драйверами — попадает
   сначала в журнал, экран стал одним из его читателей. Команда `log`.
   ⬜ Сохранение журнала в файл при крэше (нужен супервизор)
4. ✅ Консоль: текстовый экран, VT100-рендер, разбор ввода, UART-endpoint,
   мультиплексор с очередью событий, XON/XOFF на вводе (без него всплеск данных
   переполнял буфер порта и байты терялись молча — см. §9.1 архитектуры)
5. VFS: ✅ ядро слоя (монтирование, дескрипторы, владельцы, eject),
   ✅ ramfs `/tmp` (`T:`), ✅ littlefs на внутреннем flash `/sys` (`C:`),
   ✅ FAT на SD `/sd` (`A:`) — SDMMC и SPI, через адаптер к VFS от ESP-IDF;
   ✅ littlefs для `/sys` — переживает пропадание питания посреди записи
   (`joltwallet/littlefs`, `mount` → `lfs`)
6. ✅ `BOARD.CFG` и `SYSTEM.CFG` читаются на стадии `config`
7. ✅ Шелл: редактор строки с историей, разбор команд, перенаправление вывода,
   смена диска по `a:`, `dir cd type copy del md rd ren mount format hexdump
   ver mem boot uptime cls echo color ps kill fg fm errorlevel reboot help`
8. ✅ Кодовая страница экрана: `chcp 437|866|1251`, `[console] codepage` в
   `SYSTEM.CFG`, `con->codepage` в ABI. Ячейка хранит один байт, конвертация на
   краях — байт в UTF-8 на выходе в терминал, код точки в байт на входе с
   клавиатуры. Таблицы сгенерированы из codecs Python
   (`tools/gen-codepage.py`), их никто не набирал руками

**Готово, когда:** плата стартует за <400 мс, по UART доступен `A:\>`,
работают файловые команды на реальной SD.

**Уже работает в QEMU:** загрузка, приглашение, редактирование строки, все
встроенные команды, три диска (`A:` SD, `C:` внутренний flash, `T:` RAM),
копирование между дисками, `format a:`, перенаправление `dir > files.txt`,
чтение `BOARD.CFG`. Проверяется `argon test`, экран из транскрипта
восстанавливается `build-host\vtdump.exe`. Карта: `argon test -Sd`.

## Фаза 2 — «запускается приложение»

1. Менеджер памяти: ✅ арены процессов (`multi_heap` поверх куска PSRAM, полный
   возврат при завершении), ✅ `AG_MEM_*` capabilities с учётом того, что вне
   арены (быстрая, DMA, исполняемая память) — через ресурсный список
2. ✅ Загрузчик: формат `.AXE`, инструмент `tools/mkaxe.py`, релокация
   абсолютных слов, проверка заголовка. **Спайк S-2 закрыт без железа:**
   декодирование инструкций Xtensa не нужно — см. §3.1 архитектуры
3. ✅ Модель процессов: до 4 процессов, своя задача, стек, арена и рабочий
   каталог, ресурсный список, `exec`/`spawn`/`wait`/`kill`, передний план.
   Память возвращается целиком — арена процесса, а не общая куча
4. Супервизор: ✅ `Ctrl-C` как просьба, `Ctrl+\` (терминальный эквивалент
   `Ctrl-Alt-Del`) как снятие, сбор завершённых, crash-record в журнал,
   ✅ **перехват исключения** через таблицу обработчиков Xtensa — падает
   приложение, а не система, и в журнале смещение отказа в его собственном коде
   (§5.5 архитектуры); ✅ watchdog по заявке приложения (`ag_watchdog`);
   ✅ crash-record в `/sys/crash.log` с хвостом журнала;
   ✅ вывод фоновых процессов в лог, ✅ горячие клавиши `Alt+1..4` / `Alt+Tab`
   (session slots + system shell; см. `docs/plans/session-slots-and-system-shell.md`)
5. Таблицы ABI: ✅ `sys mem fs con time proc task inp dev io`; ⬜ `cfg gfx`
6. ⬜ SDK: `libargon` как библиотека, шаблон приложения; сборка `.AXE` ✅,
   примеры `apps/hello`, `spin`, `leak`, `threads`, `crash` ✅
7. ✅ Команды `ps kill fg`, `run /b`; ✅ `run`, ✅ `recv` (приём файла как hex
   через консоль — как доставлять приложение на плату без картридера)
8. ✅ **Разделение образа на две части** — код в арену D/IRAM, данные, константы
   и bss в окно данных PSRAM, у релокации признак части. Лимит на данные снят:
   проверено приложением с 512 КБ bss при арене 64 КБ; на константы тоже —
   проверено шрифтом 96 КБ, который стоит 212 байт арены. Осталось поднять лимит
   на **код** — это спайк S-1, ему нужна плата

**Готово, когда:** `run hello.axe` с карты работает, `Ctrl-Alt-Del` снимает
зависшее приложение, память после снятия возвращается полностью.
*Первое и третье выполнено и проверено в QEMU; второе выполнено в терминальном
варианте (`Ctrl+\`), сам `Ctrl-Alt-Del` ждёт USB-клавиатуру из фазы 3.*

**Уже работает в QEMU:** приложение собирается `mkaxe.py`, передаётся `recv`,
загружается двумя частями и исполняется как процесс — своя задача на ядре
приложения, свой стек, своя арена, свой рабочий каталог. Проверено там же:
`Ctrl-C` доходит до приложения, которое его слушает; `Ctrl+\` снимает то,
которое не слушает; `run /b` + `ps` + `kill <pid>` работают; приложение,
забравшее 992 КБ и не отдавшее, отдаёт всё при завершении; двойное освобождение
и чужой указатель отсекаются и попадают в журнал; приложение, разыменовавшее ноль
— в главной задаче или в потоке, — снимается с указанием смещения отказа в его
собственном коде, и приглашение возвращается; образ, который не влезает в
арену, получает отказ с объяснением. Правильность релокации доказана тестом: образ,
перенесённый загрузчиком, байт-в-байт совпадает с прямой линковкой по тем же
адресам — причём части в фикстурах разнесены на *разные* расстояния, так что один
общий сдвиг такой тест не проходит.

### Что осталось по размеру приложения

Данные уже не ограничены ничем, кроме свободной PSRAM (проверено на 512 КБ bss,
свободно оставалось 7.6 МБ), и вместе с ними — константы. Ограничен **код**.
Три шага по возрастанию цены, первый сделан:

1. ✅ **`.rodata` в часть данных.** Шрифты, картинки, таблицы исполнять не надо,
   и арену они больше не занимают. Оказалось ровно тем, чем обещало быть:
   правка скрипта линковки в `mkaxe.py` (`.rodata` в `.dimage`) и ноль изменений
   в ядре — код достаёт константу через литерал с абсолютным адресом, а тот
   релоцируется смещением данных, как любое слово, смотрящее в часть данных.
   Измерено на приложении со шрифтом 96 КБ: часть кода **212 байт вместо
   98 552**, шрифт читается из PSRAM по адресу в окне данных, контрольная сумма
   сходится (`run t:\font.axe` в QEMU). Цена: чтение из PSRAM вместо SRAM
   (кэшируется) и потеря read-only, которой без MPU всё равно нет. Обратно всё
   возвращает `--rodata code`, а одну горячую таблицу оставляет в SRAM
   `AG_HOT_RODATA` — она попадает в `.hot_rodata`, то есть в часть кода.
   **Насколько велика цена — QEMU не скажет**: он не моделирует ни кэш, ни
   латентность PSRAM, поэтому замер и выбор того, что стоит помечать горячим,
   идут вместе со спайком S-1 на плате.
2. ⬜ **Код в окно инструкций PSRAM** — мегабайты кода. Это спайк S-1, и он
   **требует платы**: перед прыжком нужны writeback записанного через шину данных
   и инвалидация кэша инструкций, а QEMU кэш не моделирует. Даже после этого
   горячие функции останутся в SRAM — icache у S3 16–32 КБ, мегабайт кода из
   PSRAM будет промахиваться постоянно; ради этого в манифесте и предусмотрен
   `hot_text`.
3. ✅ **`.text` в раздел flash и XIP** (R-1, 7 августа 2026) — если код не
   влезает в арену, загрузчик релоцирует в PSRAM, пишет в сырой раздел `appfs`
   и исполняет через `esp_partition_mmap(INST)`. Проверено в QEMU:
   `apps/bigcode` (~93 КБ `.text`) → лог `code … XIP at 0x42…`, `errorlevel 0`.
   Contiguous-образы (RISC-V) по-прежнему только в арене. Цена: запись во flash
   при каждом таком `run` (износ и задержка старта).

## Фаза 2.5 — документация для разработчиков  ✅ кроме драйверов

Отдельная фаза, а не «допишем потом»: ABI объявлен контрактом со сторонними
разработчиками [Т-13], а контракт без описания контрактом не является.

Делалась **после** модели процессов: подтаблицы `task` и `proc` появились там же,
и описывать ABI до них значило бы переписывать дважды.

1. ✅ [`docs/sdk/01-getting-started.md`](sdk/01-getting-started.md) — от нуля до
   работающего приложения: тулчейн, сборка `.AXE`, три способа доставки, запуск,
   и таблица всех отказов загрузки со причинами
2. ✅ [`docs/sdk/02-api-reference.md`](sdk/02-api-reference.md) — весь
   `argon/abi.h` по подсистемам: что делает каждый вызов, что возвращает, чего
   **не** делает; какие подтаблицы `NULL`; правила совместимости и `AG_HAS()`
3. ✅ [`docs/sdk/03-application-anatomy.md`](sdk/03-application-anatomy.md) —
   `AG_APP()`, `ag_main`, аргументы, код возврата, память, потоки, чего нет
   (libc, конструкторы, `errno`), как быть снимаемым процессом
4. ✅ [`docs/sdk/04-axe-format.md`](sdk/04-axe-format.md) — формат образа со
   смещениями полей и полным списком проверок загрузчика
5. ✅ [`docs/sdk/05-drivers.md`](sdk/05-drivers.md) — загружаемые `.SYS`
6. ✅ [`docs/user/01-shell.md`](user/01-shell.md) — все команды, перенаправление,
   диски, горячие клавиши; примеры — настоящие транскрипты из QEMU
7. ✅ [`docs/user/02-board-setup.md`](user/02-board-setup.md) — «как поднять
   ArgonOS на незнакомой плате», расширение `03-board-config.md`
8. ✅ примеры в `apps/`: к `hello`, `spin`, `leak`, `threads`, `crash` и `fm`
   добавлен **`disk`**, **`devs`**, и **`echo`** — минимальный `.SYS`,
   публикующий символьное устройство. Каждый пример проверяет себя сам
9. ✅ README на английском — он и был на английском; обновлён под текущее
   состояние и связан с документами SDK

**Готово, когда:** сторонний разработчик собирает и запускает своё приложение,
не читая исходники ядра. Выполнено, кроме драйверов: `.SYS` описывать нечем, пока
нет модели устройств.

Описание контракта словами нашло в нём **два дефекта и один пробел** — `kbhit`,
терявший клавишу, отсутствие способа задать размер стека и арены, и необъявленное
ограничение `printf` на 255 символов. Подробности в
[05-status.md](05-status.md#документация-для-разработчиков).

## Фаза 3 — «графика и клавиатура»

1. Класс устройств `display`, драйверы ST7789/ILI9341/SSD1306, RGB-панель
2. Рендер текстового экрана на дисплей (шрифт 8×16), переключение
   текст/графика
3. `gfx` API, примитивы, двойная буферизация
4. USB host: HID-клавиатура и мышь, раскладки; PS/2 как альтернатива
5. Нормализация ввода, единая очередь событий, горячие клавиши
6. Демо-приложение с графикой

**Без платы (QEMU), сделано 7 августа 2026:** soft RGB565 framebuffer в PSRAM
(`fb0`, по умолчанию 640×400 из `[display]` в `BOARD.CFG`), живая подтаблица
`gfx` (ABI 0.9 + soft-draw: line/circle/poly), шрифт 8×16, blit текстового
экрана на soft fb, команда `gfxdump`, демо `apps/gfxdemo`, CC-игра Asteroids.
Панели SPI/RGB, USB-HID и двойная буферизация
по-прежнему ждут железа.

**Готово, когда:** выполнен критерий v1 [Т-37].

## Фаза 3.5 — «на этой машине можно работать»

*Добавлено 6 августа 2026, не из исходного интервью.* Вехи — приложения, а не
части ядра. Первые две от графики не зависят: 80×25 текста и клавиатуры
достаточно, так что при желании двигаются раньше фазы 3 — что с менеджером и
произошло, он уже сделан. Третья, наоборот, требует всего сразу и потому стоит
последней.

### 1. Файловый менеджер в стиле Far — ✅ `apps/fm`, команда `fm`

Две панели, курсор, `F1`..`F10`, просмотр файла, копирование, перенос,
создание каталога, удаление, выбор диска, запуск программ с возвратом экрана.
11 КБ кода, работает и проверено в QEMU.

**Встроен в образ ОС** — это команда шелла `fm`, а не файл, который надо сначала
куда-то положить: плата, которая загрузилась, уже умеет показать свои диски. Тот
же исходник собирается и как `.AXE` (`AG_BUILTIN` в `apps/fm/fm.h`), и это не
дублирование, а проверка: встроенный вариант ходит через ту же таблицу ABI, что и
загруженный, значит контракт используется, а не обходится.

Что понадобилось от ОС: `fs->opendir`/`readdir` и `con->poke`/`fill` уже были;
не было **подтаблицы `inp`** — `con->getch` отдаёт только символ, а у стрелок и
F-клавиш символа нет. Она добавлена: `inp->poll` отдаёт целое событие с
HID-кодом и модификаторами, `flush` и `mods` рядом. `key_pressed` остался `NULL`
и честно: терминал не сообщает, что клавиша *держится*, — это появится с
USB-клавиатурой.

Ещё понадобился `argon/libc.h`: у приложения нет libc, но `memcpy` и `memset`
компилятор подставляет сам, и без них образ не линкуется. Плюс `AG_PATH_MAX`
переехал в `abi.h` — приложение, вызывающее `getcwd`, обязано знать границу.

Про скорость, как и было обещано, **измерено, а не предположено** (менеджер
показывает это в строке состояния): полная перерисовка 80×25 — **~90 мс**,
перерисовка при движении курсора — **~20 мс**. Ячейка, которая не изменилась,
в терминал не отправляется (экран помечает грязными только изменившиеся строки),
так что дело не в объёме данных: время уходит на ожидание консольного замка,
который рендерер держит, пока передаёт кадр в UART. Отсюда следующий шаг, если
захочется бодрее: собирать поток под замком, а писать в порт после его отпускания.

Чего в менеджере нет: копирования каталога целиком, рекурсивного удаления,
поиска и командной строки внизу. Всё это отказывается работать словами, а не
молча.

### 1a. Архивы ZIP — `unzip` в ядре, полный `zip` как `.AXE`  ✅

*Принято 9 августа 2026.* Распаковка нужна часто и без подготовки карты
(ROM из `.zip`, поставка файлов, просмотр архива). Упаковка — реже и тяжелее
по коду/RAM.

* ✅ **`unzip` — builtin шелла** (образ ОС): extract + list, streaming через
  tinfl/miniz (`third_party/miniz`, без writer). Без создания архивов.
* ✅ **`ZIP.AXE`** (`apps/zip/`) — create/update (store) + list/extract на карте;
  deflate-extract — builtin `unzip` (полный miniz writer в `.AXE` на S3 не
  стабилен; store-writer + ручной CD достаточно для критерия).

Критерий: `unzip a:\foo.zip t:\` и список без `.AXE` на диске; создание —
`run zip.axe …`.

### 2. Компилятор упрощённого C прямо в ОС — `CC.AXE`

Подмножество: `int`/`char`/`void`, указатели, массивы, структуры, функции,
`if`/`while`/`for`/`return`, выражения, вызовы ABI, простой препроцессор.
Без `float` на первом шаге. Цель — чтобы простое приложение писалось и
собиралось на самой плате, без ПК. Фазы A–D ниже.

Ложится на то, что уже сделано, лучше, чем кажется: компилятор пишет **готовый
`.AXE`**, а формат уже есть — две части, список абсолютных слов, загрузчик умеет
его запускать. Значит компилятору не нужны ни ELF, ни линкер, и та же арифметика
релокаций проверяется теми же host-тестами.

* однопроходный рекурсивный спуск → прямая эмиссия Xtensa, без оптимизатора;
  вызовы всегда через `l32r` + `callx`, литеральный пул перед кодом, как в
  `mkaxe.py`;
* распределитель регистров не нужен: стековая модель вычислений, как в Small-C;
* ✅ редактор текста в ОС (`edit`) — встроенная команда, тот же путь что `fm`;
* бэкенд под RISC-V — отдельный, но структура та же.

Назвать ограничения сразу: код попадёт в арену (64 КБ до S-1), скорость
сгенерированного кода будет заметно хуже GCC, и это подмножество C, а не C.

Главный риск — соблюдение вызывающего соглашения Xtensa (windowed call,
выравнивание стека): ошибка здесь выглядит как случайный крэш где-то потом.
Поэтому первым шагом делается таблица «выражение → ожидаемое значение», которая
гоняется и на хосте, и в QEMU, и только после неё — язык целиком.

**Имя языка: Argon CC** (образ `CC.AXE`).

**Сделано (август 2026):** функции + `callx8`, globals / `int[]`, `for`,
строковые литералы, ABI builtins (`ag_delay`, `ag_key`, `ag_gfx_*` draw),
релокации, `AG_AXE_NEEDS_GFX`. Целевое демо —
[`apps/cc/examples/asteroids.c`](../apps/cc/examples/asteroids.c)
(компиляция на госте через `CC.AXE`).

**Фаза A и гигиена (август 2026).** Консоль и пины: `ag_print`, `ag_printf`
(до 6 аргументов, через `con->printf` — вариадический вызов по windowed ABI
проверен на госте), `ag_print_int` / `ag_print_hex`, `ag_cls`, `ag_gotoxy`,
`ag_gpio_config` / `write` / `read`, `ag_adc_read`. Ядро при этом не менялось
ни на строку: всё перечисленное уже было в `ag_api_t`, компилятору нужны были
только смещения.

Вместе с этим — то, что дальше стоило бы дороже:

* **Кодогенерация.** Правый операнд бинарной операции берётся прямо в регистр,
  когда это константа или скалярная переменная (спилл только при реальной
  вложенности); индексация массива — два сложения вместо `movi` + `mull`;
  запись в глобальную скалярную — без спилла. Замер на одном и том же
  `asteroids.c` двумя компиляторами: **код 9 204 → 6 600 байт, −28%**.
* **Фрейм по факту.** `ENTRY` бэкпатчится реальным размером кадра вместо
  постоянных 352 байт на любую функцию; спилл-слоты переехали под локальные,
  чтобы смещение локальной было известно в момент объявления.
* **Шесть параметров** вместо четырёх. Шестой приходит в `a7`, который тут же
  становится указателем кадра, — пролог теперь сохраняет параметры до того, как
  затрёт его собой.
* **`abi_minor` по факту** использованных возможностей (8 / 9 / 10 / 14), а не
  безусловные 14: программа, которая только рисует, снова запускается на ядре
  без звука.
* **Лимиты под систему:** код до 192 КБ (потолок арены — что скомпилировалось,
  то загрузится), данные 128 КБ, 128 функций, 128 globals, 96 локальных.
* **Смещения ABI сверяются на сборке.** Таблица переехала в `cc_compile.h`, а
  `host-tests/test_cc.c` сверяет каждое смещение с `offsetof()` по настоящему
  `abi.h`. Перестановка поля в ядре теперь роняет сборку тестов, а не госта.
* **Ошибки с номером строки** — у компилятора, который живёт на самой машине,
  нет ни IDE, ни хоста, чтобы искать место вручную.
* **Самопроверка на самом языке** —
  [`apps/cc/examples/selftest.c`](../apps/cc/examples/selftest.c): host-тесты
  видят только то, что исходник скомпилировался, а верен ли *сгенерированный
  код*, показывает лишь запуск. `errorlevel 0` — прошло.

**Фаза B (август 2026).** Биты, байты и указатели — то, без чего «взрослый»
язык не начинается:

* **`& | ^ ~ << >>`** с настоящими приоритетами C (три уровня битовых операций
  между `==` и `&&`), поэтому `(x & 3) == 0` требует скобок ровно там же, где в
  C. Сдвиг на константу — одна инструкция `slli` / `srai`; на регистр — через
  `SAR`. `>>` арифметический.
* **`char`** — байт памяти, беззнаковый: `char c;`, `char b[N];`, параметры и
  возврат байта. Значение по-прежнему всегда 32-битное слово в регистре, `char`
  описывает *хранение*, поэтому `char c = 300` хранит 44.
* **Указатели `int *` / `char *`** с байтовым и словным load/store: `*p`,
  `p[i]`, `*p = v`, `&x`, `&a[i]`; арифметика шагает элементами (`p + 1` — это
  1 байт для `char *` и 4 для `int *`). Имя массива — адрес первого элемента,
  строковый литерал — `char *`, так что строка обходится обычным циклом C.
* **Символьные константы** `'a'`, `'\n'`.
* Вся типовая система — одно поле «размер того, на что указывает значение»;
  чего оно сказать не может (указатель на указатель, массив указателей), то
  компилятор отвергает, а не угадывает.

Ядро снова не менялось ни на строку — новых сервисов языку не понадобилось.
Цена — сам компилятор: `CC.AXE` **10 288 → 13 588 байт кода** за фазы A и B
вместе (+32%), при 192 КБ арены. Кодогенерация для программ без `char` и
указателей побайтово та же, что после фазы A: пути с размером элемента 4
эмитят прежние инструкции.

Кодировки новых инструкций сверены с настоящим ассемблером
(`xtensa-esp32s3-elf-gcc -Wa,--no-transform` + `objdump`) — три из них у меня
были неверны, и это ловится один раз, а не в чужой программе. Дальше их
проверяет запуск: `selftest.c` вырос до 30 проверок, включая соседство байтов в
массиве, усечение `char`, шаг указателя и приоритеты.

**Фаза C (август 2026): структуры.** `struct v { ... };`, переменные, массивы и
указатели этого типа, `s.x`, `p->x`, `a[i].x`, `p[i].x`, `&s` — в любых
сочетаниях и по обе стороны от `=`. Поля идут по порядку объявления, каждое по
своему естественному выравниванию (слово всем, кроме `char`), структура
округляется до слова — те же байты, что кладёт хостовый C для тех же полей, а
значит указатель на такую структуру можно передавать через ABI. Поле-указатель
на собственный тип разрешено (список), размер берётся из таблицы в момент
использования, а не в момент записи поля, когда он ещё нулевой.

Структура всегда живёт по адресу: ни присваивания целиком, ни параметра, ни
возврата по значению — передаётся `&s`, принимается `struct v *`. Это не
экономия усилий, а отказ угадывать: копирование структуры потребовало бы
второго вида «значения», которого в языке, где значение — слово в регистре,
просто нет.

Ради этого разбор имени переехал в одно место. Раньше `a[i]` разбиралось трижды
(чтение, присваивание, `&`) и каждый раз чуть по-своему; теперь есть *place* —
база (символ или уже посчитанный адрес) плюс константное смещение плюс тип, а
`[i]`, `.f` и `->f` только двигают его. Смещение остаётся константой как можно
дольше и уезжает в непосредственный операнд самой инструкции, поэтому `p->x` —
одна команда, а `b.r.name[0]` не платит ничего за вложенность. Побочный эффект:
`p[i]` для указателя как левый операнд арифметики раньше разбирался неверно —
быстрый путь операнда съедал имя, не глядя на `[`; теперь на это есть тест.

Цена — компилятор: `CC.AXE` **13 588 → 15 376 байт кода** (+13%); таблица типов
(24 структуры по 24 поля) добавила ~29 КБ к рабочему состоянию, которое и так
живёт в куче, то есть в PSRAM. Ядро опять не менялось. Проверка, что
кодогенерация не подорожала: `asteroids.c`, собранный госте́вым `CC.AXE`, —
**7 488 байт, байт в байт как до фазы C**.

**Препроцессор (закрытие фазы C).** `#define` / `#undef` (object- и
function-like), `#ifdef` / `#ifndef` / `#else` / `#endif`, `#include "file"` —
файл ищется рядом с компилируемым исходником (`cc_main` передаёт reader в
`cc_compile_to_axe_inc`). Без `#if` выражений, `##` и угловых скобок. Заголовок
с телом функции закрывает окно globals — определения функций остаются в `.c`.
Проверено: `selftest.c` + `ppinc.h` на госте, `failures: 0`.

**Фаза D (август 2026): FS / malloc / devices.** Builtins через те же смещения
ABI, что и `ag_print` — ядро не менялось: `ag_malloc` / `realloc` / `free`,
`ag_open` / `close` / `read` / `write`, `ag_opendir` / `readdir` / `closedir`,
`ag_dev_open` / `close` / `read` / `write` / `ioctl`. Флаги open — обычные
`#define` (`AG_O_RDONLY=0`, …). `ag_seek` нет: в ядре `int64_t`, в языке —
слово. Критерий «чтение файла + write PCM chunk» —
[`apps/cc/examples/fsmem.c`](../apps/cc/examples/fsmem.c) в QEMU, `errorlevel 0`.

Попутно починены два места, на которые фаза наткнулась: `-Put` ждал любое
«N bytes written» и ловил перерисовку прошлого файла; `||` / `&&` после правой
стороны сравнивали bool с тем, что быстрый путь оставил в `a9`, а не с нулём.

`CC.AXE` после C+preprocessor+D: **18 720 байт кода** (строки имён builtins —
в data). Язык для фазы E **не расширяли**.

**Фаза E (август 2026).** Structural DX7 **nofx** на диалекте Argon CC —
[`apps/cc/examples/dx7nofx.c`](../apps/cc/examples/dx7nofx.c): 6 op, 32 alg
(`k_alg_dest`), feedback, EG, 8 voices, LFO shapes, sine LUT, pad. Это
реимплемент под Mini-C, не прогон host `ag_dx7.c` через CC. Гость:
`run h:\cc.axe h:\dx7nofx.c h:\dx7nofx.axe` → образ ~23 КБ; QEMU smoke печатает
`dx7nofx: smoke ok`. Host `check_dx7nofx_example` в `test_cc.c`. Host
`DX7.AXE` (mkaxe) остаётся линией для FX / WAV / UI.

**Бэклог CC** (дальше по приоритету) — выращивание в Mini-C; детали и фазы
A–E в [`docs/06-ideas.md`](06-ideas.md) (§ DX7 / Argon CC):

1. ✅ **A:** `ag_print` / `ag_printf`, **GPIO** (`ag_gpio_*`)
2. ✅ **B:** `& | ^ << >>`, `char`, произвольные указатели (байтовые load/store)
3. ✅ **C:** структуры + простой препроцессор (`#define` / `#ifdef` /
   `#include "…"`). RISC-V backend — отдельно, по мере нужды (не в критерии C)
4. ✅ **D:** builtins FS (`ag_open`/`read`/`write`/`close`, `opendir`/…),
   `ag_malloc`/`free`/`realloc`, `ag_dev_open`/`read`/`write`/`ioctl`. Без
   `ag_seek` (ядро — `int64_t`, язык — 32-битное слово). Критерий: файл + PCM
   chunk — [`fsmem.c`](../apps/cc/examples/fsmem.c)
5. ✅ **E:** structural **DX7 nofx** на CC — [`dx7nofx.c`](../apps/cc/examples/dx7nofx.c)
   (без `ag_fx`, без второго ядра). Host `DX7.AXE` — FX / WAV.
6. ✅ **Сборка загружаемых драйверов `.SYS` на госте** (10 августа 2026) —
   `ag_driver_init` + `AG_AXE_DRIVER`, `#pragma drv "NAME" "VER" "AUTHOR"`,
   `&func`, builtins `ag_dev_add` / `ag_dev_remove` / `ag_dev_priv`. Пример
   [`apps/cc/examples/echo.c`](../apps/cc/examples/echo.c):
   `run h:\cc.axe h:\echo.c h:\echo.sys` → `drv load` → `d:\echo`. Host
   `mkaxe` + `AG_DRV` остаётся для модулей, которым нужен полный C.

Критерий для пинов: исходник с `ag_gpio_config` / `ag_gpio_write` собирается
CC на госте и дергает GPIO без host GCC.

Критерий для `.SYS`: ✅ исходник с `#pragma drv` / `ag_driver_init` /
`ag_dev_add` собирается CC на госте в `.SYS`, `drv load` поднимает устройство.

Критерий Mini-C / DX7-nofx: фазы A–E ✅ — см. `06-ideas.md`.

### 3. Эмулятор Sega как `.AXE`

*Добавлено 6 августа 2026 по запросу.* Не развлечение, а **самая честная
проверка системы, какую можно придумать**: эмулятор разом требует много кода,
пропускной способности дисплея, звука, ввода и файловой системы. Если он идёт —
на этой ОС можно писать что угодно. Именно поэтому он ставится вместо
синтетического «демо-приложения с графикой» из фазы 3.

Две очень разные цели, и путать их нельзя:

| Цель | Что внутри | Оценка |
|---|---|---|
| **Master System / Game Gear** | Z80 @ 3.58 МГц, VDP 256×192, PSG | реалистично в полную скорость: эмуляторы этого класса ходят на ESP32 @ 240 МГц давно |
| **Mega Drive** | 68000 @ 7.6 МГц **плюс** Z80, VDP 320×224, YM2612 + PSG | реалистично в 60 Гц: 32 КБ I-cache уже есть; звук планируем как Z80 + наш `ag_fm` + PSG на том же ядре (YM2612/второе ядро — fallback) — см. [`07-emulator-performance.md`](07-emulator-performance.md) |

Считать надо три разных ресурса и не складывать их. Кадр 320×224 в RGB565 — это
140 КБ; по SPI на 80 МГц он физически едет ~14 мс, но **через DMA это время шины,
а не процессора**, и оно накладывается на эмуляцию. При 60 Гц это 8.6 МБ/с, то
есть 86% шины и никакого запаса; при 30 Гц — 4.3 МБ/с и меньше половины. С
RGB-панелью отдельной передачи нет вообще. Процессорная же часть — 68000 с VDP —
это 9–15 мс на кадр по замерам на этом чипе, то есть ядро почти целиком.
Чужой профиль с настоящим YM2612 добавлял ещё 7–8 мс и уводил звук на второе
ядро; мы вместо чипа берём `ag_fm`, и этот запас, скорее всего, не понадобится.

Важная тонкость, которую легко принять за экономию, которой нет: **пропуск кадров
не уменьшает работу процессорных ядер.** Гостевая консоль тактируется своими
60 (NTSC) или 50 (PAL) Гц, и 68000, Z80 и звук обязаны отрабатывать всё это
время, иначе игра идёт медленно, а не с меньшей плавностью. Пропуск кадра
экономит две вещи: передачу по шине и **растеризацию VDP** — а она большая. Звук
не экономит ничего: сэмплы (YM2612 или `ag_fm`) считаются непрерывно.

С RGB-панелью картина другая и лучше: панель сама непрерывно сканирует кадровый
буфер в PSRAM, отдельной передачи кадра нет вообще, и 30 против 60 остаётся
вопросом только к тому, как часто эмулятор перерисовывает буфер.

Чего для этого не хватает в ОС, по порядку зависимостей:

1. ✅ **Код больше арены** — R-1 (flash XIP) снимает лимит для split `.AXE`.
   Эмулятор 100–300 КБ инструкций больше не упирается в «некуда положить код», и
   S-1 для realtime не нужен: чужой порт того же класса идёт 60 Гц, исполняясь
   из flash через кэш;
2. ✅ **Размещение кода** — здесь стояло, что главный блокер это `hot_text`, и это
   оказалось неверно. У собранного `MD.AXE` сначала 129 КБ кода (позже ~190 КБ
   с cycle tables в арене), а не 200–400 text-only: он целиком
   влезает в арену, поднятую до 192 КБ (`CONFIG_ARGON_APP_ARENA_KB`), и
   исполняется из internal SRAM вообще без кэша — лучше, чем выборочный
   `hot_text` при холодном коде во flash. `hot_text` остаётся в планах ради
   системы в целом (арена резервируется навсегда и общая на все процессы), но
   realtime он не блокирует;
2а. ✅ **Кэш инструкций 32 КБ вместо дефолтных 16** — сделано 9 августа 2026;
3. **`gfx`** (фаза 3): захват дисплея, RGB565, двойная буферизация, `flush`
   прямоугольником. Кадровый буфер в PSRAM — 140 КБ, это не проблема;
4. **Звук — которого в плане нет.** Нужен класс `audio` и I2S-выход: буфер,
   двойная буферизация, отдача сэмплов из приложения. `AG_DEV_AUDIO` в перечне
   классов есть, реализации нет. Добавляется в фазу 4;
5. ✅ **Ввод** — слой pad в ядре (9 августа 2026): HostFS PADPUSH →
   `ag_input_push_pad` → `/dev/joy0` + `inp->pad`/`btn`/`btnp` (ABI 0.11),
   снимок 6 байт с C/START/X/Y/Z/MODE. SMS и MD читают уровни через API, без
   `seek`/`read` файла каждый кадр. Серийная консоль по-прежнему без key-up
   (VT не отдаёт отпускание); опционально `CONFIG_ARGON_CONSOLE_KEY_EVENTS`
   просит kitty / win32-input-mode, но в классическом `cmd` это не работает.
   Осталось на плате: USB HID gamepad и GPIO-кнопки как источники в тот же слой;
6. **ROM с карты** — работает уже сегодня: 4 МБ картриджа спокойно ложатся в
   PSRAM, `fs->read` их читает.

**Порядок, который имеет смысл:** сначала Master System — она проверяет всю
цепочку (код, дисплей, звук, ввод, файлы) и при этом заведомо укладывается в
скорость (256×192 — это 96 КБ на кадр); затем Mega Drive, но не «после S-1», как
здесь стояло раньше, и не после `hot_text`, как стояло потом, а после починки
пути вывода и роста арены. Разбор бюджетов и план по шагам —
в [`07-emulator-performance.md`](07-emulator-performance.md).

✅ **MD.AXE** (9 августа 2026) — vendored gwenesis в `apps/md/`: 68000 и VDP,
затем в тот же день Z80 из SMS + PSG + YM→`ag_fm` (WAV/mock), видео прямо в
back-буфер `gfx`, pad layer, `stats`/`fps30`. Фаязуллин не используется.
Лицензии — [`apps/md/README.md`](../apps/md/README.md). В QEMU Alex Kidd с
звуком ~125% realtime; стартовые кадры и I2S — ещё открыты.

✅ **Mute SMS.AXE** (7 августа 2026) — vendored SMS Plus GX в `apps/sms/`
(GPLv2+; ядро ArgonOS — GPL-3.0-or-later, SDK/ABI — Apache-2.0). QEMU: `run` + soft `gfx` +
клавиатура→pad + `gfxdump`; звук opt-in: PSG+FM→WAV/mock; FM = OPLL→`ag_fm`
(не полный `ym2413.c`). Дальше на плате — I2S, USB gamepad, замер % realtime.

**Целевые числа, по которым это будет считаться сделанным:** эмуляция не меньше
100% реального времени (то есть кадр укладывается в 16.7 мс), показ 30 или 60 Гц
по настройке, звук без разрывов, отставание ввода до 2 кадров. Если 68000 не
укладывается — говорим об этом числом (сколько процентов реального времени идёт
эмуляция), а не «работает не очень».

## Фаза 4 — «периферия»

1. ✅ **Менеджер устройств, регистрация, `/dev`** — классы, имена, владельцы,
   эксклюзивный доступ, отзыв при извлечении носителя; устройство доступно как
   файл на диске `D:`, поэтому таблица дескрипторов и возврат ресурсов за
   снятым процессом — те же, что у файлов. Подтаблица `dev` перестала быть
   `NULL` (ABI 0.4). ✅ `probe` по I2C (`modules.probe`, `drv probe`)
2. ✅ Загружаемые драйверы `.SYS`, выгрузка, `DEVICE=` / `[modules] device=`
   в `SYSTEM.CFG`
3. Драйверы шин и периферии [Т-21]: ✅ I2C/SPI/UART/PWM; ⬜ TWAI(CAN)/
   RS-485+Modbus RTU/1-Wire/MCPWM/ADC/DAC/RMT/I2S/USB MSC/RTC
4. ✅ **`io` API, пользовательские ISR с учётом владельца** — таблица пинов с
   владельцем и причиной, пины системы и поднятых шин неотбираемы, всё занятое
   процессом возвращается при его завершении вместе со снятием обработчика.
   Шины поднимаются при первом обращении, пины и скорости из `BOARD.CFG`.
   ADC — за опцией сборки, см. «Чем за это платим» ниже
5. Класс `audio` и вывод через I2S — нужен эмулятору из фазы 3.5 и вообще всему,
   что издаёт звук
6. ✅ команды `dev`, `io` (карта пинов, настройка, скан I2C) и `drv`
   (список / load / unload)

Пункты 1, 2 и 4 — это фундамент под универсальную сборку с приёмом драйверов;
см. фазу 4.5.

## Фаза 4.5 — универсальная сборка и драйверы «на месте»

*Добавлено 6 августа 2026 по запросу.* Идея: кроме сборок под конкретные платы
есть **одна универсальная**, которая загружается почти на чём угодно, поднимает
канал связи (UART, Wi-Fi, Bluetooth) и через него принимает драйверы той
периферии, которая на этой плате есть. Плата, о которой система ничего не знает,
доводится до рабочего состояния без пересборки прошивки.

Это выполнимо, и почти всё для этого уже заложено: формат `.AXE` умеет модуль с
точкой входа `ag_driver_init` (флаг `AG_AXE_DRIVER`), загрузчик умеет положить
образ в арену и связать его с таблицей вызовов, `AG_SPAWN_RESIDENT` — остаться в
памяти после инициализации, `DEVICE=` в `SYSTEM.CFG` — список того, что грузить
при старте. Драйвер — это `.AXE`, который вместо работы регистрирует устройство.

### Что делится на две части, и это главное решение

Не всякую периферию можно вынести в загружаемый модуль, и делать вид, что можно,
нельзя:

| Что | Где живёт | Почему |
|---|---|---|
| Контроллеры внутри SoC: Wi-Fi, BT, USB host, SDMMC, I2S, LCD_CAM (RGB, i80, DVP), MCPWM | **в прошивке** | это стеки ESP-IDF в десятки и сотни килобайт, с прерываниями, DMA и своими задачами; вынести их в модуль — значит вынести половину IDF |
| Шины: I2C, SPI, UART, 1-Wire, TWAI, PWM, ADC | **в прошивке**, наружу через `io` | их немного, они нужны всем, и они дают модулям доступ к железу |
| Чипы на шинах: датчики, расширители портов, RTC, драйверы моторов, АЦП/ЦАП, считыватели | **`.SYS`** | это код, который знает про регистры одной микросхемы: килобайты, без прерываний или с одним GPIO-прерыванием |
| **Панель дисплея на SPI/I2C**: init-последовательность, коды команд окна, поворот | **`.SYS`, почти целиком данными** | панели различаются таблицей команд, а не алгоритмом; шину крутит движок |
| **RGB-панель** | **`BOARD.CFG`, без кода вообще** | различия — это тайминги и пины |
| **Сенсор камеры**: регистры по SCCB, режимы, форматы | **`.SYS`** | таблицы регистров одной микросхемы; захват DVP при этом в прошивке |

Ровно так это работало в DOS: BIOS плюс `DEVICE=`. Универсальная сборка — это
BIOS: все контроллеры SoC и все шины. Всё, что подключено *к* шинам, приезжает
модулями.

**Дисплей и камера делятся не целиком, а внутри себя** — движок интерфейса
остаётся, знание о конкретной микросхеме уезжает; разбор в §8.3
[архитектуры](00-architecture.md). Приятное следствие: драйвер панели или сенсора
— это в основном таблицы, а таблицы после переезда `.rodata` в часть данных **не
занимают арену кода** совсем. На панель остаётся килобайт-два логики.

**И то, чего «в прошивке» не даёт.** Универсальная сборка содержит все
*интерфейсы*, какие есть у чипа, — а не знание о каждой панели. Что не заработает
на S3 никаким кодом: MIPI-DSI и MIPI-CSI (нет в кремнии, есть у P4), RGB-панели
за пределами полосы PSRAM (800×480×2 при 60 Гц — это ~46 МБ/с только на скан, край
для octal PSRAM на 80 МГц), панели со своими напряжениями без обвязки на плате.
Всё остальное — вопрос таблицы и пинов, то есть данных.

### Порядок работ

1. ✅ **Модель устройств** (пункт 1 фазы 4) — классы, `/dev`, реестр с
   владельцем модуля и `ag_dev_revoke_owner`, который выгрузка модуля и
   позовёт. Таблица операций класса (`dev->ops`) в контракте есть, но пуста:
   ни одного класса со своей таблицей ещё нет, а придумывать её раньше первого
   дисплея значило бы гадать.
2. ✅ **`io` перестало быть `NULL`** — GPIO, I2C, SPI, UART, PWM с учётом
   владельца, чтобы два драйвера не дрались за шину. Пользовательские ISR тоже
   здесь; **код загруженного модуля лежит в арене, то есть в internal SRAM**, и
   это единственное место, откуда обработчик прерывания вообще имеет право
   работать — так что обработчик в `.SYS` возможен, в отличие от кода в PSRAM.
   Проверено: приложение, занявшее пин и упавшее, отдаёт его обратно.
3. ✅ **Загрузчик `.SYS`**: тот же образ, точка входа `ag_driver_init`, модуль
   остаётся резидентным, выгрузка с отзывом зарегистрированных устройств.
   Команды `drv` (что загружено), `dev` (что зарегистрировано). Пример
   `apps/echo`, `DEVICE=` / `[modules] device=` на стадии `modules`.
4. ✅ **Подбор драйвера по железу (I2C).** Таблица в `SYSTEM.CFG`
   (`modules.probe = bus:addr[:idreg=id]:path`); ядро спрашивает шину, при
   необходимости читает ID, грузит модуль с `ag_probe_hint()`. `drv probe`
   повторяет подбор вручную. Пример — `apps/whoami`. Для панелей на SPI
   сканировать нечего — тип и пины из `BOARD.CFG`.
5. ✅ **Argon CC → `.SYS` на госте** (10 августа 2026; см. бэклог CC п. 6).
   `edit` → `cc` → `drv load` / `drv install` без host `mkaxe`. Образец —
   [`apps/cc/examples/echo.c`](../apps/cc/examples/echo.c).
6. ⬜ **Каналы доставки**, по возрастанию цены:
   * **UART** — работает уже сегодня: `recv` принимает файл шестнадцатеричными
     строками, без протокола и без инструментов на другом конце. Медленно
     (~11.5 КБ/с), но драйвер — это единицы килобайт;
   * **Wi-Fi** — фаза 5. Своя точка доступа с фиксированным SSID, чтобы не было
     курицы и яйца с паролем чужой сети, и приём файла по TCP или HTTP. Стоит
     ~50 КБ internal SRAM, пока включён;
   * **Bluetooth (BLE)** — GATT-сервис с характеристикой на запись. Пропускная
     способность 10–20 КБ/с, для драйвера достаточно. Стоит десятки килобайт
     SRAM; **вместе с Wi-Fi включать по необходимости, а не всегда** — на S3
     обе подсистемы одновременно съедают заметную часть внутренней памяти.
7. ⬜ **Безопасность — обязательна, а не «потом».** Открытый канал, принимающий
   код, — это удалённое исполнение по замыслу. Для промышленного применения
   минимум: подпись образа (место в заголовке `.AXE` уже зарезервировано),
   **режим приёма, в который надо войти намеренно** (пин, перемычка или окно в
   первые секунды после сброса), и запись в журнал о каждом принятом модуле.
   Универсальная сборка, которая всегда слушает и всегда принимает, — это не
   удобство, а дефект.

**Готово, когда:** незнакомая плата с датчиком на I2C и дисплеем на SPI доводится
до работы без пересборки прошивки — драйверы приезжают по UART или по Wi-Fi,
`dev` показывает оба устройства, приложение их использует.

### Чем за это платим

* **Арена кода.** Все загруженные модули живут в тех же 64 КБ (до 192 КБ
  настройкой), что и приложения. Константы и таблицы драйвера в арену не идут —
  они уехали в PSRAM, — но инструкции идут. Десяток небольших драйверов
  укладывается; двадцать драйверов и приложение рядом — уже нет. Настоящее
  снятие лимита — это S-1 или R-1;
* **отсутствие изоляции.** Драйвер с диким указателем портит систему, как и
  приложение. Плата за модель, а не недоделка;
* **стабильность ABI.** Модуль — это двоичный файл, собранный против версии
  таблицы. Правила те же (`major`/`minor`), и с версии 1.0 ABI замораживается.

## Фаза 5 — «сеть»

Wi-Fi STA/AP, Ethernet, сокеты, DNS/SNTP, telnet-консоль, FTP/HTTP к SD,
MQTT, Modbus TCP, OTA. Подтаблица `api->net` перестаёт быть NULL.

Сделано (20 августа 2026), разбор — [10-network.md](10-network.md):

* сокеты TCP и `api->net` (не NULL с ABI 0.12), Wi-Fi STA на плате, Ethernet
  (OpenEth) в QEMU;
* **имена**: `ag_port_net_resolve` в контракте порта и `net->resolve` в ABI 0.33;
* **`wget`** — HTTP и FTP, chunked, тело без длины, перенаправления;
* **`httpd`** — каталог по HTTP, одно соединение за раз;
* **`ftp`** — сессия и одиночная выкачка, пассивный режим, двоичный;
* **`net`** — адрес, ожидание DHCP для скриптов, разрешение имён;
* проверка: 181 проверка текста протоколов на хосте, 37 в QEMU (`argon nettest`).

Осталось: Wi-Fi AP, TLS (и с ним `https`), SNTP, telnet-консоль вторым
endpoint, MQTT, Modbus TCP, OTA.

## Фаза 6 — «промышленная готовность»

Бенчмарки и бюджеты (§10 архитектуры) в CI, стресс-тесты, документация SDK
на английском, примеры, hardware-in-the-loop, релиз 1.0 и заморозка ABI.

---

## Очередь без платы (согласовано 8 августа 2026)

Работать в этом порядке, пока нет железа. Каждый пункт заканчивается чем-то
запускаемым в QEMU.

1. ✅ **HostFS** — живой диск `H:` к папке Windows (`argon run -HostFs DIR`);
   см. [`user/03-host-share.md`](user/03-host-share.md)
2. ✅ **CC.AXE** — упрощённый C → `.AXE` на самой ОС (фаза 3.5);
   `apps/cc/`, host-тесты выражений, smoke в QEMU (`run a:\cc.axe …`)
3. ✅ **littlefs** для `/sys` (`C:`) вместо FAT (`joltwallet/littlefs`,
   `mount` → `lfs`)
4. ✅ **Двойная буферизация** в soft/`gfx` — front (QEMU/`fb0`) + back в
   PSRAM; `acquire` → `double_buf`; `flush`/`swap` копируют на front
5. ✅ **Подпись `.AXE`** — HMAC-SHA256-128 в `reserved[6]`; нули = без
   подписи; `tools/signaxe.py`, `ag_axe_check_sig` в загрузчике
6. ✅ **CI** — локально `argon check`; в облаке `.github/workflows/ci.yml`
   (host-тесты + `idf.py build` на esp32s3 / IDF 5.5.5)
7. ✅ **Доводка SMS** — host **PADPUSH** → guest cache; sticky TTL fallback;
   ✅ PSG + FM → `t:\sms.wav` / mock (без I2S); FM = decode OPLL → `ag_fm`;
   выгрузка WAV пока надёжнее через `A:` + `argon get` (не через `copy` на `H:`)
8а. ✅ **Ввод для эмуляторов** — 9 августа 2026: слой pad + 6-байтовый PADPUSH
   + `ag_btnp`; с `-HostFs` направления и кнопки держатся. Разбор — в пункте 5
   выше; USB HID / GPIO на плате — отдельно;
8. **Realtime-эмуляция** — цель изменена 9 августа 2026 с «mute MD без претензии
   на realtime» на 60 Гц: разбор бюджетов, затыков и план по шагам в
   [`07-emulator-performance.md`](07-emulator-performance.md). Сделано в тот же
   день: ✅ замеры и починка пути вывода на SMS, ✅ арена 192 КБ вместо
   `hot_text`, ✅ gwenesis как `MD.AXE`, ✅ ввод через pad layer,
   ✅ ROM Alex Kidd, ✅ Ofast на VDP + `setjmp` раз за кадр, ✅ звук:
   Z80(SMS)+`ag_fm`+PSG на том же ядре (~172 КБ). ✅ `ag_fm` OPN: 4-op + sin/TL
   + упрощённый EG. ✅ сравнение с `ym2612.c` как `MDYM.AXE` (~180 КБ). ✅
   стартовые кадры = тяжёлый 68000. ✅ `api->audio` + I2S/`pcm0` (stub без пинов).
   Осталось: замеры на плате
9. ✅ **HostFS: надёжный `copy` больших файлов на `H:`** (10 августа 2026) —
   PADPUSH глушится, пока открыт writable-хэндл; гость шлёт TX кусками по 128 Б
   и крутит RX между ними (иначе host `sendall` + guest `uart_write` клинят
   одну UART/TCP трубу). `copy t:\….wav h:\…` при живом `--pad-cfg` ок
10. ✅ **Размер арены кода из `SYSTEM.CFG`** — `[memory] app_arena_kb=` клипает
    usable размер связанного потолка (`CONFIG_ARGON_APP_ARENA_KB`); хвост IRAM
    линкеру не возвращается; нужен `reboot`. После S-1 — полноценное
    резервирование из конфига (§12 архитектуры).
11. ✅ **`unzip` в ядре + `ZIP.AXE`** — см. фазу 3.5 §1a.
12. ✅ **Сеть в QEMU (срез под звук)** — 9 августа 2026: OpenEth + `api->net`
    (TCP), `hostfwd` на `:5558`, MD `net` → `tools/pcmplay.py`. Wi‑Fi/полная
    фаза 5 — на плату.
13. ⬜ **Doom: клавиатура в QEMU** — 13 августа 2026 отложено: после нескольких
    кругов правок клавиши всё ещё пропадают (нельзя стартовать с титра).
    Не гадать следующим патчем. План и что уже пробовали:
    [`plans/doom-input.md`](plans/doom-input.md).
14. ✅ **Звуковой слой приложений** — 15–16 августа 2026, в плане его не было
    (см. §4 «Звук — которого в плане нет» выше). Общий fixed-point
    [`ag_dsp`](../apps/common/dsp), синтезатор VA/FM
    [`ag_synth`](../apps/common/synth) с матрицей модуляции и `SYNTH.AXE`,
    сэмплер [`ag_smp`](../apps/common/smp), вывод
    [`ag_pcm`](../apps/common/pcm); `ag_dx7`/`ag_grain`/`ag_ir` переведены на
    общий слой. Это **userspace**, не ядро: класс устройств `audio` и I2S
    по-прежнему в фазе 4 и по-прежнему требуют платы.
15. ✅ **Проверка того, что вне ядра** — 16 августа 2026: `argon apps`
    (`tools/apps.json`, 26 образов) и `host-tests/test_dsp.c`, оба в
    `argon check` и в CI. До этого приложения не собирались нигде
    автоматически, а DSP не был покрыт ни одним тестом — грабли 30 в
    [05-status.md](05-status.md).

16. ✅ **Энергосбережение по команде** — 21 августа 2026, в плане его не было.
    Три вещи: частота (`power eco` / `doze` / `full`, шаги дерева тактов
    240/160/80/40), экран (софтовый буфер, текстовая панель и подсветка через
    `AG_IOC_DISPLAY_BACKLIGHT` у драйвера, который владеет пином), и
    **оповещение приложений** — `api->power` (ABI 0.34): система объявляет
    переход, приложение отвечает `OK` / `PARKED` / `HOLD`, молчание считается
    согласием через полсекунды, `HOLD` отказывает команде и снимается `/force`.
    118 проверок на хосте, команда и `PWR.AXE` — в QEMU. ⚠ само масштабирование
    частоты за `CONFIG_ARGON_ENABLE_PM` и **в образ S3 не влезает** (переполнение
    `dram0_0_seg` ~на 1 КБ); образ esp32 собирается. Разбор —
    [11-power.md](11-power.md). Осталось платы: миллиамперы в каждом режиме,
    целостность консоли после смены частоты APB, подсветка и sleep-in ILI9341.
    Не сделано намеренно: автоматика по простою, light sleep (сперва событийные
    консоль/сеть/супервизор), deep sleep, батарея.

После появления платы: S-1, панели, USB-HID, I2S, замеры % realtime SMS/MD,
миллиамперы в режимах `power`.

---

## Что нужно от тебя по ходу

| Когда | Что |
|---|---|
| Спайк S-1 | плата ESP32-S3 c PSRAM — это блокирует загрузчик |
| Фаза 1 | SD-карта, USB-UART |
| Фаза 3 | дисплей (любой из списка) и USB-клавиатура |
| Фаза 3.5, эмулятор | геймпад (USB HID или кнопки), выход звука на I2S-ЦАП |
| Фаза 4.5 | по одному образцу той периферии, которую надо доводить драйверами: датчик на I2C и дисплей на SPI хватит для проверки |

---

Идеи «на подумать», ещё не принятые в план: [`06-ideas.md`](06-ideas.md).
