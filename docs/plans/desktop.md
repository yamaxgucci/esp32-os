# План: графическая оболочка DESKTOP.AXE (стиль Windows 3.11)

Статус: **этап 0 сделан и проверен**, этапы 1–4 впереди. Ветка
`worktree-desktop`. Дата: 8 сентября 2026.

Готово по этапу 0: `apps/desktop/` в сборке и в CI, захват дисплея, фон стола,
строка меню и строка состояния, программный указатель без следа, перерисовка
по повреждённым прямоугольникам, юнит-тесты геометрии в `argon tests`,
`apps/desktop/check.ps1` — четыре прогона подряд с одинаковым результатом.
Читать также [`apps/desktop/README.md`](../../apps/desktop/README.md): там
описано, что нашлось по дороге.

Задача Максима: рабочий стол, папки, файлы, окна, мышь. В стиле Win 3.11.
Суперкомпактно и без тормозов. Стиль Win 95 (панель задач, «Пуск») отложен
и вынесен в §9 вместе со всем остальным, что сейчас делать не будем.

Этот документ — то, что читать, возобновляя работу. Решения в §3 приняты
до первой строки кода; менять их — только с записью, почему.

---

## 1. Что уже есть в системе и на что опирается оболочка

| Нужно оболочке | Есть | Где |
|---|---|---|
| Кадровый буфер и примитивы | soft RGB565, `fill_rect`, `blit`, `clip`, `text` 8×16, `flush(rect)` | `sdk/include/argon/abi.h`, `ag_gfx_api_t` |
| Мышь в пикселях | `AG_EV_POINTER_DOWN/UP/MOVE`, `AG_EV_WHEEL`, `ptr.buttons` | ABI 0.42, USB-мышь на S3 проверена |
| Клавиатура | `AG_EV_KEY_DOWN/UP/CHAR`, HID-коды, модификаторы | `argon/keys.h` |
| Запуск программ | `proc->exec` — блокирующий, возвращает код выхода | `ag_exec` |
| Файлы | `opendir/readdir/stat/rename/unlink/mkdir/rmdir/mountinfo` | `ag_fs_api_t` |
| Операции копирования с прогрессом | написаны, но привязаны к двухпанельному UI | `apps/fm/fmops.c` |
| Ассоциации расширений | `[assoc]` в SYSTEM.CFG, читает `fm` | `docs/03-board-config.md` |
| Автозапуск при загрузке | `AUTOEXEC.BAT` / `shell.autoexec` | там же |
| Второй шрифт | 8×8, сгенерирован из ядерного 8×16 | `apps/common/font8x8.h` |
| Виртуальная мышь и клавиатура в QEMU | `MOUSEVIRT.SYS`, `KBDVIRT.SYS`, `tools/virt.py`, `tools/inputplay.py` | `apps/mousevirt/README.md` |
| Снимок экрана QEMU | `tools/grab-window.ps1` (сломан: не находит окно SDL, см. §7) | |
| Прецедент графического приложения над файлами | `GFXFM.AXE` | `apps/gfxfm/` |

Ничего из перечисленного не требует правок ядра для первой версии.
Единственная правка вне `apps/desktop/` — вынос ядра файловых операций из
`fm` в общий код (§6, этап 3).

## 2. Где это будет работать

Размер системной поверхности решает всё, и он разный:

| Машина | Поверхность | Указатель | Годится для v1 |
|---|---|---|---|
| QEMU `-Gfx` | 640×400 (или 320×240 через `[display]`) | MOUSEVIRT / KBDVIRT | да, основная среда разработки |
| ESP32-S3-Zero + CYD как экран по проводу | 320×240 в PSRAM | USB-мышь и клавиатура в S3 | да, основная плата |
| ESP32-S3-CAM с ILI9341 | 320×240 в PSRAM | USB-мышь | да |
| CYD сама по себе | **160×120** (150 КБ под 320×240 в 179 КБ RAM не помещаются) | тач XPT2046 | **нет** — см. §9.2 |
| C6-LCD-1.47 | нет поверхности, только `present` полосами | нет | **нет** — §9.2 |

Вывод: v1 рисует через `ag_gfx_*` в системную поверхность и рассчитана на
320×240 как на **минимум**; 640×400 в QEMU — то же самое с большим столом.
Плата, где реально будут тыкать пальцем (CYD), в v1 не входит, и это надо
помнить, а не удивляться потом.

## 3. Решения, принятые до начала

### 3.1 Модель исполнения: Program Manager, не многозадачный WM

Модель DOS остаётся. Оболочка — обычный `DESKTOP.AXE`, она **не в ядре**
(`docs/00-architecture.md` §GUI: «полноценный GUI — библиотека в SDK, не в
ядре»). Все перекрывающиеся окна принадлежат ей самой: папки, диалоги, меню.
Запуск программы:

1. закрыть меню, спрятать курсор, `ag_gfx_release()`;
2. напечатать в консоль `Loading NAME ...` (как делает `fm`: загрузка с HostFS
   видна как чёрный экран, и без строки оператор думает, что всё умерло);
3. `ag_exec(path, argc, argv)` — блокируется до выхода программы;
4. вернуться: `ag_gfx_acquire()`, `ag_flush_input()`, полная перерисовка.

**Правило «нажмите любую клавишу».** Консольная программа печатает и
выходит, и её вывод исчезнет под столом за миллисекунду. Поэтому перед
запуском оболочка читает заголовок `.AXE` (`ag_axe_header_t.flags`): если
нет `AG_AXE_NEEDS_GFX` — после выхода ждать клавишу или щелчок, как `fm`.
Графические программы возвращают стол сразу. Ненулевой код выхода — в
окне сообщения.

Это ровно Win 3.11 с DOS-программой на весь экран, и это единственная
модель, в которой не нужен второй буфер на каждое окно.

### 3.2 Перерисовка: без буферов под окнами, по повреждению

- Окна не хранят своих пикселей. Есть список окон снизу вверх и список
  повреждённых прямоугольников (до 8, при переполнении — объединение).
- Перерисовка одного повреждения: `clip(rect)` → фон стола и его значки →
  каждое окно, пересекающее rect, снизу вверх, целиком (клип отсекает) →
  курсор → `flush(rect)`.
- На 320×240 с пятью окнами это доли миллисекунды растеризации; дороже
  всего `flush`, поэтому **никогда не флашить больше, чем повреждено**, и
  не рисовать, если повреждений нет. Цикл событий блокируется в
  `ag_poll_event` без таймаута, кроме двух случаев: ждём второй щелчок
  двойного (400 мс) или мигает каретка в поле ввода (500 мс).
- Оптимизация «вычесть верхние непрозрачные окна из фона» — **только по
  замеру**, см. §9.5.

### 3.3 Перетаскивание — рамкой, как в Win 3.x

Пока кнопка нажата, ходит только контур окна (4 линии толщиной 1 px). Под
линиями сохраняется подложка (максимум 2·(320+240)·2 байта ≈ 2,2 КБ),
рисуется рамка, флашатся 4 тонких прямоугольника. На отпускании — одно
перемещение окна и перерисовка двух повреждений: старого и нового места.

Это не стилизация. По проводу к CYD движущаяся картинка стоит 3,5 кадра в
секунду (`argonos-two-frame-rates`), и тащить окно телом — значит увидеть
это своими глазами. То же для изменения размера.

### 3.4 Курсор — программный

16×16, 1 бит маска + 1 бит цвет, подложка 512 байт. Движение мыши без
других изменений: восстановить старую подложку, сохранить новую, нарисовать,
флашнуть два прямоугольника 16×16. Курсор рисуется последним поверх любого
повреждения. Формы: стрелка, песочные часы (пока читается каталог или идёт
копирование), стрелки размера.

### 3.5 Внешний вид Win 3.11

- Палитра — 16 цветов VGA + серый `C0C0C0` и тёмно-серый `808080`
  (в RGB565 это точные значения, дизеринга нет). Фон стола по умолчанию
  `008080`, настраивается.
- Фаска: светлая сверху-слева (белый), тёмная снизу-справа (`808080`),
  внешняя чёрная линия у кнопок. Заголовок активного окна `000080` с белым
  текстом, неактивного — белый с чёрным. Рамка окна 4 px с угловыми
  насечками для изменения размера, как в 3.1.
- Слева в заголовке — кнопка системного меню (чёрточка), справа —
  свернуть/развернуть (треугольники). Свёрнутое окно — значок с подписью
  внизу стола, как в 3.11. Панели задач нет.
- У стола — строка меню сверху, как у Program Manager: **Файл** (Выполнить,
  Обновить, Выход в консоль), **Окно** (Каскад, Мозаика, Упорядочить значки,
  список открытых окон), **Справка** (О программе). Это замена панели задач
  в стиле 3.11 и единственное место, откуда всё достаётся без правой кнопки
  — правой кнопки в 3.11 не было, а на таче её нет вообще.
- Шрифты: **только два бесплатных** — ядерный 8×16 для заголовков, меню и
  списков, 8×8 из `apps/common/font8x8.h` для подписей значков и строки
  состояния. Своего шрифта в v1 нет (§9.6).
- Значки: один набор 16×16, 4 бита цвет + 1 бит маска = 160 байт на значок.
  В списках рисуются 1:1, на столе — с удвоением до 32×32 (пиксель-арт
  удваивается без потерь, память та же). Набор v1: диск, дискета, сетевой
  диск, папка, открытая папка, программа, текст, картинка, звук, неизвестный
  файл, свёрнутое окно, «мой компьютер». Двенадцать штук — 2 КБ.

### 3.6 Стол — это папка

Стол показывает содержимое `C:\DESKTOP\` (создаётся при первом запуске)
плюс фиксированные значки дисков из `mountinfo`. Значит, стол — тот же код,
что окно папки, с другой раскладкой. Положения значков и геометрия окон —
в `C:\DESKTOP.INI` (§5.10).

### 3.7 Раскладка на 320×240

| Элемент | Размер |
|---|---|
| Строка меню стола | 18 px |
| Заголовок окна | 18 px (текст 8×16 + 1 px) |
| Рамка окна | 4 px |
| Окно папки по умолчанию | 240×160, каскад со сдвигом 16 px |
| Строка списка | 16 px (значок 16 + текст 8×16) — на 240×160 видно 7 строк |
| Ячейка значка стола | 48×40 (32×32 + подпись 8×8 в одну строку, обрезка с `...`) |
| Полоса прокрутки | 12 px, стрелки 12×12 |
| Кнопка диалога | 64×20 |

Мишени для пальца (когда дойдём до тача) — не меньше 12 px, и это уже
соблюдено везде, кроме угловых насечек размера.

### 3.8 Ввод

- Двойной щелчок: 400 мс и ≤ 3 px между нажатиями. Порог начала
  перетаскивания 3 px.
- Колесо — прокрутка списка под курсором (не активного окна).
- Клавиатура: `Alt+F4` закрыть, `Alt+Tab` следующее окно, `Esc` закрыть
  меню/диалог, `Enter` открыть выделенное, стрелки — по списку,
  `Backspace` — на уровень вверх, `F5` обновить, `F2` переименовать,
  `Del` удалить, `F10` — в строку меню. `Alt+буква` для меню — §9.13.
- `Ctrl+C` / `AG_EV_QUIT` от супервизора — выход в консоль с сохранением
  DESKTOP.INI.

### 3.9 Память

| Что | Байт | Где |
|---|---|---|
| Окно (`dsk_win_t`) | ~96 × 12 максимум | статически |
| Запись каталога (имя ≤ 64, размер, дата, флаги, значок) | 80 | куча, массив на окно, до 512 записей = 40 КБ |
| Повреждения | 8 × 8 | статически |
| Подложка курсора | 512 | статически |
| Подложка рамки перетаскивания | ≤ 2 240 при 320×240, ≤ 4 160 при 640×400 | куча при `acquire`, по размеру экрана |
| Значки | 12 × 160 | `.rodata` |
| Шрифт 8×8 | 2 048 | `.rodata` |
| Код, оценка | 35–45 КБ | арена 192 КБ, без XIP |

Стек — 16 КБ по умолчанию, чтения каталогов на куче, рекурсии в копировании
глубиной каталога (`fmops` уже так живёт). Всё, что зависит от размера
экрана, считается от `ag_gfxinfo_t` при `acquire`, не от констант
(`argonos-fallout-16mb-later`: никаких зашитых компромиссов по памяти).

### 3.10 Что оболочка **не** трогает в ядре

Ни одной новой функции ABI в v1. Если по ходу окажется, что нужна — сначала
запись в этот план, потом код.

## 4. Устройство кода

```
apps/desktop/
  desktop.c       ag_main: acquire, цикл событий, выход
  dsk.h           общие типы: rect, окно, событие, палитра
  dsk_rect.c/.h   прямоугольники, пересечение, список повреждений   (чистый C)
  dsk_paint.c/.h  рисовальщик над ag_gfx_*: fill, hline/vline, фаска,
                  text16, text8, icon 4bpp+mask ×1/×2, save/restore rect, clip
  dsk_cursor.c    программный указатель
  dsk_wm.c        список окон, z-порядок, hit-test, активация,
                  повреждение → перерисовка, перетаскивание рамкой, размер
  dsk_menu.c      строка меню и выпадающие меню (модальный захват)
  dsk_widgets.c   кнопка, полоса прокрутки, поле ввода, список
  dsk_dlg.c       окно сообщения, окно ввода, окно прогресса
  dsk_folder.c    окно папки: чтение каталога, сортировка, вид «значки»/«список»,
                  выделение, открытие
  dsk_shell.c     сам стол: диски, C:\DESKTOP, раскладка значков, свёрнутые окна,
                  меню стола
  dsk_run.c       запуск: правило NEEDS_GFX, ассоциации, .BAT через exec как в fm
  dsk_ini.c       DESKTOP.INI: чтение/запись                                (чистый C)
  dsk_icons.c     битмапы значков и курсоров, палитра
  README.md       сборка, запуск, клавиши, скрипт проверки

apps/common/fsops/
  fsops.c/.h      ядро copy/move/delete/mkdir с обратным вызовом прогресса,
                  вынесено из apps/fm/fmops.c; fm и desktop зовут одно и то же

host-tests/
  test_desktop.c  dsk_rect, dsk_ini, раскладка значков, hit-test и z-порядок
                  dsk_wm с подменённым рисовальщиком
```

«Чистый C» — файлы без `argon.h`, компилируются в `argon tests` как есть.
`dsk_wm.c` рисует через таблицу функций `dsk_paint`, поэтому в host-тесте
рисовальщик — заглушка, которая записывает вызовы; проверяется, что при
повреждении rect перерисованы ровно те окна, что его пересекают, и в том
порядке.

Запись в `tools/apps.json`, группа `core`, `-fno-builtin` как у остальных
графических образов. Собирается `argon apps --only DESKTOP.AXE`, запускается
`run h:\desktop.axe`.

## 5. Этапы

Каждый этап заканчивается сценарием в QEMU, который прогоняется скриптом
без рук (§7), снимком экрана и, где применимо, числом.

### Этап 0 — каркас и стенд ✅ (8 сентября 2026)

1. ✅ `apps/desktop/`, запись в `tools/apps.json` (группа `core`, значит и CI).
   Заглушек не делал: файлы созданы настоящими по мере надобности, образ
   собирается с первого дня — а именно это и было целью пункта.
2. ✅ `desktop.c`: acquire, фон, строка меню (пунктов нет), строка состояния
   с координатами указателя и счётчиками событий, курсор, `Esc`/`Q`/`Ctrl+C`.
   Плюс `F5` — перерисовать всё, `F6` — послать кадр заново, ничего не рисуя.
3. ✅ `dsk_rect.c` и `dsk_layout.c` + `host-tests/test_desktop.c`: пересечение,
   объединение, слияние повреждений (включая свойство «список никогда не
   теряет добавленного»), раскладка на 320×240, 640×400, 160×120 и на
   вырожденных экранах. В `argon tests`.
4. ✅ `tools/grab-window.ps1` починен: `EnumWindows` вместо
   `MainWindowTitle`, `PrintWindow(h, dc, 3)`, и предупреждение, если снимок
   вышел одноцветным.
5. ✅ `apps/desktop/check.ps1`, и стенд получил два умения:
   `qemu-boot.ps1 -Gfx` и элементы `-Send` с префиксом `!` (команда хоста
   посреди сценария). `tools/inputplay.py` перенесён из ветки
   `worktree-fallout-cxx` — в `main` его не было.

Сделано, когда: курсор ходит без следов, `argon tests` зелёный (18 423
проверки), PNG получен скриптом. **Плюс критерий, которого в плане не было и
который стоило добавить: два снимка — как есть и после принудительной
перерисовки — должны совпадать.** По одному снимку нельзя отличить пиксели,
которых оболочка не нарисовала, от нарисованных, но не доехавших до панели;
именно этот критерий поймал разорванную пересылку кадра.

Четыре прогона подряд: 77 событий указателя, 2 клавиши, 41 пиксель различия
(цифры в строке состояния).

**Пять починок вне `apps/desktop/`, все — причины, а не симптомы:**
`panel_hw.c` не подключал `sdkconfig.h` и вся RGB-панель QEMU вырезалась
препроцессором; QEMU не создаёт панель без `-M esp32s3,graphics=on`; KBDVIRT
выбрасывал байты со свежепринятого сокета; `ensure_listen` в обоих
виртуальных драйверах затирал хороший дескриптор при гонке двух потоков;
KBDVIRT и MOUSEVIRT не публиковали `ag_input_ops_t.poll`, из-за чего
приложение, берущее события из `ag_poll_event`, не получало их вовсе. Разбор
каждой — в [`apps/desktop/README.md`](../../apps/desktop/README.md).

### Этап 1 — оконный менеджер

1. `dsk_paint`: фаска, текст обоих шрифтов, значок ×1/×2, save/restore.
2. `dsk_wm`: создать/закрыть окно, z-порядок, активация щелчком, повреждение
   и перерисовка по §3.2, тестовое окно со статическим содержимым.
3. Перетаскивание рамкой (§3.3), изменение размера за угол, свернуть в
   значок, развернуть двойным щелчком по заголовку, восстановить.
4. Меню: строка меню окна и стола, выпадающие списки, выбор мышью и
   стрелками, `Esc`, щелчок мимо закрывает.
5. Диалоги: окно сообщения (Ok / Yes-No), окно ввода строки с кареткой,
   модальность (события идут только диалогу, остальное затенено не нужно —
   в 3.11 не затеняли).
6. `Alt+Tab`, `Alt+F4`.
7. Host-тест `dsk_wm` с заглушкой рисовальщика.

Готово, когда: сценарий «открыть 4 окна, перетащить, свернуть, развернуть,
закрыть» проходит скриптом; на снимке нет мусора; при неподвижной мыши
процесс спит (`ps` показывает 0 ‰ CPU).

### Этап 2 — папки и запуск

1. `dsk_folder`: чтение каталога на кучу, сортировка (папки, потом файлы,
   по имени), два вида, полоса прокрутки, колесо, выделение, `Enter` и
   двойной щелчок; песочные часы на время чтения (HostFS медленный).
2. Открытие папки — в том же окне; `Ctrl`+двойной щелчок — в новом.
   `Backspace`/`..` — вверх.
3. `dsk_shell`: значки дисков из `mountinfo`, содержимое `C:\DESKTOP`,
   раскладка сеткой, «Упорядочить значки», свёрнутые окна внизу.
4. `dsk_run`: `.AXE` — по §3.1 с правилом NEEDS_GFX; расширение из `[assoc]`
   — запуск обработчика с файлом аргументом; `.BAT`/`.CMD` — как в `fm`;
   иначе — окно «Нет программы для этого файла».
5. Пункт «Выполнить…» с полем ввода.

Готово, когда: из стола открывается `H:`, по двойному щелчку запускается
`GFXDEMO.AXE`, после его выхода стол на месте и целый; `HELLO.AXE` показывает
свой текст до клавиши; `.TXT` открывается в `EDIT`.

### Этап 3 — операции над файлами

1. Вынести из `apps/fm/fmops.c` в `apps/common/fsops/` ядро: copy_file,
   копирование дерева, move (rename, иначе copy+delete), удаление дерева,
   с обратным вызовом прогресса и отмены. `fm` переводится на него,
   `argon test` с `fm` — регрессия.
2. Диалоги: копировать/переместить «куда» (поле ввода с путём), удалить
   с подтверждением, переименовать (`F2`), создать папку, прогресс с
   `Esc` для отмены.
3. Свойства файла: имя, размер, дата, атрибуты; свойства диска: fs, всего,
   свободно, только чтение.
4. Обновление окон, показывающих затронутый каталог, после операции.
5. Ошибки — окном сообщения с `ag_strerror`.

Готово, когда: скопировать каталог с `H:` на `C:` с прогрессом, отменить
на середине, удалить остаток — всё мышью, состояние диска после каждого шага
сверено `dir` из консоли.

### Этап 4 — сохранение, документация, плата

1. `DESKTOP.INI`: положения значков стола, геометрия и вид открытых окон
   (восстанавливаются при старте), цвет фона, время двойного щелчка.
   Пишется при выходе и после перемещения значка.
2. «О программе», строка состояния в окне папки (N объектов, M байт).
3. Документация: `apps/desktop/README.md`, страница
   `docs/user/05-desktop.md`, строки в `05-status.md` и `04-roadmap.md`,
   как запускать из `AUTOEXEC.BAT`.
4. Плата: S3-Zero + USB-мышь + CYD по проводу. Замерить и записать в
   README: время от щелчка до появления окна папки с `H:` и с `C:`,
   худший кадр при перетаскивании, флаш при движении мыши. Считать то, что
   видит Максим, а не среднее (`argonos-measure-what-he-sees`).

Готово, когда: перезапуск возвращает стол таким, каким его оставили, а
числа с платы стоят в README.

## 6. Что придётся тронуть вне `apps/desktop/`

| Файл | Зачем | Риск |
|---|---|---|
| `apps/fm/fmops.c` → `apps/common/fsops/` | одно ядро операций на два менеджера | регрессия `fm`; проверка `argon test` со сценарием копирования |
| `tools/grab-window.ps1` | снимки для проверки | нет |
| `tools/apps.json` | сборка образа в CI | нет |
| `host-tests/CMakeLists.txt` | новый тест | нет |
| `docs/*` | статус, дорожная карта, пользовательская страница | нет |

Ядро — ничего.

**Что пришлось тронуть на самом деле (этап 0).** Ни одной новой функции ABI,
как и обещано, но список файлов оказался длиннее — потому что стенда для
графики в QEMU фактически не было, а то, что от него осталось, было сломано:

| Файл | Зачем |
|---|---|
| `components/argon_port/idf/src/panel_hw.c` | `#include "sdkconfig.h"`, без которого панель QEMU не компилировалась вообще; плюс жалоба вслух, когда панель не открылась, и починка разорванной пересылки строк |
| `tools/qemu-common.ps1` | `-M esp32s3,graphics=on`, без которого панели в эмуляторе нет |
| `tools/qemu-boot.ps1` | `-Gfx` и элементы `-Send` с префиксом `!` |
| `tools/inputplay.py` | перенесён из `worktree-fallout-cxx`, в `main` отсутствовал |
| `apps/kbdvirt/kbdvirt.c`, `apps/mousevirt/mousevirt.c` | такт ядра (`ag_input_ops_t.poll`), гонка в `ensure_listen`, выброшенные байты при accept |

Правка драйверов — отступление от «ядро ничего», и оно обосновано: без такта
ядра каждое графическое приложение обязано было бы само открывать
`/dev/mouse0` и читать его каждый кадр, а забывший это выглядит как
неработающая мышь. Это уже разбирали с нуля дважды — для Doom и для этого
стола.

## 7. Как проверяется

- **Юнит:** `argon tests` — `dsk_rect`, `dsk_ini`, раскладка, hit-test,
  порядок перерисовки.
- **QEMU без рук:** `apps/desktop/check.ps1` — `qemu-boot.ps1 -Gfx -HostFs`,
  драйверы, `inputplay`, два снимка и их сравнение. Стабильно: четыре
  прогона подряд дают одинаковые числа.
- **QEMU 320×240 — пока вручную.** Размер читается из `[display]` в
  `C:\SYSTEM.CFG`, то есть нужна запись на C: и перезагрузка, а запись на C:
  в QEMU для теста недостаточно надёжна: на восьми прогонах такой формы
  трижды файл, записанный на C:, читался обратно битым (`-91`, AG_EFORMAT —
  один раз `c:\desktop.axe`, дважды драйвер, только что скопированный
  `drv install`), загрузка после такой записи занимала 128 секунд вместо
  двух, и HostFS не всегда возвращается после сброса. Оболочка на 320×240
  снята и раскладка верна; не хватает способа задать размер, не записывая на
  C: — это `tools/mksysfs.py`, отдельная задача.
- **Плата:** S3-Zero, USB-мышь, CYD-экран по проводу; числа — §5, этап 4.
  Помнить, что подглядывание в плату убивает то, что на ней идёт
  (`argonos-three-hangs`): счётчики в приложении, не консоль.

## 8. Риски и открытые вопросы

1. **`ag_exec` при удержанном дисплее.** `fm` отпускает дисплей перед
   запуском, и дитя берёт его само; следуем этому. Что с событиями ввода,
   накопившимися за время работы ребёнка — `ag_flush_input()` при возврате;
   проверить, что первый щелчок после возврата не «проваливается» в стол.
2. **HostFS медленный.** Чтение каталога из `H:` — сотни миллисекунд; на
   это время песочные часы, и никакого чтения в обработчике движения мыши.
3. **Имена файлов не ASCII.** VFS отдаёт байты, шрифт ядра — CP437/866 по
   кодовой странице консоли. В v1 показываем байты как есть; §9.14.
4. **Тач с CYD по проводу.** Обратный канал провода подтверждён для данных,
   но идёт ли по нему тач как указатель — неизвестно. Не блокирует v1
   (мышь в S3), выяснить отдельно.
5. **Размер кода.** 45 КБ — оценка. Потолок l32r 256 КБ далеко, арена
   192 КБ — тоже, но если образ уйдёт за 100 КБ, значит что-то пошло не
   так; смотреть `mkaxe` после каждого этапа.

## 9. Что сейчас делать **не будем** — и почему

Каждый пункт: почему не сейчас, что заложено, чтобы это было возможно
потом, и чего это будет стоить.

### 9.1 Панель задач и меню «Пуск» (стиль Win 95)
Решение Максима: пока 3.11. Заложено: строка меню стола — отдельный объект
`dsk_shell`, высота полос стола читается из одной структуры раскладки;
панель задач добавится как ещё одна полоса и настройка в DESKTOP.INI.
Стоимость: ~300 строк, без изменений WM.

### 9.2 Свой растеризатор полосами — для CYD (160×120) и C6 (без поверхности)
Единственный способ получить 320×240 на плате со 179 КБ RAM — не хранить
кадр вовсе: держать список того, что на экране, и растеризовать
повреждение полосами 320×16 (10 КБ) через `gfx->present`. Заложено:
всё рисование идёт через таблицу `dsk_paint`, у которой шесть операций;
второй рисовальщик пишет в полосу вместо поверхности. Не заложено: чтение
подложки (курсор, рамка перетаскивания) — в полосовом режиме оба
становятся обычными повреждениями и перерисовываются. Стоимость: ~600
строк и свой `text8x16` (ядерный рисует только в поверхность). Это самый
ценный из отложенных пунктов, потому что CYD — единственная плата с тачем.

### 9.3 Окна для *работающих* приложений (настоящий многозадачный WM)
Требует поверхности на каждое приложение или оконности в ядре — смена
модели DOS. Уже есть план слотов сессий и `Alt+Tab` между полноэкранными
приложениями (`docs/plans/session-slots-and-system-shell.md`); стол мог бы
показывать список живых слотов и переключать их. Не раньше, чем слоты
понадобятся сами по себе.

### 9.4 Консоль в окне («MS-DOS Prompt»)
Консоль ядра рисует прямо на дисплей. Нужен API «консоль в буфер ячеек»,
который стол будет отрисовывать сам — правка ядра. Второй по ценности
пункт после 9.2: с ним стол становится рабочим местом, а не запускалкой.

### 9.5 Буфер под окнами и вычитание непрозрачных окон из фона
Ускорение перерисовки, которое стоит памяти или сложности. Только если
замер на плате покажет, что перерисовка повреждения, а не `flush`,
занимает заметное время. Пока оценка говорит обратное.

### 9.6 Пропорциональный шрифт (вид MS Sans Serif)
8×16 моноширинный на 320×240 крупноват, и это будет видно. Свой
битмапный шрифт 6×10 или пропорциональный — 2–3 КБ данных и свой
рисовальщик текста (ядерный знает только 8×16). Делать, когда стол увидят
на плате и станет ясно, что мешает.

### 9.7 Перетаскивание файлов между окнами (копировать/переместить мышью)
После того как операции через меню работают. Заложено: `dsk_wm` умеет
захват мыши и hit-test по любому окну под курсором.

### 9.8 Буфер обмена для файлов (вырезать/копировать/вставить)
То же; ~150 строк поверх `fsops`.

### 9.9 Значки внутри `.AXE`
Секция ресурсов в образе и правка `mkaxe.py`. В v1 значок выбирается по
расширению из таблицы.

### 9.10 Обои-картинка
150 КБ в памяти и полный кадр по проводу при каждом обнажении фона. В v1
— сплошной цвет или узор 8×8.

### 9.11 Гашение подсветки по простою
`backlight()` есть в ABI, `[power]` в ядре есть. Мелочь, но не про окна;
после этапа 4, если Максим захочет.

### 9.12 Жесты тача: долгое нажатие как контекстное меню, инерционная прокрутка
Нет платы с тачем в v1 (см. 9.2).

### 9.13 Полная работа с клавиатуры: `Alt+буква` в меню, `Tab` по кнопкам диалога
Частично есть (§3.8); мнемоники и обход фокуса — после того как мышь
работает без замечаний.

### 9.14 Кириллица в именах файлов, UTF-8
VFS отдаёт байты; какая кодовая страница — вопрос ко всей системе, не к
столу. Показываем как есть.

### 9.15 Диалог настроек
Цвета, скорость двойного щелчка, размер шрифта — в v1 правятся в
`DESKTOP.INI` через `EDIT`.

### 9.16 Корзина, справка, звуки событий, темы
Нет.

### 9.17 Стол как системный шелл (слот 0 вместо текстовой консоли)
Архитектурное решение уровня всей ОС. Стол запускается из `AUTOEXEC.BAT`
и возвращает в консоль по выходу — этого достаточно, пока нет 9.3 и 9.4.

### 9.18 Множественное выделение (рамкой, Shift/Ctrl-щелчок)
Одиночное выделение в v1; множественное — вместе с 9.7 и 9.8, когда есть
что с ним делать.

### 9.19 Сетевые места в GUI
HostFS уже виден как диск `H:`; отдельного узла «Сеть» нет.
