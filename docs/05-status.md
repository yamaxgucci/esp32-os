# Состояние проекта

> Читать первым при возобновлении работы. Здесь — что сделано, что нет, как
> собрать и проверить, и на какие грабли уже наступили.
>
> Дата: 27 августа 2026. Обновлять при каждой смене этапа.
>
> **26–27 августа 2026 — ESP32-S3 на столе.** Первый bring-up на реальной плате
> (N16R8 CAM) закрыл то, что стояло как «требует железа»: **S-1** (код из PSRAM),
> **I2S + класс `audio`**, **ADC**, **дисплей** (`ILI9341.SYS` + графика),
> **камера** (`GC2145.SYS`), **recovery падения** без ресета платы. Пункты ниже
> приведены в соответствие. Из критерия v1 открыта одна вещь — **USB-клавиатура**
> (USB-host стека ещё нет). Разбор — [`09-esp32-cyd.md`](09-esp32-cyd.md).

## Что это

**ArgonOS** — ОС для ESP32 с моделью исполнения в стиле MS-DOS: приложение
владеет машиной, ОС — тонкий слой сервисов. Не клон Linux, не «поделка»:
целевое применение промышленное. Обоснование всех решений — в
[00-architecture.md](00-architecture.md), план — в [04-roadmap.md](04-roadmap.md),
конфигурация плат — в [03-board-config.md](03-board-config.md), что где живёт в
памяти — в [13-memory.md](13-memory.md), бюджеты и затыки
realtime-эмуляции — в [07-emulator-performance.md](07-emulator-performance.md),
что стоит симуляция аналоговых цепей — в
[08-circuit-simulation.md](08-circuit-simulation.md), что умеет и чего стоит
сеть — в [10-network.md](10-network.md), режимы энергосбережения — в
[11-power.md](11-power.md).

Основная платформа **ESP32-S3** с PSRAM 8 или 16 МБ.
**Модуль с 32 МБ PSRAM непригоден** — см. «Грабли» ниже.

Плат на столе две, и они разные не только моделью: ESP32-2432S028R («CYD»,
Xtensa LX6) — [09-esp32-cyd.md](09-esp32-cyd.md); Waveshare ESP32-C6-LCD-1.47
(**RISC-V**, консоль по USB) — [12-esp32-c6-lcd.md](12-esp32-c6-lcd.md).

## Как работать

Одна точка входа, `argon.cmd` в корне. Это батник, а не `.ps1`, потому что
Windows по умолчанию запрещает выполнение скриптов PowerShell.

**Образы приложений и шара (обязательные пути):**

| Папка | Назначение |
|---|---|
| `build/apps/` | все собранные `.AXE` / `.SYS` (архив) |
| `build/sd_card/` | HostFS (`-HostFs`) и вход для `argon sync` / `-Share` |

`tools/mkaxe.py` после сборки сам копирует образ в обе папки (если не
`--no-stage`). Пиши `-o build/apps/NAME.AXE`. Подробнее —
[`docs/user/03-host-share.md`](user/03-host-share.md). Проверка звука
QEMU→Windows: [`apps/pcmvirt/README.md`](../apps/pcmvirt/README.md).

```
argon build              собрать прошивку
argon run                запустить в QEMU, консоль в этом окне (Ctrl+A, X — выход)
argon run -Sd            то же, с образом SD-карты
argon run -HostFs build\sd_card   живая папка Windows как H:
argon run -tcp           консоль на 127.0.0.1:5556, подключаться PuTTY в Raw
argon test ver mem       автотест: загрузка, набор команд, печать экрана
argon test -Sd ...       то же с картой
argon test -Put "build\apps\HELLO.AXE=t:\hello.axe" "run t:\hello.axe"
                         то же, предварительно залив файл в гостя через recv
argon test ... "~\x1c"   послать сырые байты, не дожидаясь приглашения:
                         так проверяется Ctrl+\ по работающему приложению
argon test -cp 866 ...   разобрать экран как CP866, а не CP437
argon test -Icount ...   виртуальное время по инструкциям, а не по часам хоста:
                         так меряется стоимость DSP-кода (грабли 33)
argon tests              юнит-тесты на ПК (3480 проверок)
argon nettest            сеть в QEMU против серверов на этом ПК
                         (tools/netfixture.py; 37 проверок)
argon boardnet -port COM3   сеть на настоящей плате по Wi-Fi (14 проверок);
                         -big — плюс 725 КБ на карту, для числа скорости
argon apps               собрать все .AXE / .SYS из `tools/apps.json`
argon apps --list        показать список, ничего не собирая
argon apps --only SYNTH.AXE DX7.AXE      собрать выборочно
argon apps --warnings    добавить -Wall -Wextra
argon check              локальный CI: `tests` + прошивка + `apps`
                         (то же в GitHub Actions: `.github/workflows/ci.yml`)
argon vt                 умеет ли эта консоль показывать экран
argon flash -port COM5   прошить настоящую плату и открыть monitor;
                         -NoMonitor — прошить и вернуться, для скрипта
argon target esp32c6     плата на RISC-V (docs\12-esp32-c6-lcd.md);
                         QEMU её не запускает — перед коммитом вернуться
                         на esp32s3 и прогнать argon check там
```

**Приложения собираются отдельно от прошивки, и это ловушка.** `.AXE` — своя
линковка, поэтому зелёная сборка прошивки про них не говорит ничего. Пока
команды сборки жили по одной строке в README каждого приложения, правка в
`apps/common` могла сломать полдюжины образов, а `argon check` этого не видел —
так и уехали в main четыре неработающих звуковых тракта (грабли 30). Теперь
список лежит в [`tools/apps.json`](../tools/apps.json), его собирает
`argon apps`, и он входит в `argon check` и в CI.

### Проверка виртуального звука (QEMU → Windows)

Полный чеклист также в [`apps/pcmvirt/README.md`](../apps/pcmvirt/README.md).
Образы уже должны лежать в `build/sd_card/` (`PCMVIRT.SYS`, `DX7.AXE`; демо
`build/sd_card/dx7/`).

В **PowerShell / Windows Terminal** из корня репо: `argon` не в PATH —
вызывай `.\argon.cmd`. Python — из IDF venv (не глобальный).

Сначала в каждом хост-терминале:

```powershell
cd "D:\Work\Unity Projects\ESP32-OS"
$py = "D:\Espressif\tools\python_env\idf5.5_py3.12_env\Scripts\python.exe"
```

1. Один раз (если нет `sounddevice`):
   `& $py -m pip install sounddevice`
2. Терминал 1 — гость:
   `.\argon.cmd run -Gfx -HostFs build\sd_card`
   Дождись сети (OpenEth / DHCP).
3. В госте (один раз на этот flash-образ):
   `drv install h:\pcmvirt.sys` → `dev pcmvirt`
   Должны появиться `pcmvirt` / драйвер `PCMVIRT`. После reboot `dev` без
   повторного install — ок.
4. Терминал 2 — хост (снова `$py=…` и `cd` в корень):
   `& $py tools\pcmplay.py`
   Можно `& $py tools\pcmplay.py --reconnect` или
   `& $py tools\pcmplay.py --record build\pcm_capture.wav`.
   Ждать клиента нормально: гость **не** блокируется.
5. В госте: `run h:\dx7.axe pcmvirt`
   Ожидай `sound = /dev/pcmvirt @ 22050 Hz`, без «waiting for host».
   Клавиши Z…M / Q…I или авто-MIDI из `h:\dx7\`.
6. Pass: в `pcmplay` строка `connected: 22050 Hz, 2 ch` и слышен звук;
   без плеера DX7 не ждёт; с `--reconnect` после обрыва звук возвращается.

Снять: `drv uninstall PCMVIRT`. Порт по умолчанию `127.0.0.1:5558`
(hostfwd у `.\argon.cmd run`).

Машинно-зависимые пути живут в `tools/local-env.ps1` (не в git):

| Что | Где |
|---|---|
| ESP-IDF 5.5.5 | `D:\Espressif\esp-idf` |
| Инструменты IDF (xtensa gcc, cmake, ninja, QEMU) | `D:\Espressif\tools` |
| Портируемый CPython 3.12 | `D:\dev\python312` |
| MinGW-w64 GCC 16.1 (host-тесты) | `D:\dev\mingw64` |

**Всё на `D:`** — на `C:` почти нет места.

### Чем и как проверяется

Железа нет. Проверка трёхуровневая, и это работает:

1. **Юнит-тесты на ПК** (`argon tests`) — всё, что не зависит от чипа:
   пути, конфиг, экран, кодеки терминала, редактор строки, VFS, RAM-диск,
   журнал, загрузчик, **DSP-слой `apps/common`** ([`host-tests/test_dsp.c`](../host-tests/test_dsp.c)).
   Собирается тем же GCC, что и целевой код, поэтому диагностика совпадает.
   Заголовки берутся из порта `bare` — то есть эта половина ядра компилируется
   при полном отсутствии машины под ней.
2. **Сборка всех приложений** (`argon apps`) — 26 образов `.AXE`/`.SYS` с
   `-Wall -Wextra`, ~30 с. Не тест, но ловит то, чего не ловит ни прошивка,
   ни юнит-тесты: приложение перестало компилироваться.
3. **QEMU** (`argon test`) — прошивка реально загружается. Эмулируется и
   SD-хост, и PSRAM. `build-host\vtdump.exe` восстанавливает экран из
   транскрипта, прогоняя его через наш же модуль экрана.
4. **Слушать** — `build-host\engine_smoke.exe` пишет `build/listen/*.wav`
   каждым звуковым движком, `build-host\synth_smoke.exe` — только `ag_synth`,
   `build-host\ckt_smoke.exe` — решатель аналоговых цепей, включая пару
   `ckt_alias_os1` / `ckt_alias_os4`, на которой видно, чего стоит
   передискретизация. Все три собираются вместе с host-тестами, чтобы не сгнить.
5. **Сеть** (`argon nettest`) — `tools/netfixture.py` поднимает на ПК HTTP и
   FTP с нарочно неудобными случаями, гость с ними разговаривает, а потом сам
   становится сервером: 32 КБ уезжают в гостя по HTTP и возвращаются через его
   `httpd` **побайтово тем же файлом**. Интернет не нужен ни для одной
   проверки, кроме выкачки по имени — она считается отдельно.
6. **Сеть на плате** (`argon boardnet -port COM3`) — то, чего эмулятор не
   отвечает: радио, роутер, скорость. Плата ведётся по серийной консоли, а ПК
   выступает второй стороной: плата скачивает страницу и файл по FTP, потом сама
   поднимает `httpd`, и ПК сравнивает отданное **побайтово** с тем, что скачал
   сам. 14 проверок; числа — в [09-esp32-cyd.md](09-esp32-cyd.md).
7. **Железо** — только спайк S-1 (исполнение из PSRAM) и латентности.

**Числа в звуке проверяются числами.** Слушать полезно, но четыре дефекта
подряд (пила со смещением, FM без боковых, ADSR в 256 раз быстрее, LFO вчетверо
быстрее) прошли мимо ушей и были найдены только измерением. Поэтому
`test_dsp.c` меряет размах и среднее осциллятора, частоту LFO, длительность
сегментов огибающей в миллисекундах и линейность свёртки — а не «звучит ли».

## Что работает

| Подсистема | Где | Состояние |
|---|---|---|
| Слой портирования | [`components/argon_port`](../components/argon_port) | ✅ **в ядре нет ни одного `#include` из ESP-IDF**; контракт — 18 заголовков, реализации `idf/` и `bare/` |
| Загрузка по стадиям | `src/boot/kernel.c` | ✅ незаполненные стадии видны в отчёте `boot` |
| Определение чипа, профили | `src/boot/platform.c` | ✅ `full`/`lite`/`nano` |
| Описание платы | `src/boot/board.c` | ✅ дефолты + `C:\BOARD.CFG` |
| Журнал логов | `src/core/journal.c`, `log.c` | ✅ кольцо 4 КБ, команда `log` |
| Пути, конфиг, разбор команд | `src/core/path.c`, `cfg.c`, `cmdline.c` | ✅ |
| Текстовый экран + ANSI | `src/console/screen.c` | ✅ 80×25 по умолчанию, размер из `[console] cols/rows` (или `auto`) и команды `mode`, отложенный перенос |
| Рендер в VT100 | `src/console/vtout.c` | ✅ по одному на endpoint |
| Разбор ввода терминала | `src/console/vtin.c` | ✅ HID-коды, UTF-8, мышь |
| Кодовая страница | `src/console/codepage.c` | ✅ CP437/866/1251, `chcp`, конвертация на краях |
| Консоль-мультиплексор | `src/console/console.c` | ✅ UART, backpressure и XON/XOFF на вводе |
| VFS | `src/fs/vfs.c` | ✅ монтирование, владельцы, eject |
| RAM-диск `T:` | `src/fs/ramfs.c` | ✅ до 4 МБ в PSRAM (для WAV-захвата) |
| littlefs на flash `C:` | `storage.c` + `joltwallet/littlefs`, `idfvfs` | ✅ `/sys` power-fail-safe; тип `lfs` |
| FAT на flash `C:` | — | ❌ заменён littlefs |
| FAT на SD `A:` | `storage.c` | ✅ SDMMC и SPI, `format a:` |
| Sync папки хоста → `A:` | `tools/mkfatimg.py`, `argon sync` / `run -Share` | ✅ снимок FAT16 в `build\sdcard.img` |
| HostFS → `H:` | `src/fs/hostfs.c`, `tools/hostfsd.py`, `argon run -HostFs` | ✅ чтение/запись/`del`; крупные `copy` на `H:` при живом PADPUSH — TX кусками + пауза push на writable open |
| FatFs BPB subtype | `components/fatfs` + `tools/patch_fatfs_bpb.py` | ✅ FAT12/16/32 по BPB, не только по nclst |
| Шелл | `src/shell/` | ✅ история, перенаправление, 47 команд в таблице (`k_commands[]` в `shell.c`; сеть и радио за `#if`) |
| Загрузчик `.AXE` | `src/loader/`, `tools/mkaxe.py` | ✅ две части; R-1 flash XIP; HMAC в `reserved[6]` (`axesig`, `signaxe.py`) |
| Арена кода | `src/core/arena.c` | ✅ несколько образов в 64 КБ |
| Модель процессов | `src/proc/proc.c` | ✅ до 4 процессов, арена, cwd, учёт ресурсов |
| Потоки приложения | `src/proc/threads.c` | ✅ до 4 на процесс, мьютексы, семафоры, очереди |
| Супервизор | `src/proc/supervisor.c` | ✅ `Ctrl-C`, `Ctrl+\`, сбор завершённых |
| Перехват исключений | `src/proc/fault.c` | ✅ падает приложение, а не система |
| Ресурсный список | `src/core/reslist.c` | ✅ порядок по типам, ловит двойное free |
| Примеры приложений | `apps/hello`, `disk`, `spin`, `leak`, `threads`, `crash` | ✅ проверяют сами себя |
| Файловый менеджер | `apps/fm/` | ✅ **команда `fm`, встроена в ОС**; тот же код собирается и как `.AXE`; операции над [`apps/common/fsops`](../apps/common/fsops/) — общие с графической оболочкой, с копированием и удалением дерева; стенд `apps/fm/check.ps1` |
| Графический FM | `apps/gfxfm/` | ✅ `GFXFM.AXE` — полный паритет клавиш/ops с `fm`, soft gfx (cell UI) |
| Графическая оболочка (Win 3.11) | `apps/desktop/`, план [`plans/desktop.md`](plans/desktop.md), страница [`user/05-desktop.md`](user/05-desktop.md) | 🚧 **все четыре этапа, кроме замера на плате** (`DESKTOP.AXE`): стол, окна с рамкой/заголовком/z-порядком, перетаскивание обводкой, меню, модальные диалоги, значки, диски из `mountinfo`, окно папки со списком и прокруткой, запуск программ (слот сессии — стек процессов), файловые операции над [`apps/common/fsops`](../apps/common/fsops/) c окном прогресса и `Esc`, свойства файла и диска. Перерисовка по повреждениям через таблицу из шести операций; юнит-тесты геометрии и обхода дерева; стенд `apps/desktop/check.ps1` — два снимка, и вне строки состояния разница обязана быть нулевой. `DESKTOP.INI` — стол возвращается таким, каким его оставили (положения значков, открытые каталоги, геометрия окон, цвет, время двойного щелчка); строка состояния в окне папки. Замерено на CYD 9 сентября 2026 (160×120): окно на C:\ за 2 мс, первая отрисовка 17 мс, шаг перетаскивания 17 мс (из них 15 — флаш на панель), полная перерисовка 25 мс; рисование стоит 2 мс, остальное панель. 320×240 не влезает — 153 600 байт против блока в 65 КБ |
| ZIP | `cmd_unzip.c`, `apps/zip/`, `third_party/miniz/` | ✅ **`unzip` builtin** (list/extract, store/deflate via miniz); **`ZIP.AXE`** create/update (store) + list/extract (store; deflate → builtin) |
| Текстовый редактор | `apps/edit/` | ✅ **команда `edit`**, встроена; до 64 КБ / 2048 строк, F2 сохранить |
| Компилятор C → `.AXE` / `.SYS` | `apps/cc/` (`CC.AXE`) | ✅ **Argon CC** (фазы A–E + `.SYS` + g2d): `int`/`char`/указатели, **структуры**, **препроцессор**, **FS/mem/dev**, биты/сдвиги; builtins `ag_gfx_blit_*` (0.17); lib [`g2d`](../apps/cc/lib/g2d); демо [`tile_demo.c`](../apps/cc/examples/tile_demo.c), [`wetspot.c`](../apps/cc/examples/wetspot.c), [`asteroids.c`](../apps/cc/examples/asteroids.c), DX7 nofx, guest `.SYS` [`echo.c`](../apps/cc/examples/echo.c) |
| Реестр устройств | `src/dev/device.c` | ✅ классы, владельцы, эксклюзивный доступ, отзыв при извлечении |
| Устройства как файлы | `src/dev/devfs.c` | ✅ `/dev` — диск `D:`, одна таблица дескрипторов с файлами |
| Встроенные устройства | `src/dev/devices.c`, `storage.c` | ✅ `null zero con flash0 sd0 fb0`, команда `dev` |
| Soft display / `gfx` | `src/dev/display.c`, `draw.c`, `font8x16.c` | ✅ RGB565 front+back (640×400), `double_buf`/`swap`, dirty-rect `flush`, soft-draw + ABI **0.17** chroma blit + **0.24** `AG_GFX_TRANS` / ARGB8888 + **0.25** `text_fit` + **0.26** `blit_scaled`/`tiled`/`poly_fill_tex`, `gfxdump`, `apps/gfxdemo`, `GFXFM`; CC games: [`apps/cc/lib/g2d`](../apps/cc/lib/g2d); QEMU RGB (`argon run -Gfx`). LVGL — не в ядре ([`06-ideas.md`](06-ideas.md) §3.4) |
| Soft input / `joy0` | `src/dev/input.c` | ✅ слой pad: PADPUSH → `/dev/joy0` + `inp->pad`/`btn`/`btnp` (ABI 0.11), снимок 6 байт; `H:\sms.pad` — совместимость |
| Master System `.AXE` | `apps/sms/` (SMS Plus GX, GPLv2+) | ✅ play + `ag_btnp` / PADPUSH; рендер в back-буфер `gfx`; PSG+FM→`/dev/pcm*` или WAV (`ag_fm`); QEMU: `PCMVIRT.SYS` + `pcmplay.py` |
| Mega Drive `.AXE` | `apps/md/` (gwenesis, GPLv3 + Musashi MIT) | ⚠ play+sound: 68000 + VDP + Z80 + PSG + YM→`ag_fm`; звук в `/dev/pcmnull`/`pcmvirt` или WAV; I2S — будущий `.SYS`. Лицензии — [`apps/md/README.md`](../apps/md/README.md) |
| Doom `.AXE` | `apps/doom/` (doomgeneric / Chocolate, GPLv2+) | ⚠ картинка + OPL3/SFX через `pcmvirt`; **ввод в QEMU сломан** (клавиши теряются, с титра не стартует). Отложено: [`plans/doom-input.md`](plans/doom-input.md) |
| Общий DSP-слой | [`apps/common/dsp`](../apps/common/dsp) | ✅ fixed-point без `libm`: sin-LUT, нота→Гц, задержка, кража голосов, ADSR (сегменты в мс), LFO, RNG. Покрыт [`host-tests/test_dsp.c`](../host-tests/test_dsp.c) |
| Синтезатор VA / FM | [`apps/common/synth`](../apps/common/synth), `apps/synth/` (`SYNTH.AXE`) | ✅ 2 осц (PolyBLEP saw/sqr, tri/noise/sin, wavetable) → SVF → FET/tube → ADSR; N-оп FM с матрицей маршрутизации; матрица модуляции LFO/EG/vel/note/MW/AT → любой параметр |
| Сэмплер / ROM-плеер | [`apps/common/smp`](../apps/common/smp) | ✅ одна зона, интерполяция, петля, ADSR, 8 голосов; пресеты синтетические (орган/пианино/бас) |
| PCM-выход приложений | [`apps/common/pcm`](../apps/common/pcm) | ✅ `pcmvirt`/`pcmmix`/`pcmnull` или запись в `*.wav`, пейсинг по стенным часам |
| Свёртка с IR | [`apps/common/ir`](../apps/common/ir), `apps/irfx/` (`IRFX.AXE`) | ✅ partitioned FFT до 500 мс; IR нормируется по энергии, каждый масштаб в тракте следует за сигналом вместо фиксированного сдвига. **Расхождение с двойной точностью −66 дБ на 0 дБFS**, худший отсчёт 9 LSB (`tools/ir_check.c`). ✅ вещественное преобразование вместо комплексного: **кабинет 981 инстр./отсчёт — 9.0% ядра на 22.05 кГц** против 2453 (`cktbench fx`, `-icount`); предел длины — 1 с (87 частей, 348 КБ, 50% ядра), и упирается он в процессор, а не в память. Разбор — [`08-circuit-simulation.md`](08-circuit-simulation.md) |
| Симуляция аналоговых цепей | [`apps/common/ckt`](../apps/common/ckt), `apps/cktbench/` (`CKTBENCH.AXE`) | ✅ MNA + Ньютон: R, C, источники, диоды, JFET (Шихман-Ходжес), триод (Корен); `ag_mathf` — exp/log/pow/sqrt без libm. ⚠ прямое решение в реальном времени **не идёт** (один 12AX7 в перегрузе — 331% ядра). ✅ **`ag_stage` — та же цепь с заранее решённой нелинейностью, в сорок раз дешевле; расхождение с решателем −63…−104 дБ.** ✅ ADAA: ×2 с ним чище, чем ×4 без. **Три лампы с темброблоком при ×2 — 87% одного ядра**, продукты −40.8 дБ. Числа — [`08-circuit-simulation.md`](08-circuit-simulation.md) |
| Ламповый усилитель одномерной моделью | [`apps/common/tube`](../apps/common/tube), `apps/tubebench/` (`TUBEBENCH.AXE`) | ✅ Две лампы JCM800 как **статические кривые, запечённые тем же решателем** (одна модель Корена в дереве), вся реактивная обвязка вынесена в биквады: связь 1.58/142/7.2 Гц, катодные полки 285 Гц −2.9/−4.0 дБ **выведены из двух малосигнальных усилений**, а не подобраны. Ось подбирается **по сигналу, а не по кривой** — иначе 48 дБ мимо (сеточный ток тянет кривую до 2 кВ). **2048 точки, 32 КБ на пару, −83.8 дБ в 1.5–5 кГц** против −57.7 дБ и 139 КБ у двухосной. **×4 с ADAA — 32.7% ядра (41.7% с кабинетом), продукты −68 дБ под 5 кГц; ×2 с ADAA — 17.3% и −56 дБ** (`tubebench bench`, `-icount`). Тишина бит-точная. Гейн: **1.0 — верх диапазона, 0.5 рабочая точка**; `g12` (межкаскадный делитель) −9 дБ даёт +7 дБ к полу шума и снимает вторую лампу с асимптоты (66 176 → 1670 выходов за край). ✅ **блокинг-искажение**: сеточный ток заряжает разделительный конденсатор, окно второй лампы сдвигается −18.2…+25.9 → −24.1…+11.3 В, разница с выключенным всего 1.6 дБ ниже сигнала в 1.5–5 кГц, ценой 2.9 дБ продуктов. Линейная часть того же конденсатора остаётся биквадом — иначе он моделируется дважды. ✅ **спектр сведён с NAM-эталоном Marshall**: третьоктавные полосы 63 Гц–6.3 кГц, **1.29 дБ rms против 8.11** у неподобранного, худшая 2.7 дБ (`tube_render fit`). Два банка: перед лампами 200–1600 Гц (там решается, *что* исказится), после — верх и низ, потому что ограничитель слабый верх не пропускает, а лишний бас в сетку душит середину (10 нФ вместо 2.2 попадает в низ и стоит 4.7 дБ на 315 Гц). Кабинет решает больше эквалайзера: через Vox тот же подбор даёт только 2.53 дБ. ✅ **блэндинг**: вторая кривая на каскад (встречные диоды), запечённая тем же решателем по той же оси и нормированная по малосигнальному наклону; смешивание — пересборка таблицы, ~50 тыс. инстр. (0.2 мс), в горячем пути ноль. Все три таблицы линейны по смеси, поэтому первообразная остаётся точным интегралом и ADAA работает; блокинг гаснет вместе с примесью, потому что у диода нет сетки. ✅ **каскадов 1–4**, каскад без таблицы = усиление плюс фильтры; ✅ **пресет как байты**: печёт хост, загрузка без решателя вообще, круг сохранение-загрузка сходится по отсчётам, таблицы не зависят от частоты дискретизации, ось морозится с запасом 2× по гейну (−69.2 дБ против −36.6 при 1×). ⚠ запекание 212 M инстр. (882 мс) — но теперь только на хосте; ⚠ нет просадки питания и оконечника. **Грабли: `wet = 100/128` по умолчанию пускал 22% сухого мимо кабинета — измерено, у того же импульса 8 кГц идёт −16.5 дБ на полной мокроте против −1.6 дБ на 100, то есть утечка не ослабляла спад, а стирала его; звучало как постоянное шипение. Исправлено: `ag_ir_load` и пресеты 3/4 сами ставят `AG_IR_WET_MAX`, пресеты 0–2 — `AG_IR_WET_REVERB`, микс задаётся вызовом `ag_ir_set_wet` после загрузки. ⚠ остаётся в силе: пресеты 3/4 в `ag_ir` кабинетом не являются — выше 2.5 кГц спада нет вовсе, судить искажение через них нельзя; настоящий импульс лежит в `assets/audio/guitar-di/1 Vox AC30 1.wav`.** ✅ **живые ручки**: `tools/tube_live.c` (Windows, winmm) гоняет запись по кругу через ту же цепь и кабинет, ручки с клавиатуры, блэнд пересобирает таблицу на границе блока (0.2 мс из 11.6). Ручки идут через `ag_amp_set_knobs`, который меняет коэффициенты и **не сбрасывает состояние**: иначе каждое нажатие обнуляло бы восемь биквадов и разряжало разделительный конденсатор — щелчок и скачок смещения на сто миллисекунд; проверено побитово. Передискретизация и число каскадов честно разрывны и идут через `ag_amp_set_voicing`; все четыре цепи и вторые кривые для блэнда пекутся на старте, чтобы ни одно нажатие не ждало решателя. **Грабли № 2 того же сорта: константное ослабление перед int16-свёрткой** — на драйве 0.1 сигнал входит в кабинет на 25 дБ тише (11 бит), и блочное квантование `ag_ir` слышно как зуд почти постоянной частоты; измерено `ir_check`: ошибка/сигнал −66.5 дБ на полной шкале, −49 на −25 дБ и −13 в затуханиях нот. Исправлено адаптивным стейджингом: вход масштабируется до заполнения 16 бит и делится обратно на выходе (рост 0.35 дБ/блок из-за хвоста свёртки, падение мгновенное, потолок +30 дБ); диодная кривая блэнда дополнительно нормируется по RMS на оси (один наклон ровнял тихое и оставлял громкое тише); в `tube_live` есть полосовой соло-фильтр треть-октавы для поиска шумов на слух. Соло-фильтр показал, что стейджинг упёрся в потолок самой int16-арифметики: ошибка к сигналу −24 дБ в 0.5–2 кГц, −12 в 4–6 кГц и больше самого сигнала выше 6 кГц (спектр ошибки плоский, а динамик там падает обрывом). В `tube_live` добавлена float-свёртка кабинета (4410 отводов в лоб, ~80 мс на секунду звука, замер печатается на старте) как путь по умолчанию; `c` циклит float / int16 / выкл — разница между первыми двумя и есть шум свёртки чипа на слух. ⚠ для самого чипа вопрос открыт: int16-свёртке `ag_ir` над кабинетом этого не обойти без повышения разрядности аккумуляции или шейпинга ошибки. ⚠ **Зуд на верхах не найден, но сужен до 8 дБ.** Исключены замерами: таблица (2048 против 65536 — −68 дБ, режим `buzz`), ADAA, алиасинг (os 1/4/8 совпадают), блокинг, живой тракт (`--dump` побитово равен оффлайн-рендеру), int16-свёртка, усиление банков (гаснет вместе с сигналом), резонансная форма темброблока (полка `ag_biq_hshelf1` на слух не меняет ничего), g12 и разгон V1a. **Главный урок — диагностика врала:** рекордер писал после соло-фильтра, и «фиксированные 3.9 кГц на любой ноте» оказались центром самого соло (в файлах 3.5–5 кГц на −1.2 дБ, всё ниже 1.5 кГц на −20…−34 дБ); теперь запись берётся до соло и предупреждает об этом. Сравнение с NAM-капчей на том же клипе (драйв 0.5, один кабинет): размах огибающей ВЧ **+10.3 дБ у эталона против +10.8 у нас** — вспышки по фронтам ограничения есть и у настоящего аппарата, а пик 3.2 кГц — это динамик (без кабинета узкость ниже на 22 дБ). Остаётся **8 дБ узкости на 3.2 кГц** — разница спектральная, не механизм, поэтому её не сдвинуло ничто из перечисленного. Механизм горбов установлен: безынерционная кривая рождает все гармоники выше 2 кГц в одном месте (фронт ограничения V2, ~0.3 мс дважды за цикл), с фазами, сцепленными на фронте, а между фронтами — математическая тишина. Замер провалов (3.5–5 кГц, относительно тела сигнала): эталон −31 дБ, сухой DI −35, мы −55 — то есть дело не в высоте горбов, а в неестественно идеальной тишине между ними. Восемь попыток были построены, измерены и **удалены по решению автора**: дисперсия фазовыми звеньями, многополосный выравниватель огибающей (доходил до уровня сухого DI), динамический полюс Миллера, колено на входе, ползучий предел, шум тракта и острый нотч (во всех трёх версиях — следящей по уровню, статической и сверхострой) и дисперсия вторично, по просьбе — она двигала метрику сильнее всего остального (+13.6 → +10.1 дБ при эталоне +8.8), а на слух почти не заметна — самое явное расхождение метрики со слухом за весь поиск. Цель — не подавить модуляцию, а устранить её рождение; каждый отрицательный результат с цифрами — в README модуля. Порядок для **нового усилителя** записан там же, в разделе «Authoring a preset for another amplifier»: эталонный материал и его рецепт, кабинет первым, спецификация из схемы, подбор голоса, мастер после подбора, запекание осей на драйве 2.0. Старая версия про «25 дБ на интермодуляции» не подтвердилась: ручка банка гасит зуд вместе с сигналом, то есть дело в форме фильтра, а не в его усилении. Жалоба на неприятный широкополосный зуд 2–11 кГц на любом гейне. Измерено на сигнале, жёстко обрезанном на 1 кГц (−148 дБ в судимой полосе), против `ckt_exact`: в 3.5–6 кГц точный решатель даёт −47.7 дБ, наш тракт без банков −48.9 (то есть таблицы, ADAA и передискретизация верны с точностью до децибела), предбанк добавляет 8 дБ, темброблок — ещё 17: итого **25 дБ усиления на продуктах интермодуляции**. Подбор ровнял третьоктавную *магнитуду* и не видит разницы между плотным гармоническим рядом и редкой грязью: у эталона эта энергия есть потому, что её делают три лампы и оконечник, а наш тракт вывел то же число, усилив то, что сделали две. Следует: пассивный темброблок Marshall **не умеет поднимать**, и переподбор надо ограничить единичным усилением, а энергию в 3–6 кГц брать из **третьего каскада**, а не из подъёма фильтром. Ручка для слушания — `,`/`.` в `tube_live` (оба банка одновременно, 0–1.5), в `tube_render` есть `AG_NO_TONE` / `AG_NO_VOICE` / `AG_TOP_HZ` / `AG_MID_DB` и режим `buzz` (ошибка интерполяции таблицы: 2048 против 65536 — −68 дБ в 3.5–6 кГц, то есть не она). ✅ **NAM-захваты играет само дерево**: `tools/nam.c` + `nam_run`, порт того же WaveNet в C; проверка — **побитовое совпадение** с рендерами Go-версии на трёх прогонах, включая 31-секундный (двадцать перемоток буферов истории). `host-tests/test_nam.c` держит это постоянно и сам пропускается без чужих файлов; зависимости от Go-бинарника в `%TEMP%` больше нет. ✅ **тонматчер одной командой** — `argon match capture.nam -Model bogner`: дубль вверх на частоту захвата, захват, вниз, две подгонки, вердикт. Всё в памяти и во float (`convolve_f`, точная свёртка по FFT), без 16-битных файлов в середине. Наличие динамика в захвате **измеряется** по спаду на 6.3 кГц (`Mars Gain 8` −16.1 дБ, `Bogner` −0.4, `5150red` +0.2 — **два из трёх захватов без кабинета**); кабинет либо снимается с самого захвата, либо его нет ни на одной стороне. ✅ **порядок подгонки: сначала фронт, по динамике, а не по спектру.** Предкаскадные фильтры решают, чем перегруз является, задний банк — тембр; подгонка обоих сразу давала заднему впитать ошибку, которая должна двигать передний. Проход 1 — один параметр (подъём 100–800 Гц) против одного числа (компрессия захвата), чувствительность 0.18 дБ/дБ **измерена** режимом `sens`; по спектру этот проход расходился (3.66 → 6.19 дБ rms). **Это опрокинуло прежний вывод про `jcm800`:** его 4.5 дБ недобора компрессии были записаны как «нет оконечника, фильтром не лечится», а +4 дБ подъёма перед лампами дают **12.2 → 16.0 против 16.1 у захвата**. Осталось зерно: тракт делает −11.8 дБ выше 2 кГц против −16.4 у усилителя (на 4.6 дБ грубее), и подъём этого не двигает. У `slo` наоборот: упёрся в предел ±6 дБ, не добрав 0.6 дБ компрессии, а зерна у него на 5.4 дБ **меньше**, чем у захвата. ✅ **`sens`** — что делает каждая полоса перед каскадами: низ спереди гармоник не добавляет (−0.3…−0.7 дБ выше 2 кГц), нижняя середина это регулятор раскачки (+0.4…+0.5 дБ компрессии), зерно берётся из 1.6–3.15 кГц спереди (+0.9 дБ), 5 кГц спереди почти инертен; контрольная строка (те же +6 дБ **после** каскадов) не двигает ни одно число характера — то есть фильтр после это чистый тембр. ✅ **два подогнанных режима пост-коррекции и A/B между ними одной клавишей `z`**: режим 1 — банк остаётся, импульс подогнан на остаток (`ir_<model>_bank.wav`); режим 2 — банк в нуле, импульс несёт всё (`ir_<model>_fitted.wav`). Против усилителя **0.70/0.55/0.34 дБ (режим 1) и 0.71/0.61/0.67 (режим 2) против 1.48/2.67/0.83 у того, что едет сейчас**: подгонка импульса даёт 0.8–2.1 дБ, а режимы отличаются друг от друга на 0.01–0.33 дБ (оба линейны и стоят после последней анодной). На отложенном дубле (`AG_EVAL_DI`) запас режимов друг над другом ≤0.21 дБ, и всё хуже на 1–3 дБ — больше, чем разница между ними. Уровни двух сторон сведены **по звуку**, а не по энергии импульсов (была разница −2.7/−5.7/+2.3 дБ, слышалось как «тише и тусклее»; теперь ≤0.36 дБ). ⚠ **Две записанные здесь ранее вещи оказались неверны и исправлены:** «`ag_ir` смещает спектр на 1.18 дБ rms» — нет, **дельта и пинк-шум проходят движок с 0.00 дБ по всем полосам**; те 1.18 дБ появляются только на материале с затуханиями и это шум квантования от фиксированного стейджинга входа в `run_cab` (следящий стейджинг сделал измерение втрое хуже — подъём масштаба ломает хвост свёртки, где живёт низ, — и был откачен). И «в режиме 2 нет тон-контроля после каскадов» — нет, пассивный темброблок это схема, он на месте в обоих режимах. ✅ **пробник характера перегруза**: `harm` — синус-бёрсты (6 нот × 3 уровня), двухтоны и шумовой всплеск, анализ Гёрцелем по бинам, против того же захвата. Гитарный дубль не отделяет «сделано» от «пропущено», синус отделяет. Нашлось: **наш тракт на 18–20 дБ асимметричнее** моделируемых аппаратов (H2 у `bogner` на 147 и 349 Гц) и **на 7–15 дБ сильнее выпрямляет огибающую** двойного стопа; H3 совпадает. Один механизм с двух сторон — каскад, ограничивающий одну полуволну сильнее, плюс сеточный заряд за огибающей. Это про схему, не про фильтры. Два условия пробника были сперва нарушены и исправлены: один динамик на обеих сторонах (было +4.1 против −20.5 дБ на 6.3 кГц) и один и тот же *фильтр* (подогнанный импульс против сырого кабинета сдвигал продукт 49 Гц на 11 дБ и переворачивал знак). ✅ **третье число характера — субсоническая интермодуляция** (49 Гц от 147+196), то есть «пердёж»: ни один одиночный тон и ни один третьоктавный спектр его не видит. Контроль: фильтр **после** каскадов не двигает ни одно из трёх чисел (0.0). ✅ **`cfg.couple_mul`** — рез баса перед каждым каскадом (1.0 = схема, 2.0 = вдвое меньшая ёмкость; в горячем пути ноль, читается назад как номинал). **Измерение опровергло ожидание:** рез на разделительных ёмкостях делает пердёж *хуже* (+1.3/+1.9 дБ на каскадах 2/3, +4.2 распределённо), каскад 1 не делает ничего (угол 1.6 Гц), а работающий рычаг — **уровень** баса перед лампами (+8.1 дБ IMD на +6 дБ в 200 Гц). Значит «резать бас перед ограничителем» живёт в предкаскадном банке. ✅ **проход 1 стал двухпараметрическим** (подъём 100–800 против компрессии, рез 100–200 против IMD, наклоны из `sens`, шаг задавлен): у jcm800 компрессия 12.2 → **16.0** против 16.1 у захвата, у bogner 18.6 → 18.2 против 18.2, у slo упёрся в предел. Оба решения двигают середину и не трогают бас — цикл сам нашёл, что компрессия покупается в нижней середине, а басом она стоит интермодуляции. ✅ **тот дефект найден и в основном исправлен**: у `slo` продукт 49 Гц был на 0.8 дБ ниже тонов против 17.8 у захвата. Механизм локализован замером: **выключение блокинга снимает 9.1 дБ**, остальное даёт четвёртый каскад, — то есть виноват сеточный заряд. Причина: у `slo` все четыре связи 22 нФ, то есть бас перед горячими каскадами не режется вовсе, а τ = C·Rgrid = 22 мс совпадает с периодом тех самых 49 Гц. Плюс нашлась моя же полуправка: `couple_mul` сперва умножал только угол ФВЧ и не трогал τ (фильтр, прикидывающийся компонентом); теперь он делит `spec.ccouple`, и всё — угол, блокинг, запекание — идёт от одного значения. **Проход 0** пробует ×1/×2/×4/×8 (22 → 2.8 нФ) на каскадах после первого и режет **только если мы хуже аппарата и только до его цифры** (первая версия правила минимизировала и обрушила компрессию jcm800 с 12.2 до 7.0 дБ — рез, которого никто не просил). Итог: `slo` **−0.8 → −7.9 дБ** после прохода 0 и **−13.4** после прохода 1, компрессия ровно 18.9 против 18.9 у захвата; `jcm800` и `bogner` не тронуты (у них продукт и так на 3 и 7 дБ ниже аппаратного). Остаток 4.4 дБ. ✅ **`irnoise` решил вопрос режимов**: оба режима в точной арифметике равны, но через int16-свёртку чипа **режим 1 чище на всех моделях на 2.5–7.7 дБ** (−54.2/−26.1/−24.5 против −46.5/−22.2/−22.0) — импульс, которому дали нести 19 дБ наклона, тратит на него разрядность. Вывод по тембру («режим 2») опрокинут арифметикой: **шипить режим 1**. Абсолютные −22…−26 дБ — не импульсы (движок воспроизводит каждый с 0.00 дБ на дельте), а известное поведение `ag_ir` на затуханиях (−13 дБ в хвостах нот). ✅ **применено к `slo`**: `couple_mul` 8× на каскадах 2–4 (22 → 2.8 нФ) плюс переподобранные банки — «как едет» 0.83 → 4.17 → **1.15 дБ** (первое число со старым банком, второе с новым чипом и старым банком, третье после переподбора), компрессия **19.1 против 18.9** у захвата, пердёж −13.4 против −0.8. Форма нового ответа сама себя подтверждает: у прежнего банка было −9.5 дБ на 100 Гц позади ламп и −6.7 впереди на 400 — фильтры убирали низ, которого не должно было быть; теперь +1.1 и −0.7, оба банка в пределах ±11 дБ. Версия пресета поднята до 2 (в конфиге появилось поле). ✅ **`knee`** — развёртка по уровню, идея автора: на каждом уровне нота гонится через 1, 2, 3, 4 каскада, рядом столбец захвата. **Захват не зависит от уровня** (−18 дБ H3 на 34 дБ диапазона, разброс 0.7), **у нас ходит на 12 дБ**; **две лампы из четырёх не работают** (−64 и −54 дБ); **последний каскад уже насыщен на самом тихом уровне**. То есть распределение усиления по каскадам неверно — это `gain[]`/`g12`/делители, а не фильтр. **Дешёвый вариант проверен и отвергнут тестом:** аттенюатор перед последним каскадом (gain[3] = 0.35 или 0.5) действительно сдвигает колено на 0.05–0.12 и режет разброс с 12 дБ до 6, но `test_model_gain_order` падает — малосигнальное усиление четырёхкаскадного лида уходит **ниже** трёхкаскадного кранча, а это ровно та ошибка, ради которой тест и написан. Значит работу должны делать ранние каскады, и это нетлист (аноды, делители перед холодным клиппером), а не константа усиления. Заодно `master` у slo пошёл за цепью пятый раз: 1/84 давал пик **+2.8 dBFS** после реза баса и переподбора банков, стало 1/130. Пресеты всех трёх моделей перегенерированы, и в `build/listen` лежат пары `ab_<model>_ours.wav` / `ab_<model>_amp.wav` — наш итог и захват на одном дубле, уровни сведены в пределах 0.6 дБ, с запасом по пику. ✅ **тонблок перед каждым каскадом и лестница по уровню** (`cfg.voice[stage][]` + `cfg.vtrim[stage]`, режим `ladder`): был один банк на всю цепь, а такой банк не умеет резать бас по чуть-чуть перед **каждым** ограничителем — и выбирал не то: у кранча стояло **+5.68 дБ на 200 Гц перед тремя клипующими лампами**. Каскад подгоняется на том уровне, где он начинает работать (12 уровней 0.004–1.0, цепь разбирается на 1/2/3/4 каскада), от последнего к первому; крутилки и номиналы не двигаются. Метрика — 25 гармонических отношений (H2…H6 пяти нот) **с делением на собственную линейную АЧХ каждой стороны** (чирп на уровне, где никто не искажает; без этого провал тон-стека на 400 Гц читался как 17 дБ искажений, которых нет), спектр 63 Гц–5 кГц вполвеса и **49 Гц продукт двойного стопа односторонне** — без последнего лестница загнала четырёхкаскадную модель в блокинг и доложила, что гармоники совпали в децибел, а пердеж вырос на 12 дБ. Вместо отвергнутого аттенюатора `gain[3]` — **наклон с суммой ноль**: те же децибелы снимаются с конца цепи и отдаются началу, малосигнальное усиление не меняется и `test_model_gain_order` проходит. Правило по разделительным: **ни один срез не выше октавы под самой низкой нотой** (41 Гц) — ×8 давал у slo срезы 57/158/56 Гц, и 158 Гц срезал основной тон нижнего Ми, пропуская его вторую гармонику: H2 читалась **−2.5 дБ** против −28.8 у захвата — тридцать децибел асимметрии, сделанной фильтром и списанной на лампы. Стало ×4/×2/×4 = 28/39/28 Гц. Итог (режим 1, дубль подгонки / **30.9 с отложенный**): jcm800 0.70→0.29 / 3.51→**1.18**, bogner 0.54→0.27 / 1.97→**0.73**, slo 0.50→0.62 / 2.33→2.33; компрессия у всех трёх в пределах 0.2 дБ от захвата. По характеру, что действительно изменилось: у slo гармоники стали **ровными по уровню** (−18.7 против −18.2 у захвата, был разброс 12 дБ), H3 на 147 Гц с 20 дБ до 1, H2 от 110 до 262 Гц с 10–20 дБ до 1–6; у bogner лишние гармоники с 8.7 дБ до 4.4 и без зависимости от уровня. ⚠ **А что не изменилось — это то, на что жаловались:** H2 нижнего Ми у slo −9.5 дБ против −37.7 у захвата (было −7), 49 Гц продукт 10.9 дБ выше захвата (было 12.4), H2 у bogner 10–18 дБ выше (было 15–25). Причём по 49 Гц **дырка в самом инструменте**: член в цели поделён на собственную АЧХ, как гармонические отношения, а для пердежа так нельзя — на слух важен абсолютный уровень продукта. С коррекцией член читает 2.6–4.6 дБ и подгонка считает, что закрыла вопрос; без коррекции `harm` мерит 10.9. Выходной банк и импульс тоже мимо: они подгоняются по третьоктавам от 63 Гц, а 49 Гц ниже первой. Лечится односторонним членом без коррекции плюс полосой ниже 63 Гц в выходной подгонке. У jcm800 своя история: наша H3 на **15–20 дБ ниже** захвата на каждой ноте, потому что захват снят с гейном 8, а цепь стоит на `drive` 0.5 = полдень — это решение про то, чем модель является, а не подгонка. Цена на чипе реальная: `tubebench` под `-icount`, ×4 с ADAA — **jcm800 45.3% ядра (было 32.7), slo 77.1%** (86% с кабинетом); из них около 24 пунктов — двадцать одна секция на четверной частоте. Семь из 28 полос slo меньше 1 дБ, двенадцать меньше 1.5 — порог в `design()` вернёт 6–10 пунктов, и это решение про слышимость, а не измерение. ⚠ на длинном отложенном дубле цепь уже настолько близка, что **сырой кабинет обгоняет подогнанный IR** у slo (2.30 против 2.33) — переобучение теперь в импульсе, а не в цепи. ✅ **`selftest`**: FFT-свёртка `convolve_f`, на которой стоят все вердикты, сверена с прямой свёрткой в double — −152 дБ, пол float. Разбор — [`apps/common/tube/README.md`](../apps/common/tube/README.md) |
| Владение пинами | `src/dev/ioclaim.c` | ✅ пины системы, занятые пины, возврат за упавшим процессом |
| Энергосбережение | `src/core/power.c`, `powerctl.c`, порт `idf/src/power_hw.c` | ✅ команда **`power`**: частота (`full`/`eco`/`doze`, шаги 240/160/80), экран (софтовый буфер + текстовая панель + `AG_IOC_DISPLAY_BACKLIGHT` драйверу), **оповещение приложений** — `api->power` (**ABI 0.35-0.37**), **ступень `cruise` 160 МГц по молчанию** (APB на PLL-шагах не двигается, поэтому там молчание = согласие; девять realtime-образов в дереве объявляют обратное первой строкой), и **таймер простоя** (`power auto`, `[power]` в `SYSTEM.CFG`). Два вида перехода: автомат совещательный (никого не снимает, не понижает частоту под `FIT_FULL_ONLY`), команда человека — приказ: **приложение, не подтвердившее, что рассчитано на режим, снимается** с записью причины. Встроенные (`fm`, `edit`) система подписывает сама, загружающийся процесс не трогают. 22 теста на хосте, девять сценариев с `PWR.AXE` в QEMU. ✅ **на плате 22 августа 2026**: частота реально переключается (`cruise` 160 / `eco` 80 / `full` 240 живым чтением с чипа), консоль на всех трёх ступенях цела, подсветкой управляет драйвер панели, объявление приложения поднимает частоту мгновенно, молчащее снимается по `power eco`. `CONFIG_ARGON_ENABLE_PM=y` теперь штатно в `sdkconfig.defaults.esp32`; в образ S3 он по-прежнему не влезает (переполнение `dram0_0_seg` ~на 1 КБ). Плата нашла два дефекта: **шаг 40 МГц убивает консоль** (APB уходит за процессором — ступень убрана из порта) и **переполнение стека супервизора** на первом же снятии приложения (4 → 6 КБ плюс сторож запаса). Осталось: миллиамперы амперметром. Разбор — [11-power.md](11-power.md) |
| Железо напрямую | `src/dev/io.c` | ✅ GPIO, ISR, I2C, SPI, UART, PWM; команда `io`, скан I2C |
| Сеть: сокеты и имена | `src/net/net.c`, порт `idf/src/net_hw.c` | ✅ TCP плюс `net->resolve` (**ABI 0.33**): имя в адрес, дотированный квартет читается без резолвера. OpenEth в QEMU, Wi-Fi на плате — выше порта разницы нет |
| Скачать, отдать, передать | `src/net/httpc.c`, `apps/httpd/httpd.c` (HTTPD.AXE), `ftpc.c`, `src/shell/cmd_net.c` | ✅ команды **`net` / `wget` / `ftp`** и загружаемый файловый сервер **HTTPD.AXE** (httpd вынесен из ядра; `run a:\httpd.axe`): HTTP-клиент (chunked, тело без длины, перенаправления, `Ctrl+C`), HTTP-сервер каталога (GET/HEAD, листинг, `403` на выход из корня; с `/w` — **приём файлов из браузера** обычной формой multipart, потоком, и удаление), FTP-клиент (пассивный, двоичный, `ls/cd/get/put/md/rd/del`). **TLS нет** — `https://` отвергается разбором URL, а не молча скачивается по 80-му. Текст протоколов отделён от сокетов и покрыт 181 проверкой на хосте; в QEMU — 43 (`argon nettest`), **на плате по Wi-Fi — 14** (`argon boardnet`): 725 КБ на карту за 23 с, `httpd` отдаёт байт в байт. Разбор и цена — [10-network.md](10-network.md) |
| Радио: Wi-Fi обе стороны | порт `idf/src/wifi_hw.c`, `net/espnow.c`, `net/wifimon.c` | ✅ на плате (перепроверено 22 авг на сборке `0c1f424`): **STA + исходящий TCP** — `boardnet` 13/0 (DHCP, DNS, HTTP, FTP, httpd байт-в-байт); **SoftAP** (`wifi ap`, `[ap]` автозапуск) — **видна и подключается с iPhone, STA+AP вместе**; **ESP-NOW** (`espnow send` → `sent 12 bytes`); **monitor** — `mon` 880 кадров за окно; `mon beacon ArgonGhost` **поймана Windows-ноутбуком** (`02:00:00:32:73:53`, forced `WlanScan` — `netsh` отдаёт кэш; iOS призрак фильтрует). За флагами `ARGON_NET_WIFI_AP/ESPNOW/WIFI_MON`. **Найдено 22 авг: ESP-NOW-tx и сырая инъекция идут через интерфейс станции — при поднятой SoftAP отвечают `invalid interface 0`/I/O error; поднимать при станции/idle, не при точке.** |
| Радио: BLE обе стороны | порт `idf/src/bt_hw.c`, контракт `port/ble.h` | ✅ на плате (21 авг; наблюдатель перепроверен 22 авг — 16 соседей): HID-клавиатура была; **наблюдатель** (`ble scan`), **общий GATT-клиент** (`ble connect/read/write`), **peripheral + BLE-MIDI** (`ble adv`), **`MIDIPAD.AXE`** играет ноты на телефоне касанием. HID-хост ленивый (в `bt open`). Флаги `ARGON_BLE_CENTRAL/PERIPHERAL` |
| Радио приложениям (ABI 0.38) | `src/loader/api.c`, `sdk/include/argon/*.h` | ✅ **`api->ble` дорос** до общего центрального (scan/connect/discover/services/chars/read/write) и общего peripheral (adv_start/set_read/last_write/status) поверх уже проверенного порта; **новый `api->wifimon`** (capture/channel/filter/recv/tx_raw/counters) — `NULL` без `CONFIG_ARGON_NET_WIFI_MON` (по умолчанию выкл). Append-only, **ABI 0.38**. Собрано+слинковано под esp32 (оба радио+MON) и проверено на плате, что `.AXE` доходит до порта через таблицу. **Wi-Fi scan/connect/SoftAP и ESP-NOW осознанно оставлены шелл-only** (линк поднимает система; приложению уже есть TCP+DNS+адрес). **Радио-приложение на CYD не помещается**: при поднятом Wi-Fi свободно ~15 КБ, при BLE ~5 КБ — стек+арена `.AXE` рядом с радио в 197 КБ без PSRAM не влезают; это для S3/PSRAM, не для nano (тот же потолок, что у SMS/Doom) |
| Таблица ABI | `src/loader/api.c` | ⚠ **0.38**: `inp->btnp`, `gfx` draw/present, `net` TCP+`resolve` (0.33), **`api->ble`** — BLE-MIDI (0.34), **`api->power`** (0.35), снятие неподтвердившихся (0.36), ступень `cruise` (0.37), **общий BLE central+peripheral и `api->wifimon` monitor/raw-tx (0.38; оба под своими `CONFIG_*`, `NULL` где роль не собрана)** |
| Загрузчик `.SYS` | `src/loader/module.c` | ✅ `ag_driver_init`, резидент, `drv`, стадия `modules`, пример `apps/echo` |
| Probe I2C | `src/loader/probe.c` | ✅ `modules.probe`, `drv probe`, `ag_probe_hint`, пример `apps/whoami` |

Загрузка в QEMU, измерено 16 августа 2026 (`argon test -Sd ver mem boot`):

| Стадия | С картой | Без карты |
|---|---|---|
| `devices` | 221 мс | 210 мс |
| `media` (SD + проба HostFS) | 160 мс | ~100 мс |
| `storage` | 43 мс | 120 мс |
| `console` | 19 мс | 21 мс |
| **всего** | **467 мс** | ~390 мс |

Цель <400 мс: почти. Разброс дают `storage` и `media`: свежеснятый образ flash
монтируется дольше уже использованного (грабли 18), так что сравнивать имеет
смысл этапы, а не общее число. Самая дорогая стадия — `devices`, и она пока не
разобрана по частям.

**Загрузка держалась на 3.4 с и никто не заметил** — стадия `media` ждала
хост-хелпера HostFS три секунды, а на плате его не бывает никогда. Разбор —
грабли 29.

Ядро занимает **70 КБ** internal SRAM (экран `mem`: «70 KB used by the
system»), прошивка — **1 000 768 Б** для профиля `esp32s3` и **1 737 760 Б**
для `esp32s3-board` (30 августа 2026, коммит `3d66106`; полный разбор — ниже,
«Откуда взялся мегабайт»). Что почём по памяти, измерено сборками соседних
коммитов на этой же машине:

| Что добавилось | internal SRAM | образ |
|---|---|---|
| было (процессы, консоль, ФС) | 41 КБ | 432 КБ |
| модель устройств | +1 КБ | +6 КБ |
| `io`: пины и шины | **+0 КБ** | +35 КБ |

Ноль во второй строке не опечатка: **при загрузке ни одна шина не поднимается**.
Драйверы I2C, SPI и LEDC стоят места во flash, но не занимают ни байта
оперативной памяти, пока ими не воспользуются, — и плата, у которой в
`BOARD.CFG` пины прописаны неверно, не начинает их дёргать до того, как
кто-нибудь посмотрит на консоль.

Память приложения (измерено в QEMU, `mem` и карта линковки):

| Что | Сколько | Где |
|---|---|---|
| код | потолок `CONFIG_ARGON_APP_ARENA_KB` (4..192, S3: 192); usable клипает `[memory] app_arena_kb=` | арена в D/IRAM |
| константы `.rodata` | вся свободная PSRAM; в арене только то, что помечено `AG_HOT_RODATA` | окно данных PSRAM |
| `.data` + `.bss` | вся свободная PSRAM, мегабайты | окно данных PSRAM |
| куча `ag_mem.alloc` | **арена процесса, 1 МБ** по умолчанию (`CONFIG_ARGON_APP_HEAP_KB`) или из манифеста | PSRAM |
| стек | 16 КБ по умолчанию, или из манифеста | internal SRAM, на время запуска |

Внутренней кучи после этого остаётся **258 КБ свободной** из 293 КБ.

Куча приложения — это его собственная арена, а не общая: при завершении процесса
она возвращается целиком одной операцией, и приложение не фрагментирует кучу
ядра. Проверено: `run t:\leak.axe` забирает 992 КБ и не отдаёт, второй запуск
находит ту же арену на месте.

## Откуда взялся мегабайт

*Замерено 30 августа 2026.* Каждая строка ниже — **две сборки одного коммита
`3d66106`, различающиеся одной опцией**, в отдельных worktree; размер берётся с
`build/argonos.bin`. Раскладка внутри образа — по карте линковки, с одной
поправкой: линкер сливает все `.rodata.*.str1.4` в один блок и приписывает его
целиком первому объекту, поэтому «`esp_app_format` весит 135 КБ» в отчёте
`idf.py size` — это на самом деле **общий пул строковых констант, 135 146 Б
(13.5% образа)**, разложенный здесь по владельцам до слияния.

### Как рос образ

Профиль `esp32s3` (QEMU) — единственный, который существует с первого дня:

| дата | коммит | что появилось | образ | Δ |
|---|---|---|---|---|
| 05.08 | `1f19553` | первый интерактивный `A:\>` | 237 888 | — |
| 06.08 | `2eead94` | процессы, супервизор, потоки, ловля фолтов | 424 512 | +186 624 |
| 07.08 | `887b781` | модель устройств, `/dev`, пины и шины | 484 816 | +60 304 |
| 09.08 | `3606f72` | **сеть** (lwIP + OpenEth), littlefs, софт-графика, редактор | 701 440 | +216 624 |
| 12.08 | `9c0b4d0` | сессии, системный шелл, фокус приложений | 724 032 | +22 592 |
| 16.08 | `eb63f28` | слой порта между ядром и чипом | 750 608 | +26 576 |
| 20.08 | `c8aabc1` | сегмент данных S3, `wget`, сервер файлов | 759 216 | +8 608 |
| 26.08 | `3efcf16` | S3 на столе, звук, камера | 785 760 | +26 544 |
| 29.08 | `83b856e` | SNTP и `date`, **`httpd` вынесен в `.AXE`** | 779 280 | **−6 480** |
| 30.08 | `3d66106` | **TLS**, telnet | 1 000 768 | +221 488 |

Два вывода, которых не видно из общей цифры. Первый: **мегабайт — это не
накопленный рост, а два события** — сеть 9 августа и TLS 30 августа дают
438 КБ из 763 КБ прироста. Второй: единственное отрицательное число в таблице —
это вынос `httpd` из прошивки в `HTTPD.AXE`, и оно единственное потому, что
больше ничего оттуда не выносили.

### Что почём

Одна строка на функцию, строки не пересекаются — их можно складывать.

**По умолч.** — `y`, если функция включена хотя бы в одном профиле цели
(`esp32s3`, `esp32s3-board`, `esp32c6`), `n` — если нигде. Это не то же самое,
что значение в `Kconfig`: там `NET_WIFI`, `ENABLE_BLE`, `USB_HOST`,
`ENABLE_CAMERA` и `ENABLE_ADC` стоят `n`, а включает их профиль платы.

**Размер:** точное число — замерено двумя сборками одного коммита,
различающимися одной опцией, то есть это **цена выключения** со всем, что
функция утаскивает за собой. Со знаком **≈** — разложение карты линковки
(отдельной опции нет), включая долю владельца в общем пуле строк.

| Функция | По умолч. | Размер, Б | Что даёт |
|---|---|---|---|
| Wi-Fi | y | 330 581 | интернет по радио, своя точка доступа, ESP-NOW между платами, монитор эфира и инъекция кадров |
| BLE | y | 286 256 | клавиатура и мышь по воздуху, сканирование окружения, GATT-клиент, своя реклама |
| TLS | y | 221 648 | `wget https://`, шифрование под SSH и MQTT |
| ↳ корневые сертификаты | y | 68 987 | проверка чужого сертификата без файла на карте |
| FreeRTOS, HAL, поддержка чипа | y | ≈161 400 | задачи, прерывания, таймеры, кэш, PSRAM, загрузка, паника |
| сеть без TLS | y | 160 480 | TCP/IP, сокеты, DNS, `wget`, `ftp`, telnet-консоль, `date sync` |
| FAT + littlefs + NVS | y | ≈80 500 | карта читается на ПК, `C:` переживает потерю питания |
| драйверы периферии IDF | y | ≈69 700 | SPI, I2C, UART, GPIO, LEDC — то, через что работает `io` |
| SD-карта и flash | y | ≈66 500 | `A:` на карте, `C:` во flash, разделы, XIP |
| libc | y | ≈64 900 | `printf`, строки, память, математика |
| USB-хост | y | 63 743 | проводная USB-клавиатура в OTG-порт |
| **`ota`** | **n** | **54 784** | обновление прошивки по сети одной командой |
| шелл и его команды | y | ≈39 800 | ~60 встроенных команд, `.bat`, `PATH`, `AUTOEXEC` |
| процессы, супервизор, загрузчик `.AXE` | y | ≈30 800 | запуск приложений, арена кода, сторож, разбор фолта без перезагрузки |
| `fm` + `edit` | y | ≈29 100 | двухпанельный менеджер и текстовый редактор встроены в образ |
| экран и графика | y | ≈19 400 | текстовый экран, шрифт 8×16, софт-примитивы рисования |
| звук | y | ≈17 400 | `/dev/pcm0`, микшер на несколько приложений, I2S |
| файловые системы (наш слой) | y | ≈15 200 | VFS, буквы дисков, `ramfs`, монтирование |
| консоль и VT100 | y | ≈14 600 | терминал, ANSI, кодовые страницы 437/866/1251 |
| **`ssh`** | **n** | ≈12 600 | консоль по шифрованному каналу (переиспользует mbedTLS) |
| загрузка, конфиг, журнал | y | ≈12 600 | `BOARD.CFG`/`SYSTEM.CFG`, журнал, безопасная загрузка |
| камера (транспорт) | y | 11 521 | DVP+DMA в прошивке, сенсор приезжает как `.SYS` |
| слой порта | y | ≈9 900 | вся привязка к ESP-IDF в одном месте; цена переносимости |
| устройства и `io` | y | ≈9 300 | `/dev`, реестр с владельцем, пины и шины приложениям |
| энергосбережение | y | ≈8 200 | `power full/cruise/eco/doze`, подсветка, оповещение приложений |
| **`mqtt` + `modbus`** | **n** | **5 152** | публикация/подписка в брокер и опрос промышленных устройств |
| HostFS | y | ≈3 500 | `H:` — живая папка с ПК по UART1, только для разработки |
| ADC | y | 1 477 | измерение напряжения, фоторезистор на плате |
| камера встроенная (`esp32-camera`) | **n** | не замерено | зоопарк сенсоров в образе вместо загружаемого `.SYS` |
| `BOARD_PROBE`, `CONSOLE_KEY_EVENTS` | **n** | ~0 | диагностика платы вместо системы; события отпускания клавиш |

Как это складывается в образы:

* **`esp32s3` (QEMU) — 1 000 768 Б**: всё, где `y`, кроме Wi-Fi, BLE, USB-хоста,
  камеры и ADC — они есть только в профилях плат;
* **`esp32s3-board` — 1 737 760 Б**: то же плюс Wi-Fi, BLE, USB, камера, ADC,
  минус HostFS;
* **`esp32c6` — 1 774 946 Б**: плюс Wi-Fi и BLE, минус USB, камера, ADC, звук,
  HostFS.

Внутри радио: Wi-Fi — `libnet80211` + `libpp` (MAC, **блобы**) 225 039,
`libwpa_supplicant` (исходники) 65 220, наш код ~18 700. BLE — `libbt` (хост
NimBLE, исходники) 136 007, `libbtdm_app` + `libbtbb` (контроллер, **блобы**)
93 974, `libesp_hid` 16 714, наш код 13 664.

**Радио — 36% образа платы, и 355 360 Б из 616 837 — закрытые блобы Espressif**
(`net80211`, `pp`, `btdm_app`, `btbb`, `phy`), которые нельзя ни переписать, ни
вынести в загружаемый модуль. Не только потому, что исходников нет: **39 274 Б
радиокода лежат в `.iram0.text`** (`btdm_app` 20 103, `pp` 15 307, `net80211`
3 864) — по адресу, назначенному компоновщиком, потому что приём кадров идёт при
отключённом кэше во время операций с flash. Загружаемый `.AXE` получает арену,
PSRAM или flash-XIP; линкерного IRAM он не получает никак.

### Что с этим делать

По убыванию отношения выигрыша к работе:

1. **`ota` — 54 784 Б, из них наших 617.** Остальное — `esp_https_ota`, который
   тянет **второй** HTTP-клиент (`esp_http_client` + `http_parser` +
   `tcp_transport`), при том что свой уже есть — `httpc.c` + `netmsg.c`, 8.9 КБ
   на двоих, на них работают `wget` и `ftp`. `ota` поверх своего клиента плюс
   `esp_ota_ops` (это `app_update`, 2.8 КБ, его не обойти) должен уложиться
   примерно в 5 КБ.
2. **Набор сертификатов — 68 987 Б чистых данных в прошивке.** Ему место на
   карте: `CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=n` и корни файлом на C:, либо
   `..._DEFAULT_CMN` вместо `_FULL`.
3. **Свой BLE-хост поверх HCI** — заменяемы `libbt` 136 007 и `libesp_hid`
   16 714; ниже них стандартный, опубликованный интерфейс. По характеру работы
   это то же, что SSH-сервер с нуля. Wi-Fi так не переписать: регистры MAC не
   документированы, а калибровка PHY закрыта.
4. **`ssl_tls12_server.c` 5 128 Б и `aria.c` 4 248 Б** линкуются в образ,
   которому нужен только TLS-клиент.

## Что не работает и о чём нельзя забывать

1. **Модель процессов есть, но не вся.** Работает: до 4 процессов, своя задача,
   стек и арена, свой рабочий каталог, учёт и полный возврат ресурсов,
   `exec`/`spawn`/`wait`/`kill`, передний план и `fg`, `Ctrl-C` как просьба и
   `Ctrl+\` как снятие, crash-record в журнал, потоки и объекты синхронизации,
   перехват исключения с указанием смещения в коде приложения, per-process
   watchdog по заявке приложения (`ag_watchdog`), crash-record в
   `C:\CRASH.LOG` вместе с хвостом журнала.
   **Есть (session slots):** `Ctrl+\` → system shell (повтор → kill), `Alt+1..4` /
   `Alt+Tab`, `slots` / `fg`, `AG_EV_FOCUS_*` + `ag_focused()`, вывод
   несфокусированного приложения уходит в журнал, а не на общий экран
   (коммит `eed0743`), **слот — стек процессов** (шелл → приложение → его
   ребёнок, глубина 4): консоль и клавиатура у верхнего, при выходе слот
   возвращается родителю с событием фокуса. До этого ребёнок приложения уходил
   в первый свободный слот и получал экран без клавиатуры.
   **Чего нет:** своего экрана на процесс — журнал это не заменяет.
   Дикий указатель по-прежнему может испортить чужую память — без MMU иначе не
   будет [Т-12]. Перехват спасает от типовых отказов (нулевой указатель,
   неинициализированный указатель на функцию), но не от порчи, которая уже
   случилась.
2. **Лимит на код снят на плате (спайк S-1, 26 августа 2026).** Данные и раньше
   не ограничивались ничем, кроме свободной PSRAM (512 КБ bss); код лежал в
   D/IRAM и упирался в арену. Теперь `execmem_hw.c` мапит блок PSRAM в оба окна
   (data + instruction, `esp_mmu_map` + `MMU_MEM_CAP_EXEC`, writeback/инвалидация
   перед прыжком), и арена кода живёт в 512 КБ PSRAM. В QEMU по-прежнему D/IRAM
   (кэш не моделируется) — это чиповая, а не общая возможность.
3. **Ячейка экрана — один байт, и это уже работает как надо** (`chcp`,
   `[console] codepage`): байт значит то, что говорит кодовая страница, а перевод
   в UTF-8 для терминала делает рендерер. Осталась мелочь: рамки в файловом
   менеджере всё ещё нарисованы `+-|`, хотя символы псевдографики теперь
   доезжают. Менеджеру для этого нужно спросить `ag_codepage()` и выбрать набор —
   в CP1251 псевдографики нет.
4. **Перерисовка всего экрана — ~90 мс, при движении курсора ~20 мс** (измерено
   менеджером, он это показывает). Неизменившиеся ячейки в терминал не уходят;
   время съедает ожидание консольного замка, который рендерер держит, пока
   передаёт кадр в UART. Лечится сборкой потока под замком и записью в порт
   после его отпускания — не сделано.
5. **`Ctrl+C` не доходит до гостя** в консольном режиме QEMU: он оставлен
   консоли, чтобы можно было выйти из зависшего эмулятора.
6. **Модель устройств, `.SYS` и I2C-`probe` есть; display class_ops — да.**
   Реестр, `/dev`, `ioctl`, пины, шины, `drv load`/`unload`/`probe` работают.
   Soft `fb0` отдаёт `ag_display_ops_t` через `dev->ops(h)` (ABI 0.23), реальная
   панель — `ILI9341.SYS` (`text_row`/`blit_rect`, ABI 0.27/0.30). ✅ на плате:
   **I2S** (класс `audio`), **DVP** (камера, `GC2145.SYS`). Чего ещё нет:
   class_ops у остальных классов; probe по SPI-панелям; из шин — TWAI, 1-Wire,
   RMT, MCPWM, DAC, RTC.
7. **`io` проверено в QEMU, а не на железе, и это разные вещи.** Что проверено:
   отказ на пине системы, занятие и возврат пина, отзыв обработчика за упавшим
   процессом, чтение `BOARD.CFG`, подъём шины при первом обращении, скан I2C.
   Чего проверить нельзя: **уровень на пине** — QEMU не связывает выход со
   входом, так что `io 5 1` не меняет того, что читается, — и вообще любой
   обмен по шине, потому что на шинах эмулятора никого нет. Первое, что надо
   сделать с платой в руках: `io i2c 0` с настоящим датчиком.
8. **ADC: выключен в QEMU, включён на плате.** Линковка драйвера ESP-IDF ломает
   загрузку в QEMU (грабли 26), поэтому в дефолтной эмуляторной сборке `ADC`
   выкл; на плате S3 `CONFIG_ARGON_ENABLE_ADC=y` и фоторезистор читается.
9. **Чтение `d:\con` держит замок файловой системы, пока оператор печатает.**
   `copy d:\con t:\notes.txt` ждёт строку, а ждёт он с замком VFS, поэтому
   фоновый процесс, читающий файл, ждёт вместе с ним. Причина общая: VFS
   держит свой замок на всё время обмена с бэкендом. Лечится тем же, чем
   медленная перерисовка экрана, — отпускать замок на время передачи.
10. **Сеть и дисплей есть, USB-host — нет.** Сеть: Wi-Fi STA/AP/ESP-NOW/monitor
    + BLE на плате, сокеты/DNS/`wget`/`httpd`/`ftp`. Дисплей: `ILI9341.SYS` +
    графика на плате. Открыт **USB-host** — ни клавиатуры, ни мыши, ни MSC
    (стека tinyusb/usb_host в дереве нет).
11. **В менеджере нет** копирования каталога целиком, рекурсивного удаления,
    поиска и командной строки внизу.
12. **Doom в QEMU: клавиатура не доезжает.** Несколько попыток (фокус SDL в
    `kbdvirt`, FIFO/bitmap в госте, мышь не в меню) не починили — с титра
    нельзя стартовать. Работа остановлена; план
    [`plans/doom-input.md`](plans/doom-input.md).

## Формат `.AXE` в трёх абзацах

Приложение линкуется на хосте **двумя непрерывными частями**: код с константами
и данные с bss. При переносе части по другому адресу **все PC-относительные
инструкции внутри неё остаются верными** — сдвигаются и инструкция, и цель.
Править надо только абсолютные 32-битные слова (литеральный пул, инициализированные
указатели). Поэтому в ядре нет ни ELF-парсера, ни декодера инструкций:
`tools/mkaxe.py` выдаёт список смещений, загрузчик копирует, прибавляет смещение
и прыгает.

Части переезжают независимо, поэтому у каждой релокации два признака: в какой
части лежит слово и в какую часть смотрит адрес внутри. Оба живут в младших двух
битах смещения — смещения выровнены на слово. Для RISC-V (`medany`, код достаёт
данные PC-относительно) образ собирается слитным: флаг `AG_AXE_CONTIGUOUS`
требует положить данные сразу за кодом и относить обе части одним смещением.

Собрать приложение и проверить его в эмуляторе:

```
python tools/mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc \
    --include sdk/include -o build\apps\HELLO.AXE apps/hello/hello.c
argon test -Put "build\apps\HELLO.AXE=t:\hello.axe" "run t:\hello.axe alpha"
```

Подпись `.AXE` (HMAC в `reserved[6]`, нули = без подписи):

```
python tools/signaxe.py build\apps\HELLO.AXE
python tools/signaxe.py --verify build\apps\HELLO.AXE
argon test -Put "build\apps\HELLO.AXE=t:\hello.axe" "run t:\hello.axe"
```

Soft graphics (без платы):

```
python tools/mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc \
    --include sdk/include -o build\GFXDEMO.AXE apps/gfxdemo/gfxdemo.c
argon test -Put "build\GFXDEMO.AXE=t:\gfxdemo.axe" \
    "run t:\gfxdemo.axe" "gfxdump t:\shot.ppm" "dir t:\shot.ppm"
```

Flash XIP / R-1 (код больше арены):

```
python tools\gen_bigcode.py
python tools\mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc \
    --include sdk/include -o build\BIGCODE.AXE \
    apps\bigcode\bigcode.c apps\bigcode\bigcode_body.c
argon test -Put "build\BIGCODE.AXE=t:\bigcode.axe" -TimeoutSec 180 \
    "run t:\bigcode.axe" "errorlevel"
```

Master System (отдельное GPL-приложение): точная команда сборки — в
[`apps/sms/README.md`](../apps/sms/README.md). Проверка:

```
argon test -Put "build\SMS.AXE=t:\sms.axe" -TimeoutSec 180 `
  "run t:\sms.axe 180" "errorlevel" "gfxdump t:\sms.ppm"
```

Звук (PSG + FM/`ag_fm` → mock / WAV, без I2S):

```
argon test -Sd -TimeoutSec 120 `
  "run a:\SMS.AXE 30 nolivepad mock" "errorlevel" `
  "run a:\SMS.AXE 60 nolivepad wav" "errorlevel"
```

Скорость (печатает fps, разбивку кадра и % realtime; `fps30` — показ через кадр):

```
argon test -Sd -TimeoutSec 240 `
  "run a:\SMS.AXE 600 nolivepad stats" "errorlevel" `
  "run a:\SMS.AXE 300 nolivepad fps30 stats" "errorlevel"
```

Mega Drive — то же самое, без ROM запускается синтетический картридж, который
крутит 68000 на месте (то есть меряется VDP при простаивающем процессоре):

```
argon test -Sd -TimeoutSec 240 "run a:\MD.AXE 300 nolivepad stats" "errorlevel"
```

Вручную доставить в эмулятор: `recv t:\hello.axe`, затем строки hex, затем `END`.

## Как запустить файловый менеджер и редактор

Оба **встроены в ОС** — команды шелла, доставлять ничего не нужно:

```
argon run
```

и в приглашении:

```
fm            обе панели в текущем каталоге
fm t: c:      левая на RAM-диске, правая на flash
edit t:\a.c   создать или открыть текстовый файл
```

В менеджере `F10` или `Esc` — выход, `F1` — клавиши. В редакторе `F2` —
сохранить, `Esc` / `F10` — выход, `F1` — справка.

Тот же код собирается и как обычное приложение `.AXE` — это способ проверить, что
встроенное и загруженное ходят через одну и ту же таблицу ABI, а не мимо неё:

```
python tools\mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc ^
    --include sdk/include --include apps/fm ^
    -o build\FM.AXE apps\fm\fm.c apps\fm\fmops.c apps\fm\fm_ui_text.c
argon test -Put 'build\FM.AXE=t:\fm.axe' 'run t:\fm.axe t: c:'
```

Графический twin (`GFXFM.AXE`, нужен `-Gfx`):

```
python tools\mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc ^
    --include sdk/include --include apps/fm ^
    --cflags "-Os -ffunction-sections -fdata-sections -DFM_GFX_BUILD" ^
    -o build\apps\GFXFM.AXE ^
    apps\gfxfm\gfxfm_app.c apps\gfxfm\fm_ui_gfx.c ^
    apps\fm\fm.c apps\fm\fmops.c
```

Проверять из скриптов удобно так — клавиши посылаются сырыми байтами с префиксом
`~`: `\x1b[B` это ↓, `\x1bOP` это F1, `\x0d` — Enter, `\x09` — Tab,
`\x1b[21~` — F10:

```
argon test 'fm t: c:' '~\x1b[B' '~\x1bOP'
```

**Одинарные кавычки в PowerShell не случайны:** аргумент, кончающийся на `\`,
в двойных кавычках съедает закрывающую кавычку, и следующие аргументы
склеиваются с ним. Проявляется как «клавиши не доходят до приложения».

## Слой портирования в двух абзацах

**Ядро не вызывает ESP-IDF.** Оно вызывает порт — 14 заголовков в
[`components/argon_port/include/argon/port/`](../components/argon_port/include/argon/port),
за которыми стоит реализация из `idf/` или `bare/`. Выбор — одна строка
`set(ARGON_PORT ...)` в `CMakeLists.txt` компонента. Проверяется это не на
слово: в `components/argon_kernel` нет ни одного включения `esp_*`,
`freertos/*`, `driver/*`, `soc/*` или `sdkconfig.h`, и `grep` по ним пуст.

Правило слоя — **он не имеет права стоить**. Всё, что отображается на нижний
вызов один к одному (память, время, задачи, примитивы синхронизации), —
`static inline` в заголовке порта, и кодогенерация в этих местах совпадает
байт в байт. Отдельными `.c` живёт только то, что действительно чужое:
`fault_hw.c` (таблица исключений Xtensa), `uart_hw.c`, `flash_hw.c`,
`storage_hw.c`, `io_hw.c`, `panel_hw.c`, `net_hw.c`. Само ядро при этом
**похудело на 6.0 КБ**, порт добавил 8.3 КБ, образ вырос на 2.4 КБ (+0.3%),
занятая internal SRAM не изменилась — 70 КБ. Что именно требуется снизу и в
каком порядке это писать для голого железа —
[`components/argon_port/bare/README.md`](../components/argon_port/bare/README.md);
там же честные оценки, из которых главная: планировщик — это не обёртка, а
ядро, и на нём стоит всё остальное.

Хост-тесты собираются против порта `bare`: у переносимой половины ядра под
ногами нет вообще никакой машины, и это ровно то, что должно быть.

## Устройства в двух абзацах

**Устройство — это файл на диске `D:`.** Реестр (`src/dev/device.c`) хранит имя,
класс, драйвер, флаги и владельца; `devfs` (`src/dev/devfs.c`) показывает его как
каталог, смонтированный в `/dev`. Из этого следует главное: таблица дескрипторов
одна на файлы и устройства, а значит учёт владельца и закрытие того, что осталось
от снятого процесса, уже написаны и не нужны второй раз. Положение принадлежит
дескриптору, поэтому `hexdump d:\sd0` читает карту посекторно, а два открытия
одного диска не мешают друг другу.

Подтаблица ABI `dev` добавляет к этому три вещи, которых у файла нет: открытие по
голому имени, перечисление с фильтром по классу (так приложение находит диск, не
зная, как его назвали) и `ioctl`. Что зарегистрировано сейчас: `null`, `zero`,
`con` (консоль как файл), `flash0` и `sd0` — оба только на чтение, потому что
сверху смонтирована FAT со своим кэшем.

```
argon run
```

и в приглашении:

```
dev                          что на этой плате есть
dev sd0                      про одно устройство, с геометрией
dir d:                       то же самое как каталог
hexdump d:\sd0               что реально лежит на карте, мимо файловой системы
copy d:\con t:\notes.txt     набрать файл с клавиатуры, Ctrl+Z — конец
dir > d:\null                выбросить вывод
```

## Пины и шины в двух абзацах

**Прямой доступ к железу разрешён, забирать чужое — нет.** Таблица пинов
(`src/dev/ioclaim.c`) знает про каждый пин, кто его держит и зачем. Пин системы
— консоль, карта, flash, PSRAM, а также пины поднятой шины — не отдаётся никому
и отвечает `-AG_EACCES`; пин, занятый другим процессом, — `-AG_EBUSY`; читать
можно любой. Всё, что процесс занял, возвращается при его завершении, вместе со
снятием обработчика прерывания: обработчик, переживший свой код, — это плата,
которая встанет на следующем фронте и не скажет почему.

Шины (`src/dev/io.c`) берут пины и скорости из `BOARD.CFG` и **поднимаются при
первом обращении**. Поэтому в памяти они не стоят ничего, пока не нужны, и
неверно описанная плата не начинает дёргать пины на старте. Номера — как в
даташите: `[i2c0]`, `[spi2]`, `[uart1]`.

```
io                           карта пинов: кто что держит
io 5 out                     настроить пин, io 5 1 — выставить
io i2c 0                     что на шине; первое, что делают с новой платой
```

То же самое из приложения, через таблицу ABI, — `apps/devs/devs.c`; он проверяет
себя сам, поэтому это ещё и тест того, что встроенное и загруженное ходят одной
дорогой:

```
python tools\mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc ^
    --include sdk/include -o build\DEVS.AXE apps\devs\devs.c
argon test -Sd -Put 'build\DEVS.AXE=t:\devs.axe' 'run t:\devs.axe' 'errorlevel'
```

## Грабли, которые уже стоили времени

Каждая из них проявлялась как что-то совсем другое.

1. **Окно внешней памяти S3 — 32 МБ, общее для flash и PSRAM.** При 32 МБ PSRAM
   `spi_flash_mmap` не находит адресов, и `esp_partition` **не видит ни одного
   раздела** — потому что читает таблицу через отображение. Выглядит как
   «сломана таблица разделов», хотя `esp_flash_read` по тому же адресу отдаёт
   корректные байты. Плата с 32 МБ PSRAM непригодна; QEMU запускается с 8 МБ.
2. **`src/con/` — зарезервированное имя устройства Windows.** Git молча
   пропускает такой каталог: заголовок закоммитился, `.c` — нет. Запрещены
   `con aux prn nul com1..9 lpt1..9`.
3. **`MALLOC_CAP_EXEC | MALLOC_CAP_8BIT` даёт ноль.** IDF помечает исполняемую
   память как только-словную — наследие оригинального ESP32.
4. **PowerShell может встать в середину вывода дочернего процесса.** Оператор
   вызова `& $qemu` заставляет PowerShell читать вывод как текст и печатать
   заново своим рендерингом, который не интерпретирует escape-последовательности
   — экран приходит стеной `←[2J` при правильно настроенной консоли. Нужен
   `Start-Process -NoNewWindow`.
5. **Классическая консоль Windows не интерпретирует ANSI**, пока не включить
   `ENABLE_VIRTUAL_TERMINAL_PROCESSING`, причём на handle, открытом как
   `CONOUT$` по имени — `GetStdHandle` не всегда консоль.
6. **TCP-порт QEMU без подключённого клиента выбрасывает вывод.** Нужен
   `wait=on`, иначе тест гоняется с эмулятором и иногда проигрывает.
7. **Логи через экран теряются при всплеске** — рендерер отправляет текущее
   состояние строки. Ради этого и сделан журнал.
8. **Длинные имена в FAT у ESP-IDF выключены по умолчанию.**
9. **Splatting массива в PowerShell передаёт аргументы позиционно** — `-Sd`
   приехал как значение `-Marker`. Нужен hashtable.
10. **`void**` на 64-битном хосте читает 8 байт** там, где в образе 32-битное
    слово. Образ — 32-битное адресное пространство независимо от машины.
11. **Секция в `.iram1` лежит в образе целиком.** Арена 64 КБ добавила ровно
    64 КБ нулей во flash и копирование их при старте, потому что имя секции не
    похоже на bss. Нужна `.iram.bss.*` — она `NOLOAD`, стоит только адресов.
12. **GCC уносит статический указатель, который нигде не пишется, в `.rodata`.**
    В фикстуре из-за этого не оказалось ни одной релокации в части данных, и
    проверять было нечего. Внешняя видимость (`g_`) лишает компилятор права так
    решать. С тех пор `.rodata` сама уехала в часть данных, так что такой
    указатель попадает туда же; внешняя видимость в фикстуре осталась — она
    проверяет именно *записываемое* слово.
13. **`-replace` со scriptblock в PowerShell 5.1 не вызывает блок**, а
    подставляет его исходный текст. Escape-последовательности в тесте
    превратились в мусор, и Ctrl+\ просто не доехал до гостя — выглядело как
    «супервизор не работает». Разбор `\xNN` теперь руками.
14. **Одна строка журнала — это несколько записей** (голова, тело, перевод
    строки). Без замка задача, вытесненная между ними, вклеивает свою строку в
    чужую; на одном ядре это было редко, с приложением на втором ядре стало бы
    обычным делом. В `log.c` теперь рекурсивный мьютекс.
15. **GCC удаляет разыменование литерального нуля целиком.** Тест «чтение по
    адресу 0» на `-Os` не проверял ничего: разыменование нуля — undefined
    behaviour, и компилятор вправе выбросить весь оператор, что и делает.
    Выглядело как «QEMU не моделирует отказ на чтении». Нужен указатель,
    про который компилятор не может доказать, что он нулевой (`volatile`).
16. **Удаление уже удалённой задачи FreeRTOS не сообщает ни о чём** — это
    повторное использование освобождённого TCB, и система просто перестаёт
    работать без единого сообщения. Так проявился упавший поток, который удалил
    себя, но остался в ресурсном списке помеченным как работающий. Любой путь
    завершения потока обязан сначала пометить запись.
17. **Консоль читала порт медленнее, чем идёт линия.** Один блок 64 байта за
    такт в 10 мс — это 6.4 КБ/с против 11.5 КБ/с на 115200. Всплеск переполнял
    буфер драйвера, и байты **терялись молча**: файл через `recv` приезжал тихо
    неверным. Теперь порт вычитывается до конца в пределах такта, и добавлен
    XON/XOFF — а обвязка сверяет число записанных байт с размером файла.
18. **Транскрипт QEMU — это отрендеренный экран, а не лог.** При прокрутке строки
    перерисовываются, поэтому считать в нём вхождения (например приглашения)
    можно только там, где «стало больше» — достаточный признак. Для «это точно
    произошло» нужен текст, который печатается один раз.
19. **Образ flash для QEMU нельзя проверять на свежесть по времени файла.**
    Эмулятор сам пишет в этот файл (в нём живёт littlefs на `C:`), поэтому после
    любого прогона образ выглядит новее прошивки, из которой собран, и тест
    молча гоняет предыдущую сборку. Час ушёл на «watchdog не работает», который
    работал. Теперь сверяется записанная метка (время и размер `argonos.bin`).
    Смена таблицы разделов / прошивки пересоздаёт образ и снова автоформатирует
    `C:`.
20. *(номер пропущен при правке 13 августа; оставлен, чтобы не сдвигать
    ссылки на грабли 21–28.)*
21. **Задачу, держащую мьютекс, нельзя снимать.** Легального способа отобрать
    мьютекс у удалённой задачи в FreeRTOS нет: удаление семафора с ожидающими —
    UB, пересоздание оставляет ждущих на старом хэндле. Поэтому супервизор
    сначала спрашивает `xSemaphoreGetMutexHolder`, ждёт до 500 мс и, если замок
    так и не отпущен, **отказывается** снимать процесс и говорит об этом.
22. **Обвязка QEMU писала транскрипт как текст, а не как байты.**
    `WriteAllText` без кодировки — это UTF-8, а транскрипт собирался посимвольно
    из байтов линии, поэтому каждый байт выше 0x7f уезжал в файл **двумя**. Пока
    гость говорил только ASCII, это было не видно; первая же строка кириллицы
    приехала как `Ð?Ñ€Ð¸Ð²ÐµÑ‚` — и выглядело это как ошибка в рендерере, а не в
    инструменте, которым его проверяют. Теперь Latin-1, то есть байт в байт.
    Та же ошибка в третий раз в этом проекте: **инструмент проверки надо
    проверять первым**, иначе он врёт про исправную систему.
23. **Таблицу переходов от `switch` GCC складывает в `.rodata`**, и в ней лежат
    абсолютные адреса, смотрящие в `.text`. Пока константы ехали вместе с кодом,
    это было слово в части кода, смотрящее в часть кода, — самый безобидный
    случай. После переезда `.rodata` в часть данных то же слово стало
    «в данных, смотрит в код», и такой комбинации в обычном приложении раньше
    не было ни одной. Формат её предусматривал, и загрузчик правит его верно, но
    проверялось это только специально написанным указателем на функцию; теперь
    такая таблица есть в фикстуре, и её ставит компилятор, а не автор теста.
24. **Управляющий код — это не символ, а нажатая клавиша.** Разбор ввода отдаёт
    `Ctrl+Z` как событие с `keycode = Z`, `mods = CTRL` и `unicode = 0` — и это
    правильно, потому что `Ctrl+Z` нечего писать в файл. Устройство `con`
    сначала читало `getch`, который отдаёт только символы, поэтому конца ввода
    оно не видело **никогда**: `copy d:\con t:\notes.txt` не заканчивалось, а
    следующие набранные команды уезжали в файл. Выглядело как «`copy` сломан».
    Всё, что ждёт управляющую клавишу, обязано читать события.
25. **Устройство, которое не кончается, вешает команду шелла.** `type d:\zero`
    читает до конца файла, а у потока конца нет, и `Ctrl+C` в шелле до этого
    никуда не смотрел: он отменяет набранную строку, а команда уже выполнялась.
    Машину оставалось только сбросить. Теперь `Ctrl+C`, нажатый когда на переднем
    плане никого нет, записывается супервизором, а `type`, `copy` и `hexdump`
    спрашивают об этом между порциями. Урок общий: новая возможность (устройство
    как файл) сделала законной ситуацию, которой у файлов не бывает.
26. **Линковка драйвера ADC из ESP-IDF ломает загрузку в QEMU.** Не вызов —
    **линковка**. `esp_adc` содержит конструктор `adc_hw_calibration()`, который
    выполняется до `app_main` и читает калибровочный автомат аналогового тракта;
    QEMU его не моделирует, и загрузка останавливается там, где последней
    строкой стоит предупреждение про efuse, до баннера ArgonOS. Искать при этом
    надо не в своём коде — в этой цепочке его нет вовсе. Поэтому ADC — опция
    сборки `CONFIG_ARGON_ENABLE_ADC`, по умолчанию выключенная, а `io->adc_read`
    без неё `NULL`. Общий урок: **компонент может стоить работоспособности,
    ничего не будучи вызванным.**
27. **«Владелец — ядро» недостаточно, когда ядро не одно.** Пины поднятой шины
    сначала занимались от имени `AG_PID_KERNEL` — и шелл, который тоже ядро,
    спокойно забирал у работающей шины её SDA командой `io 8 out`, потому что
    владелец совпадал. Ровно та драка, ради предотвращения которой таблица и
    заведена, только этажом выше, чем её ищут. Теперь шина **резервирует** пины,
    а резерв не отбирается ни у кого, включая ядро.
28. **Скан пустой шины писал в лог по строке на адрес.** Драйвер I2C из ESP-IDF
    считает таймаут ошибкой — и правильно, когда драйвер разговаривает с чипом,
    который должен быть на месте; но скан спрашивает про 112 адресов, и экран
    заполнялся сообщениями ESP-IDF, среди которых терялся собственный результат.
    На время скана тег `i2c.master` заглушается. И вторая половина той же
    ошибки: «на этом адресе никого» и «такой шины нет» сначала возвращали один
    код, так что скан несуществующей шины выглядел точно как скан пустой.
29. **Ограничение таймаута — это не починка зависания, а плата на каждой
    загрузке.** Проба HostFS могла не поймать хелпера с первого PING (QEMU
    переоткрывает TCP на UART1 после гостевого `reboot`), и цикл повторов
    ограничили тремя секундами. Ограничение сработало — и загрузка стала
    стоить **3.4 с вместо 0.4 с**, потому что «хелпер не отвечает» это не
    исключение, а норма: на плате его нет никогда. Цель проекта <400 мс при
    этом никто не перепроверил. Теперь на пути загрузки один короткий PING
    (100 мс), а ждут хелпера в фоновой задаче, которая монтирует `H:` когда он
    появится. Урок: **прежде чем ограничивать ожидание, спроси, кто платит за
    него в обычном случае.**
30. **Что не собирается автоматически — то и ломается.** Приложения
    линкуются отдельно от прошивки, их команды сборки лежали по одной строке в
    README каждого, и в CI не собиралось ни одно. На 3000 строк нового DSP не
    было ни одного теста. `argon check` был зелёным, пока в main лежали: пила
    с постоянной составляющей в полшкалы, FM-матрица без единой боковой
    полосы (модуляция прибавлялась к 32-битной фазе без сдвига — 0.000012
    цикла), ADSR, работавший в 256 раз быстрее задуманного, и LFO-треугольник
    вчетверо быстрее. Все четыре — арифметические, все четыре ловятся
    измерением за миллисекунду. Теперь есть `argon apps` и
    [`host-tests/test_dsp.c`](../host-tests/test_dsp.c), и оба входят в
    `argon check`.
31. **Свёртку нормируют по энергии, а не по пику.** IR-движок приводил все
    импульсные характеристики к одинаковому пиковому отсчёту — а усиление
    свёртки задаёт `sqrt(Σh²)`, и 500-мс хвост шума несёт в сотни раз больше
    энергии, чем 20-мс кабинет с тем же пиком. Выход упирался в потолок на
    каждом отсчёте, и wet-сигнал **перестал зависеть от входного уровня
    вообще** — вход тише в 16 раз, выход тот же. Вторая половина той же
    ошибки: спектры хранились в int16 с жёстким сдвигом `>>9`, который для
    одного IR переполняет корзины, а для другого оставляет в них единицы, так
    что свёртка превращалась в шум округления. Теперь IR нормируется по
    энергии, сдвиг выбирается по конкретному IR и снимается на выходе, а тест
    проверяет то единственное, что линейный фильтр обязан делать: вдвое тише
    на входе — вдвое тише на выходе.
32. **Генератор случайных чисел уровня файла делает результат
    невоспроизводимым.** `rnd16()` в `ag_ir.c` продолжал последовательность
    между вызовами, поэтому один и тот же пресет реверба строил каждый раз
    другую комнату: звук менялся под руками, а ни один рендер нельзя было
    повторить. Сеется при каждой загрузке пресета.
33. **Часов, которыми можно мерить DSP, у нас не было — а выглядело, что
    есть.** `api->time->cycles()` в таблице ABI возвращал `esp_timer_get_time()`,
    то есть ровно то же число, что `us()`. Для единственной задачи, ради которой
    запись существует (померить внутренний цикл, где микросекунда — это 240
    тактов), она была бесполезна, и никто этого не замечал, потому что её никто
    не звал. После починки на `esp_cpu_get_cycle_count()` выяснилась вторая
    половина: **в QEMU этот счётчик — виртуальные часы с частотой ядра 40 МГц**,
    а сами виртуальные часы следуют за временем хоста. Проверка меряет
    подразумеваемую частоту на трёх разных нагрузках и получает 39–40 МГц для
    всех трёх — признак того, что счётчик не считает работу. Годного счётчика
    тактов в эмуляторе нет; есть `-icount shift=0`, при котором одна выполненная
    инструкция продвигает виртуальное время ровно на наносекунду, и тогда
    микросекунды — это точный счёт инструкций (проверено: целочисленная цепочка
    даёт 6.000 на итерацию при 4000, 40 000 и 400 000 повторов). Ключ
    `-Icount` добавлен в `argon test`. **`sleep=off` к нему добавлять нельзя** —
    выглядит как «не тормози», а делает холостой цикл гостя платным: та же
    работа шла минуту с ним выключенным и не кончилась за одиннадцать с
    включённым. Разбор целиком — [`08-circuit-simulation.md`](08-circuit-simulation.md).
34. **Сходимость Ньютона нельзя отлаживать по агрегатам.** «2883 отсчёта из
    4800 не сошлись» не говорит ни где, ни почему, и пять правок подряд были
    сделаны мимо: демпфирование шага, гладкое колено сеточного тока, LU в
    double, решение относительно приращения, невязка в double — каждая из них
    сделала **хуже**. Причина оказалась в обусловленности матрицы, и увидел её
    первый же прогон вывода последовательности итераций: чистый цикл периода
    три амплитудой 7 мВ на узле в 190 В. Трассировщик надо писать раньше
    гипотез, а якобиан — сверять с конечными разностями, а не читать глазами.
35. **На RISC-V «D/IRAM» — это не свойство кремния, а PMP.** На Xtensa одна
    физическая SRAM видна с обеих шин: писать по адресу и исполнять по нему же
    можно, и на этом стоит вся арена кода. У C6 адресное пространство одно, а
    разделение обеспечивается защитой — ESP-IDF при старте ставит PMP так, что
    всё до `_iram_text_end` читается и исполняется, а всё после читается и
    пишется. Арена лежит в `.iram0.bss`, то есть **после** этой границы.
    Первый же `.SYS` загрузился, срелоцировался и умер на первой своей
    инструкции: `Instruction access fault`, `MEPC` в середине арены — по
    адресу, куда загрузчик только что успешно писал. Записываемое и
    неисполняемое — это ровно DRAM-половина того разделения. Лечится
    `CONFIG_ESP_SYSTEM_PMP_IDRAM_SPLIT=n`; обоснование — в
    `sdkconfig.defaults.esp32c6`, потому что это защита, которую эта
    архитектура не может сохранить [Т-12], и терять её молча нельзя.
36. **Неинициализированная структура ESP-IDF ждёт годами и стреляет от чужой
    правки.** `ag_port_uart_config` заводил `uart_config_t hw;` без
    инициализации и заполнял только те поля, о которых у порта есть мнение.
    У IDF там с тех пор появились `rx_flow_ctrl_thresh` и `flags`, чей бит
    `allow_pd` просит sleep retention, которого у UART S3 нет. Значение
    бралось со стека — и `uart_param_config` то отказывал порту с «not able to
    power down in light sleep», то нет. Вылезло как `hostfs: UART1
    unavailable` и четыре красных `nettest` после правки **этажом выше** по
    стеку, которая ничего в UART не трогала. Обнуление перед заполнением
    чинит и то, что IDF добавит следующим.

    **1 сентября 2026: описание было в `main`, а правка — нет.** Этот абзац
    приехал с e7df926, а само зануление осталось на невлитой ветке
    `ext-radio` — вместе с такой же правкой второго бага, переконфигурации
    порта на каждом чтении в `src/dev/io.c`. То есть `main` нёс оба бага, а
    список грабель читался так, будто не нёс.

    Второй раз первый из них вылез совсем с другого конца: не `hostfs: UART1
    unavailable` в QEMU, а приложение на настоящей плате, которое просит
    921600 и получает `input/output error`, притом что 115200 через тот же
    путь работает. Один и тот же вызов, разный стек, разный ответ.

    В `main` теперь своё зануление, и стоит оно **внутри `fill()`**, а не в
    месте вызова, чтобы его не потерял второй вызывающий. **При вливании
    `ext-radio` надо взять одну из двух версий, а не две**: там зануление в
    месте вызова плюс явный `rx_flow_ctrl_thresh = 0`, а в `io.c` — та же
    мысль без кэша применённого режима.

    Мораль шире самого UART: **запись в списке грабель не есть доказательство
    правки.** Грабли и правка едут в разных коммитах и могут разъехаться по
    ветками. Если грабли описаны в прошедшем времени — сверять с кодом той
    ветки, где работаешь.

### Экран, ввод и звук для S3 (1 сентября 2026)

У S3 нет своего стекла, и теперь его заменяет CYD на двух проводах:
`REMDISP.SYS` публикует устройство класса display, чья панель — это UART1, а
`REMTERM.AXE` на CYD кладёт пришедшее на стекло. **15 кадров в секунду на полном
кадре 320x240 и 154 на изменившихся прямоугольниках**, сжатие от 8 до 50 раз
(габаритная рамка изменений плюс палитра с индексами — она выигрывает на 100%
прямоугольников, потому что содержимое восьмибитное). Приложения не меняются:
ядро рассылает выведенную полосу каждому устройству класса display, у которого
есть `blit_rect`, и `ag_gfx_flush` штатной поверхности идёт тем же путём.

Порядок запуска, требование Fallout к размеру поверхности и чем отличить
работающее от сломанного — в [09-esp32-cyd.md](09-esp32-cyd.md), раздел «Как
посадить приложение на этот экран». Пропускная способность самого провода
измерена отдельно: **4 Мбод = 400 067 Б/с**, 4 МБ без единой ошибки.

**Ввод: USB-мышь и клавиатура работают** в OTG-порт (донгл Logitech 046d:c542
проверен, 541 отчёт). Состояние указателя спрашивается командой `ptr` — она
существует потому, что события приходят, пока рука на устройстве, а поймать
момент на машине с экраном в другом месте нечем. **Apple Magic по Bluetooth
недостижимы**: у S3 радио только BLE, а они говорят классическим HID — это
кремний, не код.

**Звук: у CYD есть и услышан** (динамик припаян к SPEAK, `beep 440 500` слышно —
первое подтверждение аналоговой цепи для этой платы). **Но по проводу звук не
проброшен** — это оставшаяся часть замысла, а не сделанная. У S3 усилитель
MAX98357A выпаян (пины
`[audio]` 47/48/21 остались, за ними ничего), у CYD усилитель на плате есть, но
разъём SPEAK приходит пустым и динамик надо припаять. И звук по проводу не
сделан: линк несёт пиксели и текст. `/dev/pcm0` откроется и будет играть в
воздух, и программно это неотличимо от работы.

### Ждёт платы

**Цель `esp32` (CYD) снова линкуется** (1 сентября) — 1.62 МБ из раздела
1.9 МБ. Причина была не та, что здесь стояла: на ESP32 `uart_tx_wait_idle` —
`static inline` в заголовке IDF, а не символ ROM, и оба записанных кандидата на
починку вели мимо — первый слинковал бы ошибочную версию из ROM. Разбор —
в [09-esp32-cyd.md](09-esp32-cyd.md), раздел «Собрать и прошить».

Осталось проверить на плате: вызов держит консоль при перехвате UART0, так что
проверка — это первый же баннер после прошивки: молчание в консоли значит,
что слив не сработал.

**RGB-панель QEMU не работала вовсе, и признак был один — размер окна**
(8 сентября). `components/argon_port/idf/src/panel_hw.c` проверял
`#if CONFIG_ARGON_PANEL_QEMU`, **не подключив `sdkconfig.h`**; неопределённый
макрос в `#if` — это ноль, и вся панель вырезалась препроцессором молча.
`ag_port_panel_open` отвечал «нет панели», дисплей выделял софтовую
поверхность, приложения в неё рисовали, а окно SDL открывалось своего размера
800×600 и оставалось чёрным. Единственная зацепка снаружи: **окно не того
размера, которого просил гость** — а значит, панели за ним нет. Плюс QEMU не
создаёт эту панель без `-M esp32s3,graphics=on` (это опция машины, а не
устройство: `-device esp_rgb` не существует). Оба исправлены; см.
[`apps/desktop/README.md`](../apps/desktop/README.md).

**Виртуальные мышь и клавиатура: `ensure_listen` затирал хороший дескриптор**
(8 сентября). Его зовут два потока — такт ввода ядра и шелл — оба видят
`listen < 0` до того, как кто-то сохранит результат; проигравший получает
`-AG_EBUSY` и записывает `-1` поверх сокета победителя. Порт после этого
закрыт до конца загрузки, а лог за миллисекунду до того сообщает, что порт
открыт. Отказ теперь не трогает дескриптор, который поставил кто-то другой.

**Запись на C: и перезагрузка в QEMU ненадёжны** (8 сентября). На восьми
прогонах трижды файл, записанный на C:, читался обратно битым (`-91`,
AG_EFORMAT — в том числе драйвер, только что скопированный `drv install`), и
загрузка после такой записи занимала 128 секунд вместо двух. Не строить на
этом тесты: раздел C: печётся в образ прошивки —
`tools/mksysfs.py --board none --display-size WxH`, потом
`qemu-boot.ps1 -SysFs`. Тогда весь прогон — одна загрузка, и гость во флеш
ничего не пишет. Образец — `apps/desktop/check.ps1`.

**Приложение не должно рисовать до фокуса** (8 сентября). Процесс попадает в
слот сессии позже, чем начинает исполняться (здесь 90 мс), и до этого
`gfx_may_present` отбрасывал каждый `flush`: `acquire` отвечал «да», рисующие
вызовы молчали, на стекле оставалась консоль. Зависело от размера
поверхности — на 320×240 первый кадр терялся всегда, на 640×400 нет, потому
что вчетверо больше пикселей заливаются дольше, чем сессия выдаёт фокус.
Ядро исправлено: владелец дисплея может отправлять кадр, не дожидаясь фокуса
(сессия и так отбирает дисплей при уходе фокуса). Приложениям всё равно
следует ждать `ag_focused()` — образец в `apps/desktop/desktop.c`.

## Порядок дальнейших работ

Первые два пункта — по значимости, дальше по зависимостям.

1. **Модель процессов и супервизор — почти всё.** Сделано: процессы, арены, учёт
   ресурсов, `exec`/`spawn`/`wait`/`kill`, `Ctrl-C` и `Ctrl+\`, `ps`/`kill`/`fg`,
   подтаблицы ABI `proc` и `task`, перехват исключения, watchdog по заявке,
   crash-record в журнал и на диск. ✅ вывод несфокусированных процессов в
   журнал, ✅ `Alt+1..4` / `Alt+Tab`. Осталось по мелочи:
   * свой экран на процесс, а не общий на всех (разумно делать вместе с
     классом устройств `display`) — журнал это не заменяет.
2. ✅ **Документация для сторонних разработчиков** — написана, см. раздел
   [Документация для разработчиков](#документация-для-разработчиков), включая
   [`05-drivers.md`](sdk/05-drivers.md).
3. **Размер приложения — что осталось.** Данные и константы уже не ограничены
   ничем, кроме свободной PSRAM; ограничен код. Подробнее в
   [04-roadmap.md](04-roadmap.md):
   * ✅ **`.rodata` в часть данных** — шрифт 96 КБ стоит теперь 212 байт арены
     вместо 98 552, `AG_HOT_RODATA` оставляет отдельную таблицу в SRAM,
     `--rodata code` возвращает старое размещение целиком.
   * ✅ **Код в окно инструкций PSRAM** (спайк S-1, 26 августа 2026) — мегабайты
     кода: `execmem_hw.c` мапит PSRAM в оба окна, арена кода живёт в 512 КБ PSRAM.
   * ✅ **`.text` в раздел flash и XIP** (R-1) — авто при переполнении арены;
     раздел `appfs`, `apps/bigcode` в QEMU.
4. ✅ **Кодовая страница экрана** — `chcp 437|866|1251`, `[console] codepage` в
   `SYSTEM.CFG`, `con->codepage`/`set_codepage` в ABI (0.3). Ячейка хранит один
   байт, конвертация на краях. Проверено в QEMU: файл в CP866 читается как
   кириллица, набранная кириллица доходит до `echo`, рамка из символов CP437 с
   кириллицей внутри рисуется приложением через `poke`, а после `chcp 1251` те же
   байты честно становятся другими буквами. Осталось: научить `fm` рисовать
   рамки псевдографикой, когда страница её содержит.
5. **Модель устройств, `io`, `.SYS` и I2C-probe — сделаны.** ✅ Реестр, `/dev`,
   `ioctl`, `dev`; ✅ пины и шины, `io`; ✅ загрузчик модулей, `drv`, стадия
   `modules`; ✅ `modules.probe` / `drv probe` / `ag_probe_hint` (ABI 0.7),
   примеры `apps/echo`, `apps/whoami`.
   Дальше, по зависимостям:
   * ✅ на плате: **I2S** (класс `audio`), **ADC**, **DVP** (камера);
   * остальная периферия: TWAI(CAN), RS-485/Modbus, 1-Wire, RMT, MCPWM, DAC, RTC;
   * классы устройств со своими vtable (`class_ops`) сверх display.

   Это же фундамент под **универсальную сборку с приёмом драйверов на месте** —
   фаза 4.5 в [04-roadmap.md](04-roadmap.md), добавлена 6 августа 2026.
6. **Дисплей и ввод**: ✅ soft framebuffer + `gfx` (ABI 0.9 draw) в QEMU;
   ✅ **на плате** — `ILI9341.SYS` (консоль на стекле, графика через `blit_rect`),
   тач опрашивается консолью, класс `audio` через I2S. Открыто на железо: **USB
   host HID (клавиатура)** — единственный незакрытый пункт критерия v1; драйверы
   ST7789/SSD1306/RGB — тем же контрактом по мере плат.
7. ✅ **littlefs вместо FAT для `/sys`**
8. **Сеть**: ✅ сокеты, ✅ Wi-Fi на плате, ✅ имена (DNS), ✅ `wget` / HTTPD.AXE /
   `ftp` как команды системы — [10-network.md](10-network.md). Осталось:
   telnet-консоль как второй endpoint, TLS (и с ним `https`), SNTP, OTA. Приём
   драйверов и приложений по сети вместо UART теперь делается тем, что есть:
   `wget` кладёт `.AXE` прямо на карту.
9. ✅ **Спайк S-1 на железе** (26 августа 2026): исполнение кода из PSRAM
   доказано (`execmem_hw.c`). Осталось по мере надобности: замер латентности
   SRAM/PSRAM/flash-XIP и бенчмарки бюджетов из §10 архитектуры.
10. **Эмулятор Sega как `.AXE`** (фаза 3.5) — не развлечение, а проверка всей
    системы разом: код больше арены, полоса дисплея, звук, ввод, файлы. `MD.AXE`
    и `SMS.AXE` идут в QEMU со звуком; на плате есть код из PSRAM (S-1), I2S и
    дисплей. Осталось: замер % realtime на плате и USB-геймпад.

## Документация для разработчиков

Написана — это была фаза 2.5, и она закрыта, кроме драйверов, которым нужна
модель устройств.

* ✅ [`docs/sdk/01-getting-started.md`](sdk/01-getting-started.md) — от нуля до
  работающего приложения: тулчейн, сборка `.AXE`, доставка, запуск, таблица
  отказов загрузки.
* ✅ [`docs/sdk/02-api-reference.md`](sdk/02-api-reference.md) — весь `abi.h` по
  подсистемам: что делает каждый вызов, что возвращает, чего не делает; какие
  подтаблицы `NULL` и почему; правила совместимости и `AG_HAS()`.
* ✅ [`docs/sdk/03-application-anatomy.md`](sdk/03-application-anatomy.md) —
  манифест, точка входа, память, потоки, отсутствие libc, как быть снимаемым
  процессом, что печатается при падении.
* ✅ [`docs/sdk/04-axe-format.md`](sdk/04-axe-format.md) — формат образа со
  смещениями полей и полным списком проверок загрузчика, для тех, кто пишет свои
  инструменты сборки.
* ✅ [`docs/sdk/05-drivers.md`](sdk/05-drivers.md) — загружаемые `.SYS`:
  `AG_DRV`, `ag_driver_init`, `ag_dev_add`, `drv`, пример `apps/echo`.
* ✅ [`docs/user/01-shell.md`](user/01-shell.md) — все команды, перенаправление,
  диски, горячие клавиши; примеры — настоящие транскрипты из QEMU.
* ✅ [`docs/user/02-board-setup.md`](user/02-board-setup.md) — «как поднять
  ArgonOS на незнакомой плате», от требований к модулю до таблицы симптомов.
* ✅ примеры в `apps/`: `hello` (две части образа, константы, bss больше арены),
  **`disk` (файлы и диски)**, **`devs` (устройства и железо напрямую)**,
  **`echo` / `whoami` (`.SYS`)**, **`gfxdemo` (soft gfx)**, **`sms` (Master
  System, GPLv2+ core)**, `spin`, `leak`, `threads`, `crash`, `fm`, `edit`,
  **`cc` (tiny C → `.AXE`)**.
  Приложения проверяют себя сами (`run` + `errorlevel`); `echo` — через
  `drv load`; `whoami` — через probe hint; `edit` — встроенная команда;
  `gfxdemo` / `sms` — `AG_AXE_NEEDS_GFX` + `gfxdump`; `cc` — компиляция на
  госте (`run a:\cc.axe in.c out.axe`).

Написано после модели процессов, как и планировалось: подтаблицы `task` и `proc`
появились там же, и описывать ABI до них значило бы переписывать дважды.

**Чего документация стоила помимо текста** — два дефекта и один пробел, найденные
именно при попытке описать контракт словами:

1. `con->kbhit()` **забирал событие из очереди** и выбрасывал его, так что
   `if (kbhit()) c = getch();` — старейший идиом, какой есть, — терял клавишу и
   вставал на `getch`. Теперь есть `ag_console_peek_event`, который смотрит, не
   вынимая; проверено приложением, которое считает полученные символы.
2. `AG_APP` не давал задать **размер стека и арены**, хотя поля есть в формате
   образа и загрузчик их уважает. Добавлен `AG_APP_SIZED`; проверено: образ,
   попросивший 32 КБ стека и 2 МБ арены, получает ровно это.
3. `printf` ядра **обрезает вывод на 255 символах** (буфер форматирования один и
   на стеке). Не дефект, но то, о чём обязана сказать документация, иначе
   обрезанная строка выглядит как потеря вывода.
