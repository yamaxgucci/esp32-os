# Состояние проекта

> Читать первым при возобновлении работы. Здесь — что сделано, что нет, как
> собрать и проверить, и на какие грабли уже наступили.
>
> Дата: 5 августа 2026. Обновлять при каждой смене этапа.

## Что это

**ArgonOS** — ОС для ESP32 с моделью исполнения в стиле MS-DOS: приложение
владеет машиной, ОС — тонкий слой сервисов. Не клон Linux, не «поделка»:
целевое применение промышленное. Обоснование всех решений — в
[00-architecture.md](00-architecture.md), план — в [04-roadmap.md](04-roadmap.md),
конфигурация плат — в [03-board-config.md](03-board-config.md).

Основная платформа **ESP32-S3** с PSRAM 8 или 16 МБ.
**Модуль с 32 МБ PSRAM непригоден** — см. «Грабли» ниже.

## Как работать

Одна точка входа, `argon.cmd` в корне. Это батник, а не `.ps1`, потому что
Windows по умолчанию запрещает выполнение скриптов PowerShell.

```
argon build              собрать прошивку
argon run                запустить в QEMU, консоль в этом окне (Ctrl+A, X — выход)
argon run -Sd            то же, с образом SD-карты
argon run -tcp           консоль на 127.0.0.1:5556, подключаться PuTTY в Raw
argon test ver mem       автотест: загрузка, набор команд, печать экрана
argon test -Sd ...       то же с картой
argon tests              юнит-тесты на ПК (1002 проверки)
argon vt                 умеет ли эта консоль показывать экран
argon flash -port COM5   прошить настоящую плату
```

Машинно-зависимые пути живут в `tools/local-env.ps1` (не в git):

| Что | Где |
|---|---|
| ESP-IDF 5.5.5 | `D:\Espressif\esp-idf` |
| Инструменты IDF (xtensa gcc, cmake, ninja, QEMU) | `D:\Espressif\tools` |
| Портируемый CPython 3.12 | `D:\dev\python312` |
| MinGW-w64 GCC 16.1 (host-тесты) | `D:\dev\mingw64` |

**Всё на `D:`** — на `C:` почти нет места.

### Чем и как проверяется

Железа нет. Проверка трёхуровневая, и это работает:

1. **Юнит-тесты на ПК** (`argon tests`) — всё, что не зависит от чипа:
   пути, конфиг, экран, кодеки терминала, редактор строки, VFS, RAM-диск,
   журнал, загрузчик. Собирается тем же GCC, что и целевой код, поэтому
   диагностика совпадает.
2. **QEMU** (`argon test`) — прошивка реально загружается. Эмулируется и
   SD-хост, и PSRAM. `build-host\vtdump.exe` восстанавливает экран из
   транскрипта, прогоняя его через наш же модуль экрана.
3. **Железо** — только спайк S-1 (исполнение из PSRAM) и латентности.

## Что работает

| Подсистема | Где | Состояние |
|---|---|---|
| Загрузка по стадиям | `src/boot/kernel.c` | ✅ незаполненные стадии видны в отчёте `boot` |
| Определение чипа, профили | `src/boot/platform.c` | ✅ `full`/`lite`/`nano` |
| Описание платы | `src/boot/board.c` | ✅ дефолты + `C:\BOARD.CFG` |
| Журнал логов | `src/core/journal.c`, `log.c` | ✅ кольцо 4 КБ, команда `log` |
| Пути, конфиг, разбор команд | `src/core/path.c`, `cfg.c`, `cmdline.c` | ✅ |
| Текстовый экран + ANSI | `src/console/screen.c` | ✅ 80×25, отложенный перенос |
| Рендер в VT100 | `src/console/vtout.c` | ✅ по одному на endpoint |
| Разбор ввода терминала | `src/console/vtin.c` | ✅ HID-коды, UTF-8, мышь |
| Консоль-мультиплексор | `src/console/console.c` | ✅ UART, backpressure на вводе |
| VFS | `src/fs/vfs.c` | ✅ монтирование, владельцы, eject |
| RAM-диск `T:` | `src/fs/ramfs.c` | ✅ 1 МБ в PSRAM |
| FAT на flash `C:` | `src/fs/idfvfs.c`, `storage.c` | ✅ |
| FAT на SD `A:` | `storage.c` | ✅ SDMMC и SPI, `format a:` |
| Шелл | `src/shell/` | ✅ история, перенаправление, 22 команды |
| Загрузчик `.AXE` | `src/loader/`, `tools/mkaxe.py` | ✅ см. ниже |
| Таблица ABI | `src/loader/api.c` | ⚠ `sys mem fs con time`, остальные `NULL` |

Загрузка в QEMU: **172 мс с картой, 146 мс без**. Цель <400 мс.
Ядро занимает **~32 КБ** internal SRAM. Прошивка 409 КБ.

## Что не работает и о чём нельзя забывать

1. **Нет модели процессов.** Приложение исполняется на задаче шелла, `ag_exit`
   возвращает управление через `longjmp`. Дикий указатель уносит шелл. Нет
   `Ctrl-C`, `Ctrl-Alt-Del`, ресурсного списка, супервизора.
2. **Арена приложений 16 КБ.** Зарезервирована на линковке в D/IRAM, потому что
   это единственная память, одновременно исполняемая и записываемая через один
   адрес, а ESP-IDF на S3 не отдаёт её в кучу. Лимит снимается разделением
   образа на две части.
3. **Ячейка экрана — один байт.** Не-ASCII показывается как `?`. Нужна кодовая
   страница (CP437/CP866, аналог `chcp`).
4. **`Ctrl+C` не доходит до гостя** в консольном режиме QEMU: он оставлен
   консоли, чтобы можно было выйти из зависшего эмулятора.
5. **Нет сети, дисплея, USB, драйверов периферии, `littlefs`.**
6. **`/sys` на FAT, а не littlefs** — не переживёт пропадание питания посреди
   записи. Замена одного вызова в `storage.c`.

## Формат `.AXE` в двух абзацах

Приложение линкуется на хосте одним непрерывным образом по номинальному адресу.
При переносе по другому адресу **все PC-относительные инструкции остаются
верными** — сдвигаются и инструкция, и цель. Править надо только абсолютные
32-битные слова (записи литерального пула с адресами). Поэтому в ядре нет ни
ELF-парсера, ни декодера инструкций: `tools/mkaxe.py` выдаёт список смещений
слов, загрузчик копирует, добавляет смещение и прыгает.

Собрать приложение:

```
python tools/mkaxe.py --arch xtensa --gcc xtensa-esp32s3-elf-gcc \
    --include sdk/include -o HELLO.AXE hello.c
```

Доставить в эмулятор: `recv t:\hello.axe`, затем строки hex, затем `END`.

## Грабли, которые уже стоили времени

Каждая из них проявлялась как что-то совсем другое.

1. **Окно внешней памяти S3 — 32 МБ, общее для flash и PSRAM.** При 32 МБ PSRAM
   `spi_flash_mmap` не находит адресов, и `esp_partition` **не видит ни одного
   раздела** — потому что читает таблицу через отображение. Выглядит как
   «сломана таблица разделов», хотя `esp_flash_read` по тому же адресу отдаёт
   корректные байты. Плата с 32 МБ PSRAM непригодна; QEMU запускается с 8 МБ.
2. **`src/con/` — зарезервированное имя устройства Windows.** Git молча
   пропускает такой каталог: заголовок закоммитился, `.c` — нет. Запрещены
   `con aux prn nul com1..9 lpt1..9`.
3. **`MALLOC_CAP_EXEC | MALLOC_CAP_8BIT` даёт ноль.** IDF помечает исполняемую
   память как только-словную — наследие оригинального ESP32.
4. **PowerShell может встать в середину вывода дочернего процесса.** Оператор
   вызова `& $qemu` заставляет PowerShell читать вывод как текст и печатать
   заново своим рендерингом, который не интерпретирует escape-последовательности
   — экран приходит стеной `←[2J` при правильно настроенной консоли. Нужен
   `Start-Process -NoNewWindow`.
5. **Классическая консоль Windows не интерпретирует ANSI**, пока не включить
   `ENABLE_VIRTUAL_TERMINAL_PROCESSING`, причём на handle, открытом как
   `CONOUT$` по имени — `GetStdHandle` не всегда консоль.
6. **TCP-порт QEMU без подключённого клиента выбрасывает вывод.** Нужен
   `wait=on`, иначе тест гоняется с эмулятором и иногда проигрывает.
7. **Логи через экран теряются при всплеске** — рендерер отправляет текущее
   состояние строки. Ради этого и сделан журнал.
8. **Длинные имена в FAT у ESP-IDF выключены по умолчанию.**
9. **Splatting массива в PowerShell передаёт аргументы позиционно** — `-Sd`
   приехал как значение `-Marker`. Нужен hashtable.
10. **`void**` на 64-битном хосте читает 8 байт** там, где в образе 32-битное
    слово. Образ — 32-битное адресное пространство независимо от машины.

## Порядок дальнейших работ

Первые два пункта — по значимости, дальше по зависимостям.

1. **Модель процессов и супервизор.** Приложение на своей задаче и своём ядре
   CPU; ресурсный список (память, файлы, таймеры, ISR); `exec`/`spawn`/`wait`/
   `kill`; `Ctrl-C` и `Ctrl-Alt-Del`; перехват исключений с определением
   виновника; crash-record в журнал и на диск. Подтаблицы ABI `task` и `proc`.
   Команды `ps`, `kill`, `fg`. **Без этого любое приложение опасно.**
2. **Документация для сторонних разработчиков** — см. следующий раздел.
3. **Двухчастный образ**: код в окно инструкций (PSRAM, мегабайты), данные в
   окно данных, признак части в таблице релокаций. Снимает лимит 16 КБ.
4. **Кодовая страница экрана** (CP437 по умолчанию, CP866 для кириллицы,
   команда `chcp`). Ячейка хранит один байт, конвертация на входе и выходе.
5. **Модель устройств и драйверы**: `/dev`, регистрация, загружаемые `.SYS`,
   `DEVICE=` в `SYSTEM.CFG`, подтаблицы `dev` и `io`. Дальше конкретная
   периферия: I2C, SPI, UART, TWAI, RS-485/Modbus, PWM, ADC.
6. **Дисплей и ввод**: класс `display`, драйверы ST7789/ILI9341/SSD1306, рендер
   текстового экрана шрифтом, переключение текст/графика, подтаблица `gfx`;
   USB host для HID-клавиатуры, подтаблица `inp`. Это закрывает критерий v1.
7. **littlefs вместо FAT для `/sys`** — переживает пропадание питания.
8. **Сеть**: Wi-Fi, сокеты, telnet-консоль как второй endpoint, подтаблица `net`.
9. **Спайк S-1 на железе**: исполнение из PSRAM, замер латентности, бенчмарки
   бюджетов из §10 архитектуры.

## Документация для разработчиков (запланировано)

Отдельная задача, пункт 2 выше. Что должно появиться:

* `docs/sdk/01-getting-started.md` — от нуля до работающего приложения:
  установка, сборка `.AXE`, доставка на плату, запуск.
* `docs/sdk/02-api-reference.md` — полный разбор `argon/abi.h` по подсистемам:
  что делает каждый вызов, что возвращает, чего не делает. Правила
  совместимости и `AG_HAS()`.
* `docs/sdk/03-application-anatomy.md` — `AG_APP()`, `ag_main`, аргументы, код
  возврата, что можно и нельзя (нет libc по умолчанию, нет потоков пока).
* `docs/sdk/04-axe-format.md` — формат образа для тех, кто пишет свои
  инструменты.
* `docs/sdk/05-drivers.md` — когда появится модель устройств.
* `docs/user/01-shell.md` — все команды с примерами, перенаправление, диски.
* `docs/user/02-board-setup.md` — расширение `03-board-config.md` до
  «как поднять ArgonOS на незнакомой плате».
* `apps/` — примеры, которые собираются и работают: hello, чтение файла,
  работа с диском, графика (когда будет).

Писать **после** модели процессов: ABI подтаблиц `task`/`proc` появятся там же,
и документировать её до этого — значит переписывать дважды.
