# ArgonOS partition table - 8 MB flash baseline.
# Larger flash sizes get a second app slot for OTA; see docs/00-architecture.md.
#
# sysfs holds configuration and the log and is mounted at /sys, drive C:.
# appfs holds relocated .AXE code for flash XIP (R-1).  Raw data subtype 0x40,
# not a filesystem: the loader erases, programs and mmap()s slots itself.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x300000,
sysfs,      data, fat,      0x310000, 0x100000,
appfs,      data, 0x40,     0x410000, 0x3F0000,
