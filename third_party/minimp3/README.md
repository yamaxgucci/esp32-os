# minimp3 (vendored)

Upstream: https://github.com/lieff/minimp3 (CC0 1.0).

Single-header MP3 decoder. Used by `apps/common/mp3` / `AMP.AXE`.

Build with `MINIMP3_IMPLEMENTATION` in exactly one translation unit and
`MINIMP3_NO_SIMD` on Xtensa.
